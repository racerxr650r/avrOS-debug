<?xml version="1.0" encoding="UTF-8"?>
<!--
  Project.xml — single source of truth for this project's spec stack.
  See tools/Developers_Guide.md for the schema reference.

  This file was created by `render_doc.py` in init mode. Fill in the
  payload sections (sdd, stp, hlrs, llrs, tests) as the project takes
  shape, then regenerate the markdown specs with `render_doc.py`.
-->
<project name="avrOS-debug" short_name="aod" schema_version="1.3"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="../../../.vscode/extensions/tracer.vscode-project-xml-0.1.0/dist/python/project.xsd">
  <metadata>
    <document id="SDD"          title="Software Design Document"     source="doc/SDD.md"          version="0.1" date="2026-05-16" author="John Anderson"/>
    <document id="HLRs"         title="High-Level Requirements"      source="doc/HLRs.md"         version="0.1" date="2026-05-16" author="John Anderson"/>
    <document id="LLRs"         title="Low-Level Requirements"       source="doc/LLRs.md"         version="0.1" date="2026-05-16" author="John Anderson"/>
    <document id="STP"          title="Software Test Plan"           source="doc/STP.md"          version="0.1" date="2026-05-16" author="John Anderson"/>
    <document id="Traceability" title="Traceability Matrix"          source="doc/Traceability.md" version="0.1" date="2026-05-16" author="John Anderson"/>
    <counts>
      <count name="hlrs"       value="43"/>
      <count name="llrs"       value="63"/>
      <count name="tests"      value="115"/>
      <count name="test_files" value="8"/>
    </counts>
  </metadata>

  <!-- Software Design Document payload (see tools/Developers_Guide.md §3). -->
  <sdd>
    <kind>command-line GDB stub application</kind>
    <audience>embedded application developers and toolchain integrators</audience>

    <scope>
      <intro>describes the design of the source modules that implement the avrOSdb server</intro>
      <file path="src/main.c">Entry point, CLI argument parsing, and the top-level select()-based event loop.</file>
      <file path="src/updi.c">UPDI physical layer: UART serial management, UPDI protocol framing, NVM flash programming, and UPDI console bridge.</file>
      <file path="src/gdb_rsp.c">GDB Remote Serial Protocol server: packet codec, command dispatch, and session lifecycle management.</file>
      <file path="src/elf_parser.c">ELF parser: locates avrOS FLASH-resident system tables and produces the AvrOsSymbolIndex.</file>
      <file path="src/fsm_mapper.c">avrOS FSM-to-GDB virtual thread translator: maps FSM state tables to GDB thread objects and synthesizes per-thread register frames.</file>
      <file path="src/monitor.c">Custom monitor command handler: implements the `avros events` and `avros queues` sub-commands via non-intrusive UPDI reads.</file>
      <file path="Makefile">Build orchestration: compile, test, install, uninstall, check-tools, and bundle (Debian .deb, Red Hat .rpm, Homebrew formula) targets.</file>
      <file path="tests/hw/hw_test.c">On-target hardware integration test harness: links against `src/updi.c` and exercises the live UPDI silicon, device-info report, SRAM round-trips, NVM page programming, and the RSP server's TCP path. Manual-only — never wired into `make test`.</file>
      <file path="doc/avrOSdb.1">Unix man page: reference documentation for the avrOSdb command.</file>
      <outro><![CDATA[It does not cover the build system, IDE adapter layers (Cortex-Debug, Zed DAP), or Windows support, all of which are out of scope for the initial release (see `doc/PVD.md §7.2`).]]></outro>
    </scope>

    <overview>
      <para><![CDATA[`avrOSdb` is a POSIX C99 GDB stub that bridges the UPDI debug interface of modern AVR microcontrollers (DA/DB families) to standard IDEs over the GDB Remote Serial Protocol (RSP). A developer launches the stub, points it at a serial device and an ELF binary, and connects any GDB-compatible front-end — VS Code with Cortex-Debug, Zed with its DAP adapter, or bare `avr-gdb` — using the standard `target extended-remote` command.]]></para>
      <para><![CDATA[The server connects to the target via a TTL-level UART serial adapter with a 1 kΩ resistor on the UPDI line. No external JTAG programmer or proprietary debugger hardware is required — only a USB-to-serial adapter or direct Raspberry Pi UART pins.]]></para>
      <para><![CDATA[Unlike conventional GDB stubs that expose a flat memory model, `avrOSdb` provides native avrOS state-machine awareness. At attach time it parses the supplied ELF binary to locate the avrOS FSM registration tables in FLASH, then presents each registered finite state machine as a standard GDB virtual thread. The currently executing FSM appears as the active thread; all suspended FSMs appear as additional threads, each with a synthetic register frame whose PC points to the FSM's current state function pointer.]]></para>
      <para><![CDATA[The project ships with a `make install` target that installs the compiled binary to `$(PREFIX)/bin/` and the accompanying Unix man page to `$(PREFIX)/share/man/man1/`. A `make check-tools` target validates that all required build tools (`gcc`, `make`, `avr-gcc`, `avr-binutils`) are present on the host before any compilation is attempted. A `make bundle` target produces native distribution packages for three platforms: a Debian binary package (`.deb`), a Red Hat RPM package (`.rpm`), and a Homebrew formula (`dist/avrOSdb.rb`) for macOS. All output artefacts are written under the `dist/` directory. These targets ensure the project can be built, deployed, and distributed by a developer from a single `make` command sequence with no manual file copying.]]></para>
      <para><![CDATA[**Planned — Phase 9 — CI-Grade Loader & Link Diagnostics (not yet implemented).** A forthcoming iteration extends the programmer and the `--device` diagnostic mode with the following capabilities so the tool can be trusted as a CI build/flash step and used as a first-line link-health probe. Tracked in GitHub issue #28; not yet bound to HLRs/LLRs.

* **Read-back verify after `--load`.** After every successful NVM write, re-read the affected pages via UPDI and compare against the ELF payload. Mismatching pages shall be reported by `(window, page-aligned address, expected-CRC, actual-CRC)`, the application shall exit with a distinct non-zero exit code (`2`, reserved for verify failure as opposed to `1` for I/O failure), and shall not enter debug or open the GDB listener. Verify shall cover FLASH, EEPROM, USERROW, and FUSES windows; LOCK is excluded because lockbits become read-only post-write.
* **`--prog` program-and-exit mode.** A new mutually-exclusive operating mode that programs the supplied ELF into NVM, verifies it (as above), and exits — no GDB listener is opened, no `updi_enter_debug()` is issued, the UPDI link is dropped cleanly with the target left running. Mode shall print, to `stdout`, the negotiated `baud=<N>` line at start-up and a single-line ANSI-aware progress bar (`[#####.....] 53%  page 27/51 erasing|writing|verifying <window>`) that updates in place when `stdout` is a TTY and degrades to one line per phase transition when piped. Intended use is `avrOSdb --prog /dev/ttyUSB0 firmware.elf` from a CI script or a Makefile `flash:` target.
* **Fuses pretty-printer (`--device` mode).** Extend the existing `--device` SIGROW/REVID dump (LLR-MAIN-08, LLR-UPDI-26) to read all FUSE bytes, then decode each bit-field against the per-family fuse table (BODCFG, OSCCFG, SYSCFG0/1, CODESIZE, BOOTSIZE, …) using human-readable enum names rather than raw hex. Lock byte is decoded with the same scheme. Output format mirrors avrdude's `-Tu` so existing tooling can consume it.
* **Auto-baud / link-quality probe (`--device` mode).** Replace the hard-coded 115200 baud with a probe that walks a candidate ladder (`230400, 200000, 150000, 115200, 57600, 38400, 19200`), at each step opening the link, issuing a small fixed UPDI transaction sequence (LDCS, NVMCTRL STATUS read × N), and recording the byte error / retry rate. The highest rate with zero retries over the sample window becomes the reported negotiated baud. Result is printed as `baud=<N> errors=<K>/<total>` per candidate and the chosen rate is highlighted. Probe is read-only; no NVM, fuses, or system-reset paths are touched.

Phase 9 work shall reuse the existing layered architecture (§2.1): the NVM read primitives live in `src/updi.c`, the verify and progress-bar wiring lives in `src/main.c`, and the fuse-decode tables live alongside the per-family `g_device_table[]` in `src/updi.c` so they are selected by the same `updi_select_device()` path. No new top-level module is introduced.]]></para>
      <para><![CDATA[**Planned — Phase 10 — GDB Protocol Completion (avarice Feature Parity) (not yet implemented).** The Phase 1–8 RSP server implements the minimum subset of the GDB Remote Serial Protocol needed to attach, read state, single-step, set up to two hardware breakpoints, continue, interrupt, and detach. Phase 10 brings the protocol surface up to functional parity with the legacy `avarice` JTAG/dW stub for the AVR-Dx UPDI use case: equivalent `monitor` verbs, equivalent `(gdb) load` behaviour, equivalent watchpoint and software-breakpoint experience, and equivalent extended-remote lifecycle. Wire-level drop-in compatibility with `avarice`-specific scripts is an explicit non-goal — individual `.gdbinit` files, IDE launch configurations, and CI invocations may need targeted edits to reach the same outcome. Tracked in GitHub issue #31; bound to HLRs in §12 below.

The following protocol features and monitor commands are in scope; each is justified by an `avarice` capability that today forces the operator either to drop out of the GDB session or to bridge through a second tool (avrdude, pymcuprog, raw scripts).

* **Flash programming from the GDB session (`vFlashErase`, `vFlashWrite`, `vFlashDone`).** Implements `(gdb) load` so a developer can rebuild and reflash without exiting `avr-gdb`. The handlers shall reuse the existing `updi_nvm_*` page primitives in `src/updi.c`; on `vFlashDone` the application shall re-enter OCD via `updi_enter_debug()` and present the post-load CPU as halted at reset for the next `c`/`s`. `vFlashErase` requests outside the on-target FLASH window (i.e. into EEPROM/USERROW/FUSES) shall be rejected with `E22` rather than silently corrupting non-FLASH NVM, because `gdb load` has no per-section verb to disambiguate.
* **Software breakpoints via flash `BREAK` opcode (`Z0`/`z0` true SW BP).** Phase 1–8 aliases `Z0` to the two OCD hardware comparators (HLR-016), capping the user at two simultaneous breakpoints. Phase 10 adds a true software-breakpoint path that patches the AVR `BREAK` opcode (`0x9598`) into the affected FLASH page, with a per-session shadow map of `(address, original 2-byte opcode)` so `z0` restores the original instruction on remove. The save/patch/restore sequence shall preserve live register, SREG, and SP state across the unavoidable NVMPROG enter/exit (which issues a CPU reset). Implementation shall reuse the page-aligned read-modify-write helper from `src/updi.c`. When the operator opts out via `monitor avros bp-mode hw-only`, `Z0` continues to alias to the HW comparators (the Phase 1–8 behaviour).
* **Hardware data watchpoints (`Z2`/`Z3`/`Z4`, `z2`/`z3`/`z4`).** Exposes the AVR-Dx OCD data-address comparators (`DABP0`, `DABP1`) as GDB write (`Z2`), read (`Z3`), and access (`Z4`) watchpoints. Implementation lives in `src/updi.c` (new `updi_ocd_set_data_bp()` / `_clear_data_bp()` primitives) and `src/gdb_rsp.c` (new `on_insert_wp`/`on_remove_wp` handler slots). Watchpoint hits shall be reported in the stop-reply packet with the standard `T05watch:<addr>;` / `rwatch` / `awatch` keys so the IDE displays the offending address.
* **Standard GDB monitor commands.** The `monitor avros` namespace is kept (and remains the home for FSM-specific introspection); Phase 10 adds an `avarice`-compatible top-level set of monitor verbs: `monitor reset` (issue `updi_reset()` and halt), `monitor halt` (call `updi_halt()`), `monitor go` (call `updi_run()`), `monitor erase` / `monitor chip-erase` (CHIPERASE via `updi_chip_erase()`, refused unless the session was opened with `--allow-erase`), `monitor version` (print server version, git hash, and target family), and `monitor help` (list every known verb). Implementation lives in `src/monitor.c`; the dispatcher shall continue to route the `avros` prefix to the existing avrOS-specific handlers, falling through to the new generic handlers for any other verb.
* **Extended-remote lifecycle and protocol cleanup.** Implements `vRun`/`vAttach`/`vKill` so an IDE can launch and relaunch the debug target from one persistent GDB session (`target extended-remote` semantics), and fills in the small standard packets that `avarice` answers but Phase 1–8 leaves as empty replies: `qC` (current thread), `qOffsets` (returns zeros for our position-independent reset vector), `T<tid>` (is-thread-alive), and `R` (RCmd restart, equivalent to `monitor reset` + `c`). These are individually trivial but cumulatively eliminate the "selected thread is gone" and "unsupported feature" spurious warnings that some IDE configurations promote to errors.

Phase 10 work shall continue to honour the layered architecture (§2.1) and the FSM-first principles (`doc/PVD.md §6.1`): no SW-breakpoint or watchpoint installation may unwind or rewrite FSM virtual-thread state, and `monitor reset` / `monitor chip-erase` shall always invalidate the FSM context before silicon side-effects so the next register/memory query sees a coherent thread list. The single-threaded `select()` event loop and `updi_*` primitive APIs are unchanged.]]></para>
    </overview>

    <definitions>
      <term name="UPDI"><![CDATA[Unified Program and Debug Interface — the single-wire debug protocol used by AVR DA/DB-family microcontrollers. Carried over a UART at the configured baud rate with a 1 kΩ isolation resistor on the UPDI pin.]]></term>
      <term name="RSP"><![CDATA[GDB Remote Serial Protocol — the text-based packet protocol that `avr-gdb` uses to communicate with a remote stub over a socket or serial link.]]></term>
      <term name="FSM"><![CDATA[Finite State Machine — the cooperative task unit in avrOS, represented at runtime by a function pointer into a FLASH-resident state table. All FSMs share the single hardware stack.]]></term>
      <term name="ELF"><![CDATA[Executable and Linkable Format — the binary file produced by `avr-gcc` that carries machine code, DWARF debug information, and the symbol table used to locate avrOS system tables.]]></term>
      <term name="avrOS"><![CDATA[A cooperative real-time operating system for AVR microcontrollers. Tasks are modelled as FSMs; inter-task communication uses queues and named events.]]></term>
      <term name="Virtual Thread"><![CDATA[A GDB thread object synthesized from an avrOS FSM entry. Each virtual thread has a unique GDB thread ID and a synthesized register frame whose PC is set to the FSM's current state function pointer.]]></term>
      <term name="Harvard Architecture"><![CDATA[The AVR memory model, in which FLASH (program memory) and SRAM (data memory) occupy separate address spaces. ELF virtual addresses must be converted to physical FLASH word addresses during symbol resolution.]]></term>
    </definitions>

    <references>
      <ref><![CDATA[Product Vision Document: [doc/PVD.md](PVD.md)]]></ref>
      <ref><![CDATA[GDB Remote Serial Protocol: https://sourceware.org/gdb/current/onlinedocs/gdb.html/Remote-Protocol.html]]></ref>
      <ref><![CDATA[Microchip AVR UPDI Programming Reference (AN3272): https://www.microchip.com/en-us/application-notes/an3272]]></ref>
      <ref><![CDATA[System V ABI ELF Specification: https://refspecs.linuxfoundation.org/elf/elf.pdf]]></ref>
      <ref><![CDATA[AVR-LibC Reference Manual: https://avrdudes.github.io/avr-libc/]]></ref>
    </references>

    <architecture>
      <intro><![CDATA[`avrOSdb` is a single-process C99 application. Responsibilities are divided into six source modules arranged in protocol layers: an entry-point and event-loop layer, a hardware layer (UPDI), a network layer (GDB RSP), and three application-layer modules (ELF parser, FSM mapper, monitor handler).

**Concurrency model:** The application is entirely single-threaded. All I/O multiplexing is performed by a single `select()` call inside `event_loop()`. No POSIX threads (`pthreads`) are used at any point. All module entry points are called synchronously and must return before the next I/O event can be processed. Long-blocking operations (e.g., NVM flash write, UPDI link initialisation) are therefore only permitted at startup or in response to explicit GDB commands, never in the event-loop hot path.

**Inter-module data flow:**

| Producing module | Data produced | Consuming module |
| ---------------- | ------------- | ---------------- |
| `src/main.c` | `AppConfig` (argv, fd values) | All modules (passed by pointer at init) |
| `src/updi.c` | Raw target memory bytes, execution-control status | `src/gdb_rsp.c`, `src/fsm_mapper.c`, `src/monitor.c` |
| `src/elf_parser.c` | `AvrOsSymbolIndex` (resolved FLASH/SRAM addresses) | `src/fsm_mapper.c`, `src/monitor.c` |
| `src/fsm_mapper.c` | `FsmContext` (virtual thread list, synthesized register frames) | `src/gdb_rsp.c` |
| `src/monitor.c` | Formatted O-packet text | `src/gdb_rsp.c` (via `rsp_send_packet()`) |
| `src/gdb_rsp.c` | Decoded RSP packet payload strings, thread-selection state | `src/updi.c`, `src/fsm_mapper.c`, `src/monitor.c` |

**Resource ownership:**

| Resource | Owner module | Lifetime | Open call | Close call |
| -------- | ------------ | -------- | --------- | ---------- |
| UART serial fd | `src/updi.c` | Process | `updi_open()` | `updi_close()` |
| GDB TCP listen socket | `src/gdb_rsp.c` | Process | `rsp_listen()` | `rsp_close()` |
| GDB TCP client socket | `src/gdb_rsp.c` | Session | `rsp_accept()` | detach / `rsp_close()` |
| ELF file descriptor + heap buffers | `src/elf_parser.c` | Session | `elf_open()` | `elf_close()` |
| `FsmContext` static array | `src/fsm_mapper.c` | Session | `fsm_build_thread_list()` | `fsm_invalidate()` |
| `AppConfig` struct | `src/main.c` (stack) | Process | `parse_args()` | implicit on `main()` return |]]></intro>
      <component path="src/main.c"><![CDATA[Entry point; owns the top-level `select()`-based event loop and coordinates all other layers.]]></component>
      <component path="src/updi.c"><![CDATA[UPDI physical layer; owns the UART serial file descriptor, UPDI protocol framing, execution-control primitives (halt, run, step), NVM flash programming, and the UPDI console bridge.]]></component>
      <component path="src/gdb_rsp.c"><![CDATA[GDB RSP layer; owns the GDB TCP socket, RSP packet codec, and command dispatch table.]]></component>
      <component path="src/elf_parser.c"><![CDATA[ELF parser; produces the `AvrOsSymbolIndex` (FLASH addresses of avrOS system tables) at attach time.]]></component>
      <component path="src/fsm_mapper.c"><![CDATA[FSM mapper; reads UPDI memory using the symbol index and exposes avrOS FSMs as GDB virtual threads.]]></component>
      <component path="src/monitor.c"><![CDATA[Monitor handler; implements the custom `monitor avros events|queues` introspection commands without halting the CPU.]]></component>
      <flow>
        <intro>The startup and attach sequence proceeds as follows:</intro>
        <step><![CDATA[`main()` parses argv, opens the UART serial device, and starts the GDB TCP listener socket.]]></step>
        <step><![CDATA[A GDB client connects; the RSP layer sends the initial hello and negotiates capabilities via `qSupported`.]]></step>
        <step><![CDATA[On the first `vAttach` or `?` packet, the ELF parser scans the supplied `.elf` file and populates the `AvrOsSymbolIndex` with avrOS FSM table addresses.]]></step>
        <step>The FSM mapper reads the FLASH-resident tables via UPDI background reads and builds the virtual thread list.</step>
        <step><![CDATA[Subsequent GDB packets (register reads, memory reads, breakpoints, step/continue) are dispatched through the RSP layer to the UPDI layer.]]></step>
        <step><![CDATA[`monitor avros` commands are intercepted by the monitor handler, which issues UPDI background reads without halting the CPU core.]]></step>
      </flow>
    </architecture>

    <design_goals>
      <goal name="FSM-First Visualization"><![CDATA[Virtual threads map 1-to-1 to avrOS FSM table entries. The server never attempts to unwind dormant stack frames; execution context is defined solely by the FSM's current state function pointer.]]></goal>
      <goal name="Lean Host Architecture"><![CDATA[The server is a single C99 process. Runtime dependencies are limited to libc and the POSIX serial and socket APIs. No Java, Python virtualization, or Electron runtime is required on the host.]]></goal>
      <goal name="Editor-Agnostic Core"><![CDATA[The server exposes only standard GDB RSP. IDE-specific integration (Cortex-Debug, Zed DAP adapter) is handled entirely by the GDB client; the server does not implement any IDE extension protocol.]]></goal>
      <goal name="Non-Intrusive Polling"><![CDATA[`monitor avros` commands use UPDI background reads, which can be issued while the CPU is running. The core is halted only by an explicit user breakpoint or a step/continue boundary.]]></goal>
      <goal name="Fail-Safe Address Resolution"><![CDATA[If ELF symbol resolution fails to locate the avrOS system tables, the server degrades gracefully to a standard bare-metal GDB stub rather than refusing to connect.]]></goal>
      <goal name="Single-Threaded Concurrency Model"><![CDATA[The server uses a single POSIX `select()` event loop with no POSIX threads. All module entry points are synchronous and return to the event loop promptly. Long-blocking operations (NVM programming, UPDI link initialisation) are permitted only at startup or in direct response to an explicit GDB command, never during the event-loop hot path.]]></goal>
      <goal name="Deterministic Memory Allocation"><![CDATA[Static and stack allocation are used for all hot-path data structures (`AppConfig`, `FsmContext`, RSP packet buffers, breakpoint table). The only heap (`malloc`) usage is in `elf_open()` to load the ELF symbol and string tables; this allocation is bounded by the size of the ELF binary and occurs once per session at attach time. `elf_close()` frees all heap memory on detach.]]></goal>
      <goal name="POSIX C99 Build"><![CDATA[The stub is compiled with a host C99 compiler (gcc ≥ 4.8 or clang ≥ 3.4) against the POSIX.1-2008 API. No compiler-specific extensions, no C11 atomics, and no dynamic libraries beyond libc are required. The Makefile produces a standalone executable with no runtime package dependencies.]]></goal>
      <goal name="Modular Test Isolation"><![CDATA[Each module exposes a public C API declared in a matching `.h` header. The UPDI layer can be exercised against a loopback serial device or pre-recorded byte stream. The ELF parser and FSM mapper can be driven against any AVR ELF binary without live hardware. The RSP layer can be tested over a loopback TCP socket. No module requires a live AVR target to run its unit tests.

A companion **on-target hardware integration harness** (`tests/hw/hw_test.c`) supplements the PTY-based unit tests by exercising the same UPDI public API against a real AVR-Dx target connected via a serial device. The harness is manual-only and is never invoked by `make test` (which must remain hardware-independent for CI). It is driven by the dedicated Makefile targets `hw-test`, `hw-test-nvm`, `hw-test-rsp`, and `hw-test-all`; destructive operations (FLASH erase/write, RSP-server spawn) are gated behind explicit opt-in variables.]]></goal>
    </design_goals>

    <modules>
      <module path="src/main.c">
        <purpose><![CDATA[is the entry point for the `avrOSdb` executable, providing CLI argument parsing, resource initialization, and the top-level `select()`-based event loop that multiplexes the GDB client socket and the UPDI serial device.]]></purpose>
        <responsibility><![CDATA[Define `main()` and parse command-line arguments into an `AppConfig` struct.]]></responsibility>
        <responsibility>Open the UART serial device and pass the file descriptor to the UPDI layer.</responsibility>
        <responsibility>Create the GDB listener socket and pass it to the RSP layer.</responsibility>
        <responsibility><![CDATA[Run the event loop, dispatching each ready file descriptor to the appropriate layer until the GDB client disconnects.]]></responsibility>
        <responsibility>Release all resources on exit and return an appropriate exit code.</responsibility>
        <responsibility><![CDATA[When `--device` is supplied, execute the one-shot device-info diagnostic path: open UPDI, call `updi_read_device_info()`, print the formatted report to `stdout`, and return without entering `event_loop()`. Every failure on this path emits a verbose diagnostic on `stderr` that names the failed UPDI step.]]></responsibility>
        <interfaces>
          <interface title="Public C API"><![CDATA[`src/main.c` has no exported public API; all functions are `static` or `main()` itself. It is the top-level consumer of all other module APIs. The only externally visible symbol is:

```c
extern volatile sig_atomic_t g_quit;  /* set to 1 by SIGINT/SIGTERM handler */
```

This variable is checked at the top of each `event_loop()` iteration and is also accessible to any module that needs to detect a shutdown-in-progress condition.]]></interface>
          <interface title="Command-Line Arguments"><![CDATA[`avrOSdb [--port <port>] [--baud <baud>] [--erase] [--load] [--allow-lock-updi] <serial-device> <elf-file>`
`avrOSdb --device [--baud <baud>] <serial-device> [elf-file]`

*   `--port <port>` — TCP port for the GDB listener (default: `1234`).
*   `--baud <baud>` — UART baud rate (default: `115200`).
*   `--erase` — issue a UPDI chip-erase before any `--load` step; required when the ELF contains a LOCK segment.
*   `--load` — program every `PT_LOAD` segment of the ELF to the matching NVM kind (FLASH, EEPROM, USERROW, FUSES, LOCK). SIGROW segments are skipped (read-only). Mutually exclusive with `--device`.
*   `--allow-lock-updi` — when set, `--load` is allowed to write LOCK byte patterns that would disable the UPDI interface (`UPDIDIS`); without this flag only the 4-byte unlock pattern `0x5CC5C55C` is accepted.
*   `--device` — one-shot diagnostic mode: open the UPDI link, read the target SIGROW and ASI status, print a verbose human-readable report to `stdout`, and exit without binding the GDB listener. `<elf-file>` is optional in this mode. Mutually exclusive with `--load`.
*   `<serial-device>` — path to the UART device (e.g. `/dev/ttyUSB0`).
*   `<elf-file>` — path to the AVR ELF binary.]]></interface>
        </interfaces>
        <data_structures><![CDATA[The event loop multiplexes three file descriptors using POSIX `select()`:

*   **`int listen_fd`** — passive TCP listener socket; present in `fd_set` at all times.
*   **`int gdb_fd`** — active GDB client socket; `-1` when no client is connected; added to `fd_set` only while a session is active.
*   **`int updi_fd`** — UART serial device; added to `fd_set` while a GDB session is active to forward unsolicited console traffic.

All three file descriptors are stored directly in `AppConfig`. The maximum fd value is recomputed before each `select()` call to keep the fd_set range tight.]]></data_structures>
        <functions>
          <function signature="int main(int argc, char *argv[])"
                    summary="Parse argv, open resources, run the event loop, and return an exit code."/>
          <function signature="static void parse_args(int argc, char *argv[], AppConfig *cfg)"
                    summary="Walk argv and populate cfg; print usage to stderr and exit on error."/>
          <function signature="static int run_device_mode(const AppConfig *cfg)">
            <purpose><![CDATA[Diagnostic path taken when `cfg->device_info` is true. Opens the UPDI link, invokes `updi_read_device_info()`, prints a verbose human-readable report to `stdout`, and returns the process exit code (0 on success, 1 on any UPDI failure). Skips ELF loading, `rsp_listen()`, and the entire event loop.]]></purpose>
            <pre><![CDATA[`cfg->serial_device` is non-NULL; `cfg->baud_rate` is positive; `cfg->device_info` is true.]]></pre>
            <post>UPDI fd is closed before return; no other resources are allocated.</post>
            <returns>0 on success; 1 on any failure (UPDI open, BREAK/SYNCH, or any SIGROW/ASI read).</returns>
            <logic>
              <step><![CDATA[`updi_open(cfg->serial_device, cfg->baud_rate)` \u2014 on failure, print `"error: cannot open UPDI device '<dev>': <errno-text>"` to `stderr` and return 1.]]></step>
              <step><![CDATA[`updi_read_device_info()` \u2014 on failure, print `"error: <fail_op> failed (rc=<fail_errno>) \u2014 check wiring, target power, UPDIDIS fuse"` to `stderr`, close UPDI, and return 1.]]></step>
              <step><![CDATA[Look up the 3-byte signature in the static `device_family[]` table; print `Serial device`, `Baud rate`, `Signature`, `Family`, `Revision`, `Serial`, and `UPDI status` lines to `stdout`.]]></step>
              <step>Close UPDI and return 0.</step>
            </logic>
          </function>
          <function signature="static void event_loop(AppConfig *cfg)">
            <purpose>Run the top-level select()-based multiplexer until the process receives a termination signal.</purpose>
            <pre><![CDATA[`cfg->listen_fd` and `cfg->updi_fd` are valid open file descriptors; `cfg->gdb_fd` is -1.]]></pre>
            <logic>
              <step><![CDATA[Build an `fd_set` from `listen_fd` and, when `gdb_fd >= 0`, from `gdb_fd` and `updi_fd`.]]></step>
              <step><![CDATA[Call `select()` with no timeout; block until at least one descriptor becomes readable.]]></step>
              <step><![CDATA[If `listen_fd` is ready and `gdb_fd` is -1, call `rsp_accept()` to accept the incoming GDB client; store the returned fd in `cfg->gdb_fd` and call `elf_find_avros_tables()` / `fsm_build_thread_list()` to initialise the session.]]></step>
              <step><![CDATA[If `updi_fd` is ready, call `updi_console_poll()` and write any returned bytes to `stdout`.]]></step>
              <step><![CDATA[If `gdb_fd` is ready, call `rsp_recv_packet()` followed by `rsp_dispatch()`; on a zero-byte read (client disconnect) close `gdb_fd` and reset it to -1.]]></step>
              <step>Repeat until a SIGINT or SIGTERM is caught, then fall through to resource cleanup.</step>
            </logic>
          </function>
        </functions>
        <algorithm><![CDATA[**Signal handling:** `main()` registers a `SIGINT`/`SIGTERM` handler that sets a global `volatile sig_atomic_t g_quit` flag. The `event_loop()` tests this flag at the top of each iteration and returns cleanly, allowing `main()` to call `updi_close()`, `rsp_close()`, and `elf_close()` before `exit(0)`.

If the `--load` flag is set, `main()` invokes `updi_nvm_write_flash()` for each ELF `PT_LOAD` segment before entering `event_loop()`. A progress line is printed to `stdout` for each FLASH page written.

**Exit code table:**

| Exit code | Condition |
| --------- | --------- |
| `0` | Normal exit after SIGINT/SIGTERM or GDB `k` (kill) packet. |
| `1` | Fatal startup error: bad arguments, serial device open failure, or socket bind failure. |
| `1` | `--load` flash write failure (UPDI error or write-protection). |
| `0` | `--device` diagnostic mode completed; device info printed to stdout. |
| `1` | `--device` diagnostic mode failed at any UPDI step (BREAK/SYNCH, SIGROW read, ASI read). |

**Resource teardown sequence** (guaranteed order on all exit paths):

1.  `rsp_close(cfg.gdb_fd)` — close the active client socket if present.
2.  `rsp_close(cfg.listen_fd)` — close the listener socket.
3.  `elf_close(&ctx)` — free ELF heap buffers and close ELF file descriptor.
4.  `updi_close(cfg.updi_fd)` — drain and close the serial port.
5.  `return exit_code` from `main()`.

**Test approach for `src/main.c`:** Integration-level only. Verified by launching the compiled binary with valid and invalid argument combinations and checking exit codes and stderr output. Full session flow is tested by driving a GDB client script against a loopback serial device.]]></algorithm>
        <dependencies>
          <dep><![CDATA[`src/updi.c` — `updi_open()` / `updi_close()` / `updi_console_poll()`]]></dep>
          <dep><![CDATA[`src/gdb_rsp.c` — `rsp_listen()` / `rsp_accept()` / `rsp_recv_packet()` / `rsp_dispatch()` / `rsp_close()`]]></dep>
          <dep><![CDATA[`src/elf_parser.c` — `elf_open()` / `elf_find_avros_tables()` / `elf_close()`]]></dep>
          <dep><![CDATA[`src/fsm_mapper.c` — `fsm_build_thread_list()`]]></dep>
        </dependencies>
        <error_handling>
          <case name="Missing or invalid arguments"><![CDATA[Print usage string to `stderr` and `exit(1)`.]]></case>
          <case name="Serial device open failure"><![CDATA[Print `strerror(errno)` to `stderr` and `exit(1)`.]]></case>
          <case name="GDB socket bind failure"><![CDATA[Print `strerror(errno)` to `stderr` and `exit(1)`.]]></case>
          <case name="--load flash write failure"><![CDATA[Print the UPDI error message to `stderr` and `exit(1)`; the target is left in an indeterminate state.]]></case>
          <case name="--device combined with --load"><![CDATA[`parse_args()` detects the conflict, prints `"error: --device is mutually exclusive with --load"` to `stderr`, prints the usage line, and calls `exit(1)`.]]></case>
          <case name="--device UPDI link failure"><![CDATA[`run_device_mode()` emits a verbose `stderr` diagnostic naming the failed UPDI step (open, BREAK, SYNCH, SIGROW read, ASI read) and returns 1. No GDB listener is bound; no event loop is entered.]]></case>
        </error_handling>
      </module>

      <module path="src/updi.c">
        <purpose><![CDATA[implements the UPDI physical layer, managing the UART serial port and encoding/decoding UPDI protocol frames for memory access, execution control, NVM flash programming, and UPDI-based console bridging.]]></purpose>
        <responsibility>Open, configure (8E2, half-duplex), and close the UART serial device. On hosts where the underlying device cannot honour even parity (notably Linux PTY slaves used by the unit-test harness), fall back to 8N2 so the link still works in test environments.</responsibility>
        <responsibility><![CDATA[Encode and transmit UPDI commands (`STCS`, `LDCS`, `ST`, `LD`, `KEY`, `REPEAT`, `ST_PTR_WORD`, `ST_PTR_LONG`) over the UART.]]></responsibility>
        <responsibility>Receive UPDI response frames and validate ACK/NAK bytes.</responsibility>
        <responsibility>Provide non-intrusive background memory reads while the CPU is running.</responsibility>
        <responsibility>Implement NVM write sequences for FLASH page erase and program.</responsibility>
        <responsibility><![CDATA[Expose execution-control entry points (`updi_halt`, `updi_run`, `updi_step`) as -1 stubs; the full halt/run/step implementation is deferred to the Phase 3 OCD layer, which carries its own Microchip specification independent of the §35 UPDI document.]]></responsibility>
        <responsibility>Bridge the avrOS software UART console channel to host stdout for CLI and automated test use.</responsibility>
        <interfaces>
          <interface title="Public C API (src/updi.h)"><![CDATA[All functions exported from `src/updi.c` and declared in `src/updi.h`:

```c
/* Lifecycle */
int  updi_open(const char *device, int baud);
void updi_close(int fd);

/* Memory access */
int  updi_mem_read (int fd, uint32_t addr,       uint8_t *buf, size_t len);
int  updi_mem_write(int fd, uint32_t addr, const uint8_t *buf, size_t len);

/* Execution control */
int  updi_halt(int fd);
int  updi_run (int fd);
int  updi_step(int fd);

/* NVM programming */
int  updi_nvm_write_flash(int fd, uint32_t word_addr,
                          const uint8_t *data, size_t len);

/* Console bridge */
int  updi_console_poll(int fd, char *buf, size_t cap);

/* Device-signature diagnostics (Phase 7) */
typedef struct {
    uint8_t  device_id[3];     /* SIGROW DEVICEID0..2 @ 0x1100-0x1102 (DS §7.6.1)  */
    uint8_t  revid;            /* SYSCFG.REVID        @ 0x0F01        (DS §8.3.2.1) */
    uint8_t  serial[16];       /* SIGROW SERNUM0..15  @ 0x1110-0x111F (DS §7.6.2.3) */
    uint8_t  asi_sys_status;
    uint8_t  asi_key_status;
    uint8_t  asi_statusb;
    const char *fail_op;       /* NULL on success      */
    int         fail_errno;    /* negative updi error  */
} UpdiDeviceInfo;

int  updi_read_device_info(int fd, UpdiDeviceInfo *info);
```

Error return convention: all functions return 0 on success and -1 on error (errno not set); `updi_nvm_write_flash()` additionally returns `UPDI_ERR_WP` (-2) when FLASH write-protection is active.]]></interface>
          <interface title="Hardware Connection"><![CDATA[Half-duplex UART at the configured baud rate (default 115200). The AVR UPDI pin is connected to both TX and RX on the host adapter through a 1 kΩ isolation resistor. The UPDI protocol requires 8E2 framing (8 data bits, even parity, 2 stop bits) per AVR128DA datasheet §35.3.1.]]></interface>
        </interfaces>
        <data_structures><![CDATA[UPDI protocol constants used throughout the module:

| Constant | Value | Description |
| -------- | ----- | ----------- |
| `UPDI_SYNCH`       | `0x55` | UPDI synchronisation character sent after BREAK. |
| `UPDI_ACK`         | `0x40` | Acknowledgement byte returned by the target after each data byte. |
| `UPDI_MAX_BLOCK`   | `256`  | Maximum bytes transferred in a single REPEAT+LD/ST burst. |

UPDI ASI (Application System Interface) Control/Status register map (AVR128DA §35.4):

| Register | Offset | Description |
| -------- | ------ | ----------- |
| `ASI_STATUSB`     | `0x01` | Status B; reading clears the PESIG bit set on every BREAK. |
| `ASI_CTRLA`       | `0x02` | UPDI control register A; bit 2 = IBD (inter-byte delay enable). |
| `ASI_CTRLB`       | `0x03` | UPDI control register B. |
| `ASI_KEY_STATUS`  | `0x07` | NVM-key acceptance status. |
| `ASI_RESET_REQ`   | `0x08` | Write `0x59` to assert CPU reset; write `0x00` to release. |
| `ASI_SYS_CTRLA`   | `0x0A` | System control A. |
| `ASI_SYS_STATUS`  | `0x0B` | System status; bit 3 (`0x08`) = NVMPROG (NVM programming mode active). |

NVM controller registers (accessed via UPDI ST/LD at base address `0x1000`):

| Register | Offset | Description |
| -------- | ------ | ----------- |
| `NVMCTRL_CTRLA`  | `0x00` | NVM command register; write command code to initiate NVM operation. |
| `NVMCTRL_STATUS` | `0x02` | Status register; bit 0 = BUSY. |

AVR-Dx OCD (On-Chip Debug) register block — accessed via UPDI LDS/STS at UPDI base `0x0F80`. The CPU must be halted before reading or writing these registers; behaviour against a running CPU is undefined.

| Register | UPDI address | Width | Description |
| -------- | ------------ | ----- | ----------- |
| `OCD_BP0A`    | `0x0F80` | 3 B | Hardware breakpoint 0 address (LE byte address). |
| `OCD_BP1A`    | `0x0F84` | 3 B | Hardware breakpoint 1 address (LE byte address). |
| `OCD_CTRL0`   | `0x0F88` | 1 B | OCD control 0; `OCD_CTRL0_HWBP` (0x01) global HW-BP enable, `OCD_CTRL0_STEP` (0x04) arms single-step on next RUN. |
| `OCD_CTRL1`   | `0x0F89` | 1 B | OCD control 1; `OCD_CTRL1_BP0` (0x01) and `OCD_CTRL1_BP1` (0x02) enable individual comparators. |
| `OCD_STATUS0` | `0x0F8C` | 1 B | OCD halt status 0. |
| `OCD_STATUS1` | `0x0F8D` | 1 B | OCD halt status 1; `OCD_STATUS1_EXTBRK` (0x10) = external break / STOP / Ctrl-C halt cause. |
| `OCD_PC`      | `0x0F94` | 2 B | Program counter (word-aligned PC+1 storage; converted to/from GDB byte address by `updi_ocd_read_pc()` / `updi_ocd_write_pc()`). |
| `OCD_SP`      | `0x0F98` | 2 B | Stack pointer (little-endian SPL\|SPH). |
| `OCD_SREG`    | `0x0F9C` | 1 B | Status register. |
| `OCD_REGFILE` | `0x0FA0` | 32 B | General-purpose register file r0..r31. |

UPDI CS-space OCD registers (accessed via LDCS/STCS):

| CS register | Offset | Description |
| ----------- | ------ | ----------- |
| `ASI_OCD_CTRLA`  | `0x04` | OCD control: write `ASI_OCD_CTRLA_STOP` (0x01) to halt, `ASI_OCD_CTRLA_RUN` (0x02) to resume. |
| `ASI_OCD_STATUS` | `0x05` | OCD status; `ASI_OCD_STATUS_STOPPED` (0x01) = CPU halted. |]]></data_structures>
        <functions>
          <function signature="int updi_open(const char *device, int baud)"
                    summary="Open and configure the serial port for UPDI; return fd or -1 on error."/>
          <function signature="void updi_close(int fd)"
                    summary="Drain pending output and close the serial port."/>
          <function signature="int updi_mem_read(int fd, uint32_t addr, uint8_t *buf, size_t len)">
            <purpose>Read len bytes from target address addr via UPDI LD commands; supports both SRAM and FLASH address spaces.</purpose>
            <pre><![CDATA[`fd` is a valid UPDI serial file descriptor; `buf` points to a caller-allocated buffer of at least `len` bytes; the UPDI link has been initialised by `updi_open()`.]]></pre>
            <post>On success, `buf` contains the verbatim target memory contents at the requested address range.</post>
            <returns>0 on success; -1 on UART framing error, timeout, or UPDI NAK.</returns>
            <logic>
              <step><![CDATA[If `len` is greater than `UPDI_MAX_BLOCK`, split the read into consecutive transactions each transferring up to `UPDI_MAX_BLOCK` bytes.]]></step>
              <step><![CDATA[For each block, transmit three separate UPDI frames (datasheet §35.3.3.4): (a) the pointer-set frame — `ST_PTR_WORD` (`0x69`) followed by the 16-bit target address in little-endian order when `addr ≤ 0xFFFF`, or `ST_PTR_LONG` (`0x6A`) followed by the 24-bit address in little-endian order when `addr > 0xFFFF` (required for the AVR128DA/DB mapped-Flash region above 64 KiB) — then read the single ACK (`0x40`); (b) `REPEAT` (`0xA0`) followed by `(block_len - 1)`; (c) `LD ptr++` (`0x24`).]]></step>
              <step><![CDATA[Use `select()` with a 100 ms timeout before each `read()`; accumulate `block_len` data bytes into the destination buffer.]]></step>
              <step><![CDATA[Return -1 immediately on any `select()` timeout or framing error (unexpected byte in place of ACK).]]></step>
            </logic>
          </function>
          <function signature="int updi_mem_write(int fd, uint32_t addr, const uint8_t *buf, size_t len)"
                    summary="Write len bytes to target address addr via UPDI ST; return 0 or -1."/>
          <function signature="int updi_halt(int fd)">
            <purpose><![CDATA[Halt the AVR-Dx CPU by writing `ASI_OCD_CTRLA_STOP` (0x01) to `ASI_OCD_CTRLA` (UPDI CS `0x04`) via STCS, then polling `ASI_OCD_STATUS` via LDCS until `ASI_OCD_STATUS_STOPPED` (0x01) is observed.]]></purpose>
            <returns>0 on success; -1 on UPDI failure or if STOPPED is not asserted within the LDCS poll budget.</returns>
          </function>
          <function signature="int updi_run(int fd)"
                    summary="Resume the halted CPU by writing ASI_OCD_CTRLA_RUN (0x02) to ASI_OCD_CTRLA via STCS; returns 0 immediately without polling. Callers that need to wait for a halt use updi_ocd_poll_halted()."/>
          <function signature="int updi_step(int fd)"
                    summary="Single-step one instruction: set OCD_CTRL0_STEP in OCD_CTRL0 via STS, write ASI_OCD_CTRLA_RUN to ASI_OCD_CTRLA via STCS, then poll ASI_OCD_STATUS until STOPPED. Returns 0 or -1."/>
          <function signature="int updi_enter_debug(int fd)"
                    summary="Arm AVR-Dx OCD mode for the lifetime of the session: send the UPDI KEY opcode + 8-byte 'OCD     ' key, pulse ASI_RESET_REQ (0x59 then 0x00), and poll ASI_OCD_STATUS for STOPPED. Called once per session from main.c after updi_open() and any --load step."/>
          <function signature="int updi_ocd_poll_halted(int fd, int timeout_ms)"
                    summary="Poll ASI_OCD_STATUS at ~1 ms intervals for the STOPPED bit. Returns 0 if STOPPED, +1 on timeout (CPU still running), -1 on UPDI failure."/>
          <function signature="int updi_ocd_read_halt_status(int fd, uint8_t *status0, uint8_t *status1)"
                    summary="Read OCD_STATUS0 (0x0F8C) and OCD_STATUS1 (0x0F8D) via UPDI LDS into the caller's bytes. OCD_STATUS1.EXTBRK (0x10) distinguishes external-break / Ctrl-C halts from hardware-breakpoint, single-step, BREAK-opcode, change-of-flow, and interrupt halts."/>
          <function signature="int updi_ocd_read_gpr(int fd, uint8_t n, uint8_t *val)"
                    summary="Read AVR GPR r&lt;n&gt; (0..31) via UPDI LDS at OCD_REGFILE + n (0x0FA0 + n). CPU must be halted."/>
          <function signature="int updi_ocd_write_gpr(int fd, uint8_t n, uint8_t val)"
                    summary="Write AVR GPR r&lt;n&gt; via UPDI STS at OCD_REGFILE + n. CPU must be halted."/>
          <function signature="int updi_ocd_read_sreg(int fd, uint8_t *val)"
                    summary="Read AVR SREG via UPDI LDS at OCD_SREG (0x0F9C)."/>
          <function signature="int updi_ocd_write_sreg(int fd, uint8_t val)"
                    summary="Write AVR SREG via UPDI STS at OCD_SREG."/>
          <function signature="int updi_ocd_read_sp(int fd, uint16_t *val)"
                    summary="Read AVR SP (little-endian SPL|SPH) via UPDI LDS at OCD_SP (0x0F98)."/>
          <function signature="int updi_ocd_write_sp(int fd, uint16_t val)"
                    summary="Write AVR SP via UPDI STS at OCD_SP."/>
          <function signature="int updi_ocd_read_pc(int fd, uint32_t *byte_addr)"
                    summary="Read OCD_PC (0x0F94) via UPDI LDS and convert the silicon's word-aligned PC+1 storage to a GDB byte address (shift left 1, undo the AVR +1 post-increment)."/>
          <function signature="int updi_ocd_write_pc(int fd, uint32_t byte_addr)"
                    summary="Convert a GDB byte address back to the word-aligned PC+1 form and write OCD_PC via UPDI STS."/>
          <function signature="int updi_ocd_set_hw_bp(int fd, uint8_t idx, uint32_t byte_addr)"
                    summary="Program hardware-breakpoint comparator idx (0 or 1) to a GDB byte address: write the 3-byte LE address to OCD_BP0A/OCD_BP1A via UPDI STS, set the matching OCD_CTRL1_BP0/OCD_CTRL1_BP1 enable bit, and set the global OCD_CTRL0_HWBP enable bit."/>
          <function signature="int updi_ocd_clear_hw_bp(int fd, uint8_t idx)"
                    summary="Disable hardware-breakpoint comparator idx by clearing the matching OCD_CTRL1_BP0/OCD_CTRL1_BP1 bit via UPDI LDS/STS read-modify-write on OCD_CTRL1."/>
          <function signature="int updi_nvm_write_eeprom(int fd, uint32_t addr, const uint8_t *data, size_t len)"
                    summary="Program EEPROM (UPDI window 0x814000–0x8143FF) byte-by-byte using NVMCTRL command EEERWR (0x13), polling NVMSTATUS.EEBUSY (bit 1) between bytes. Window-checks addr/len; returns -1 on out-of-window or BUSY timeout."/>
          <function signature="int updi_nvm_write_userrow(int fd, uint32_t addr, const uint8_t *data, size_t len)"
                    summary="Program USERROW (UPDI window 0x810080–0x8100FF, 128 B) using the same EEERWR + EEBUSY-poll sequence as updi_nvm_write_eeprom."/>
          <function signature="int updi_nvm_write_fuses(int fd, uint32_t addr, const uint8_t *data, size_t len)"
                    summary="Program FUSES (UPDI window 0x820000–0x82001F, 32 B) using the same EEERWR + EEBUSY-poll sequence as updi_nvm_write_eeprom."/>
          <function signature="int updi_nvm_write_lockbits(int fd, uint32_t addr, const uint8_t *data, size_t len, bool allow_updi_disable)"
                    summary="Program LOCK (UPDI window 0x820040–0x820043, 4 B). Returns UPDI_ERR_LOCKED (-3) if ASI_SYS_STATUS.LOCKSTATUS is asserted, or if !allow_updi_disable and the 4-byte little-endian payload is not the unlock pattern UPDI_LOCK_UNLOCKED (0x5CC5C55C). Otherwise programs via EEERWR + EEBUSY poll."/>
          <function signature="int updi_nvm_write_flash(int fd, uint32_t word_addr, const uint8_t *data, size_t len)">
            <purpose>Erase and program one or more FLASH pages starting at word_addr using the UPDI NVM controller write-page sequence.</purpose>
            <pre><![CDATA[Target CPU is halted (`updi_halt()` has been called); `word_addr` is aligned to a FLASH page boundary; `len` is a non-zero multiple of the target FLASH page size (512 bytes for AVR DA/DB).]]></pre>
            <returns><![CDATA[0 on success; -1 on NVM timeout or UPDI communication error; `UPDI_ERR_WP` (a distinct negative constant) if FLASH write protection is detected in `NVMCTRL_STATUS`.]]></returns>
            <logic>
              <step><![CDATA[Transmit the UPDI `KEY` opcode (`0xE0`) followed by the 8-byte NVM key string `"NVMProg "`.]]></step>
              <step><![CDATA[`STCS ASI_RESET_REQ = 0x59` to assert reset, then `STCS ASI_RESET_REQ = 0x00` to release reset (per AVR128DA §35.3.7.2).]]></step>
              <step><![CDATA[Poll `ASI_SYS_STATUS` via LDCS until bit 3 (NVMPROG, mask `0x08`) is set; timeout after 100 polling iterations (1 ms each).]]></step>
              <step><![CDATA[For each FLASH page: write the page-sized data block via `updi_mem_write()` (which itself uses the 3-frame `ST_PTR_WORD` + `REPEAT` + `ST ptr++` burst); then issue the NVMCTRL ERWP (Erase + Write Page) command by writing `0x03` to `NVMCTRL_CTRLA` (`0x1000`).]]></step>
              <step><![CDATA[Poll `NVMCTRL_STATUS` (`0x1002`) bit 0 (BUSY) via `updi_mem_read()` until clear; timeout after 20 iterations per page.]]></step>
              <step><![CDATA[Check `NVMCTRL_STATUS` bit 2 (WRERR); if set, return `UPDI_ERR_WP`.]]></step>
              <step><![CDATA[After all pages are written, exit NVM mode with `STCS ASI_RESET_REQ = 0x59` followed by `STCS ASI_RESET_REQ = 0x00`.]]></step>
            </logic>
          </function>
          <function signature="int updi_console_poll(int fd, char *buf, size_t cap)"
                    summary="Poll the UPDI console channel and copy any pending bytes to buf; return byte count or -1."/>
          <function signature="int updi_read_device_info(int fd, UpdiDeviceInfo *info)">
            <purpose><![CDATA[Read the AVR-Dx SIGROW.DEVICEID0..2 at `0x1100`–`0x1102` (datasheet §7.6.1, Table 7-4), the SYSCFG.REVID byte at `0x0F01` (datasheet §8.3.2.1; SYSCFG base `0x0F00`), the 16-byte SIGROW.SERNUM0..15 at `0x1110`–`0x111F` (datasheet §7.6.2.3), and the UPDI ASI status registers (`ASI_SYS_STATUS`, `ASI_KEY_STATUS`, `ASI_STATUSB`), populating the caller-supplied `UpdiDeviceInfo` struct. The operation is non-destructive — the CPU is **not** halted and no NVM activity is initiated.]]></purpose>
            <pre><![CDATA[`fd` is a valid UPDI serial file descriptor; `info` points to a caller-allocated `UpdiDeviceInfo` struct; the UPDI link has been initialised by `updi_open()`.]]></pre>
            <post><![CDATA[On success, every field of `*info` carries verbatim bytes from the target; on failure, `info->fail_op` carries a short identifier of the failed UPDI step (one of `"break"`, `"synch"`, `"asi-statusb"`, `"sigrow"`, `"revid"`, `"sernum"`, `"sys-status"`, `"key-status"`) and `info->fail_errno` carries either the negative `updi_mem_read` return or 0.]]></post>
            <returns>0 on success; -1 on any UPDI read failure.</returns>
            <logic>
              <step><![CDATA[Read three bytes at `0x1100` via `updi_mem_read()` into `info->device_id[0..2]`; on failure record `"sigrow"` and return -1.]]></step>
              <step><![CDATA[Read one byte at `0x0F01` (SYSCFG.REVID, datasheet §8.3.2.1) into `info->revid`; on failure record `"revid"` and return -1.]]></step>
              <step><![CDATA[Read sixteen bytes at `0x1110` (SIGROW.SERNUM0..15, datasheet §7.6.2.3) into `info->serial[0..15]`; on failure record `"sernum"` and return -1.]]></step>
              <step><![CDATA[Read `ASI_SYS_STATUS`, `ASI_KEY_STATUS`, and `ASI_STATUSB` via three `LDCS` opcodes; record `"sys-status"` / `"key-status"` / `"asi-statusb"` on failure.]]></step>
              <step><![CDATA[On success, clear `info->fail_op` to `NULL` and return 0.]]></step>
            </logic>
          </function>
        </functions>
        <algorithm><![CDATA[**UPDI link initialisation sequence (performed inside `updi_open()`):**

1.  Open the serial port with `O_RDWR | O_NOCTTY | O_NONBLOCK`, then restore blocking mode via `fcntl()`.
2.  Configure `termios` for raw 8E2 mode: `cfmakeraw()`, set `CS8`, `CSTOPB`, `PARENB` (with `PARODD` cleared = even parity), disable flow control, and apply `cfsetispeed()` / `cfsetospeed()` for the operating baud rate.
3.  If the kernel rejects `PARENB` with `EINVAL` (Linux PTY slaves silently strip parity from virtual terminals), retry the `tcsetattr()` call with `PARENB` cleared. Real serial hardware accepts 8E2; the fallback only affects PTY-backed unit tests.
4.  Run up to three cold-start attempts that mirror avrdude's `serialupdi` programmer (verified bit-for-bit by `strace`-ing avrdude against the same target):
    - **Attempt 0 — fast path** (succeeds against an already-prepped target, e.g. immediately after a previous successful session): write one `0x00` wake byte at session baud, then `tcdrain()` + `tcflush(TCIFLUSH)`.
    - **Attempts 1, 2 — slow path** (required on cold power-on with passive single-wire combiners such as the RPi PL011): switch the kernel baud to 300 with `CSTOPB` cleared (1 stop bit) via `tcsetattr(TCSADRAIN)`, write two `0x00` bytes — each immediately followed by `tcdrain()` (mandatory: without it the queued byte is flushed or re-clocked by the subsequent baud switch, destroying the BREAK pulse) — and read-and-discard their half-duplex echoes at 300 baud (each byte holds TX low ≈ 33 ms = ≥ 60 ms total). Wait ≈ 50 ms (`nanosleep`) so any line-bus echoes still in flight reach the kernel RX queue at 300-baud framing, then `tcflush(TCIFLUSH)` to discard them, then restore session baud (`tcsetattr(TCSADRAIN)` + a second `tcflush(TCIFLUSH)`).
    - For every attempt, follow the wake / BREAK with three frames: `STCS ASI_CTRLB = ASI_CTRLB_CCDETDIS (0x08)` (disable contention detection), `STCS ASI_CTRLA = ASI_CTRLA_IBDLY (0x80)` (enable inter-byte delay on responses), and `LDCS ASI_STATUSA` (link probe — `ASI_STATUSA` carries UPDIREV and is always readable while UPDI is enabled). If all three succeed, read the 32-byte System Information Block (`SYNCH` + `UPDI_OP_KEY_SIB` `0xE6`, followed by `UPDI_SIB_LEN = 32` response bytes) to wake any target left in UPDI SLEEP from a prior session and to confirm the link end-to-end; on success the function returns the open fd. Otherwise it advances to the next attempt.
5.  After three failed attempts the function returns -1.

**Why the 300-baud BREAK trick is necessary:** No portable POSIX API generates a multi-millisecond line-low BREAK on demand. `tcsendbreak()` is too short (and on the RPi PL011 driver is silently ignored). Switching the kernel baud rate is the only mechanism that reliably stretches a single `0x00` byte to ≥ 30 ms of line-low time, which is what cold-power AVR-Dx UPDI requires to clear its contention-detect latch.

**Why the SIB-read SLEEP wake is necessary:** A target left in UPDI SLEEP from a prior debug session will accept every link-layer probe (STCS, LDCS) — those frames merely echo on the half-duplex line — yet reject every memory access, manifesting as `updi_mem_read()` returning -1 immediately after a successful-looking `updi_open()`. avrdude reads the 32-byte SIB unconditionally on every connect; the act of issuing a key-opcode-class frame (`0xE6`) is what wakes the target. The 32-byte payload itself (e.g. `"    AVR P:2D:1-3M2 (A7.KV001.0)\0"`) is discarded.

**Half-duplex echo cancellation:** Because TX and RX share the same physical wire, every transmitted byte is echoed back on the RX line. All UPDI transmit helpers skip one echo byte per transmitted byte before reading response data.

**UPDI timing budget:**

| Operation | Timeout | Notes |
| --------- | ------- | ----- |
| `updi_mem_read()` per-byte | 100 ms (`select()`) | A `select()` with `timeval { 0, 100000 }` guards every `read()`. |
| `updi_nvm_write_flash()` NVMPROG wait | ≈ 100 ms | 100 LDCS polls of `ASI_SYS_STATUS` at 1 ms intervals. |
| `updi_nvm_write_flash()` page BUSY poll | ≈ 20 ms per page | 20 reads of `NVMCTRL_STATUS` (bit 0 = BUSY). |
| Cold-start slow-path BREAK | ≥ ≈ 60 ms per attempt | Two `0x00` bytes at 300 baud, mirroring avrdude's serialupdi. |

Note: `updi_halt()`, `updi_run()`, `updi_step()`, and `updi_ocd_poll_halted()` use a bounded LDCS poll on `ASI_OCD_STATUS` (typical halt latency ≤ 1 ms after `STCS ASI_OCD_CTRLA = STOP`); `updi_ocd_poll_halted()` exposes the deadline as a millisecond argument so the RSP `c` handler can multiplex OCD polling with a `select()` on the GDB client socket.

**UPDI command opcode encoding:** Each command frame begins with a single command byte followed by its operands. Key opcodes (AVR128DA §35.3.3):

| Opcode mnemonic | Byte value | Purpose |
| --------------- | ---------- | ------- |
| `LDCS rd, cs`   | `0x80\|cs` | Load Control/Status register `cs`. |
| `STCS cs, rr`   | `0xC0\|cs` | Store to Control/Status register `cs`. |
| `ST_PTR_WORD`   | `0x69`     | Set the burst pointer with a 16-bit address operand (precedes REPEAT/LD/ST bursts for addresses ≤ 0xFFFF). |
| `ST_PTR_LONG`   | `0x6A`     | Set the burst pointer with a 24-bit address operand (used for addresses > 0xFFFF, e.g. AVR128DA/DB mapped Flash). |
| `ST ptr++, rr`  | `0x64`     | Store with pointer post-increment (burst write). |
| `LD rd, ptr++`  | `0x24`     | Load with pointer post-increment (burst read). |
| `REPEAT`        | `0xA0`     | Set repeat count for the next bulk transfer (n-1 in operand). |
| `KEY`           | `0xE0`     | Transmit 8-byte key to unlock a privileged mode. |

**Test approach for `src/updi.c`:** Unit-testable using a POSIX pseudo-terminal pair (`openpty()`): one end is passed to `updi_open()`, the other is driven by the test harness. Test cases cover: termios 8E2 configuration (with PARENB-fallback under PTYs), the cold-start wake-byte/STCS-CCDETDIS/STCS-IBDLY/LDCS-STATUSA probe and its 3-retry policy, the 3-frame `ST_PTR_WORD`/`REPEAT`/`LD-or-ST` burst, the 100 ms `select()` deadline, NVM precondition checks, NVMPROG and per-page BUSY timeouts, and the OCD control primitives (`updi_enter_debug`, `updi_halt`, `updi_run`, `updi_step`, `updi_ocd_poll_halted`, `updi_ocd_read_halt_status`, and the OCD register-file readers/writers).]]></algorithm>
        <dependencies>
          <dep><![CDATA[POSIX `termios` serial API (`tcgetattr`, `tcsetattr`, `cfsetspeed`, `cfmakeraw`).]]></dep>
          <dep><![CDATA[Standard POSIX file I/O (`open`, `read`, `write`, `close`, `fcntl`, `tcdrain`).]]></dep>
        </dependencies>
        <error_handling>
          <case name="UART framing error or UPDI NAK">Return -1 to the caller; the caller decides whether to retry or abort the session.</case>
          <case name="NVM write protection"><![CDATA[Return `UPDI_ERR_WP` so the caller can report `"Flash write-protected"` to the GDB console.]]></case>
          <case name="UPDI link lost mid-session"><![CDATA[Any `read()` returning 0 or any unexpected byte sequence causes the current operation to return -1; the RSP layer then sends a GDB error packet and awaits the next client command.]]></case>
        </error_handling>
      </module>

      <module path="src/gdb_rsp.c">
        <purpose><![CDATA[implements the GDB Remote Serial Protocol server, accepting a single GDB client connection on a TCP port, decoding RSP packets, dispatching them to command handlers, and encoding response packets.]]></purpose>
        <responsibility>Bind and accept a single GDB client connection on the configured TCP port.</responsibility>
        <responsibility><![CDATA[Decode incoming RSP packets (`$<data>#<checksum>`), verify checksums, and send ACK (`+`) or NAK (`-`).]]></responsibility>
        <responsibility>Dispatch decoded packets to command handlers for register access, memory access, breakpoints, step/continue, and monitor commands.</responsibility>
        <responsibility>Encode and transmit RSP response packets.</responsibility>
        <responsibility><![CDATA[Handle lifecycle packets: `qSupported`, `qAttached`, `vAttach`, `D` (detach), and `k` (kill).]]></responsibility>
        <interfaces>
          <interface title="Public C API (src/gdb_rsp.h)"><![CDATA[All functions exported from `src/gdb_rsp.c` and declared in `src/gdb_rsp.h`:

```c
/* Lifecycle */
int  rsp_listen(uint16_t port);      /* create + bind + listen; return fd or -1 */
int  rsp_accept(int listen_fd);      /* block until client; return client fd */
void rsp_close (int fd);             /* close client or listen fd */

/* Packet codec */
int  rsp_recv_packet(int fd, char *buf, size_t cap);
    /* Returns: payload byte count; 0 on disconnect; -1 on error */
int  rsp_send_packet(int fd, const char *payload);
    /* Returns: 0 on success; -1 on write error */

/* No-ack mode (toggled by QStartNoAckMode) */
void rsp_set_noack(bool enabled);
bool rsp_get_noack(void);

/* Dispatch */
int  rsp_dispatch(int fd, const char *packet, RspHandlers *h);
    /* Returns: 0 on success; -1 on write error from a handler */

/* Default handlers (used by main; tests may override individual slots) */
void rsp_default_handlers(RspHandlers *h, RspContext *ctx);
```

The `RspHandlers` and `RspContext` structs are declared in `src/gdb_rsp.h`. The caller normally invokes `rsp_default_handlers()` to populate `RspHandlers` with the production handler set before passing it to `rsp_dispatch()`. `RspContext` carries the shared session state (UPDI fd, FSM context, symbol index, selected thread IDs, GDB client fd pointer, and quit flag pointer) and is stored in `RspHandlers.ctx`. Socket options applied by `rsp_listen()`: `SO_REUSEADDR` (allow rapid server restart) and `TCP_NODELAY` (disable Nagle algorithm to minimise GDB round-trip latency).]]></interface>
          <interface title="GDB Network Interface"><![CDATA[TCP server socket on the configured port (default `1234`). Accepts exactly one client connection at a time; a new connection is accepted after the previous client detaches.]]></interface>
        </interfaces>
        <data_structures><![CDATA[**`RspHandlers`** — Function pointer table dispatched by `rsp_dispatch()`. Each handler receives the raw packet payload string, the client socket fd, and a pointer to the shared application context.

| Field | Packet(s) handled | Description |
| ----- | ----------------- | ----------- |
| `on_halt_reason`  | `?`                       | Stop-reason query; calls `signal_for_halt_status()` (reads `OCD_STATUS1` via `updi_ocd_read_halt_status()`) and replies `T02thread:<id>;` (SIGINT) when `OCD_STATUS1.EXTBRK` is set or `T05thread:<id>;` (SIGTRAP) otherwise. |
| `on_read_regs`    | `g`                       | Read all 35 GDB AVR registers (R0-R31, SREG, SP, PC; 78 hex chars) live from the OCD register file via `updi_ocd_read_gpr()` / `read_sreg()` / `read_sp()` / `read_pc()` when the selected GDB thread is the active FSM thread (or when no FSM thread is established); delegate to `fsm_get_registers()` for non-active virtual threads. |
| `on_write_regs`   | `G`, `P`                  | Write all registers (`G`) or a single register (`P n=vv`); both route per-slot to `updi_ocd_write_gpr/sreg/sp/pc` (no `updi_mem_write()`). |
| `on_read_mem`     | `m addr,len`              | Read memory; flips GDB unified-address bit 23 to produce the UPDI physical address before calling `updi_mem_read()`. |
| `on_write_mem`    | `M addr,len:data`, `X addr,len:bin` | Write memory: `updi_nvm_write_flash()` for program-space addresses (GDB bit 23 clear), `updi_mem_write()` for data-space addresses (GDB bit 23 set). |
| `on_continue`     | `c`, `vCont;c`            | Resume target via `updi_run()`, invalidate the FSM cache, multiplex `select(client_fd)` against `updi_ocd_poll_halted(1 ms)` to deliver Ctrl-C interrupts during the run, then halt and reply with `T02thread:<id>;` (Ctrl-C) or the signal from `signal_for_halt_status()`. Bounds UPDI poll failures with `UPDI_FAIL_MAX = 8`. |
| `on_step`         | `s`, `vCont;s`            | Single-step via `updi_step()`, rebuild the FSM thread list, reply with the stop-reason packet produced by `on_halt_reason`. |
| `on_insert_bp`    | `Z0`, `Z1`                | Install a hardware-breakpoint comparator in one of two `RspContext.hw_bp_addr[]` slots via `updi_ocd_set_hw_bp()`; both `Z0` and `Z1` route to the same two comparators. |
| `on_remove_bp`    | `z0`, `z1`                | Release a hardware-breakpoint comparator via `updi_ocd_clear_hw_bp()` and reset the shadow slot. |
| `on_thread_info`  | `qfThreadInfo`/`qsThreadInfo` | Enumerate virtual thread IDs from `FsmContext`. |
| `on_thread_extra` | `qThreadExtraInfo`        | Return FSM name string as hex-encoded ASCII. |
| `on_set_thread_g` | `Hg<tid>`                 | Select virtual thread for subsequent `g`/`G`/`P` operations; thread IDs -1 and 0 both map to the active FSM thread. |
| `on_set_thread_c` | `Hc<tid>`                 | Select virtual thread for subsequent `c`/`s` operations. |
| `on_monitor`      | `qRcmd`                   | Forward the raw ASCII-hex command body to `monitor_dispatch()`. |
| `on_detach`       | `D`, `k`                  | Detach (`D`: call `hw_bp_clear_all(ctx)` to release both OCD comparators, then `updi_run()` and close the client fd) or kill (`k`: set the global quit flag). |

**Breakpoint shadow:** The two AVR-Dx OCD hardware-breakpoint comparators (`BP0`, `BP1`) are tracked in the session context as `RspContext.hw_bp_addr[0..1]`, each storing the GDB byte address currently programmed or the sentinel `HW_BP_SLOT_EMPTY` (`0xFFFFFFFF`). Both `Z0` (software) and `Z1` (hardware) GDB packets route to the same two comparators — patching the AVR `BREAK` opcode into FLASH at runtime would require exiting OCD mode, entering NVMPROG (which resets the CPU and destroys live register/SREG/SP state), patching, and re-entering OCD, a sequence whose state-preservation cost outweighs the benefit. `on_insert_bp` programs silicon via `updi_ocd_set_hw_bp()`; `on_remove_bp` releases via `updi_ocd_clear_hw_bp()`; `on_detach` calls the `hw_bp_clear_all()` helper so silicon is left clean for the next session.

**Async Ctrl-C interrupt:** While `on_continue` is polling the target for a halt, it uses `select()` on the GDB client fd with a 5 ms timeout to detect the GDB Ctrl-C async-interrupt byte (`0x03`). On detection it calls `updi_halt()` and replies `T02thread:<id>;` (SIGINT). The poll loop also bounds consecutive `updi_ocd_poll_halted()` failures with `UPDI_FAIL_MAX = 8` and replies `E01` if the link degrades to that point, rather than spinning indefinitely.

**Packet buffer:** `RSP_PACKET_MAX` (2048 bytes) is the compile-time maximum for a single RSP payload, sized to accommodate a full AVR `g`-packet response (35 registers × 2 hex chars/byte = 70 bytes) plus worst-case `qXfer` memory-map XML overhead.]]></data_structures>
        <functions>
          <function signature="int rsp_listen(uint16_t port)"
                    summary="Create, bind, and listen on the GDB TCP socket; return the listen fd or -1."/>
          <function signature="int rsp_accept(int listen_fd)"
                    summary="Block until a GDB client connects; return the client fd."/>
          <function signature="int rsp_recv_packet(int fd, char *buf, size_t cap)">
            <purpose>Read one complete RSP packet from the client socket into buf, verify its checksum, and send ACK or NAK.</purpose>
            <pre><![CDATA[`fd` is a connected GDB client socket; `buf` points to a caller-allocated buffer of at least `cap` bytes; `cap` is at least `RSP_PACKET_MAX`.]]></pre>
            <post>On success, `buf` holds the null-terminated payload string (between `$` and `#`); the checksum bytes have been consumed from the socket.</post>
            <returns>Number of payload bytes written to buf on success; 0 on client disconnect; -1 on checksum mismatch (NAK sent) or socket error.</returns>
            <logic>
              <step><![CDATA[Read bytes one at a time until `$` is received; discard any `+`/`-` ACK/NAK bytes that precede it.]]></step>
              <step><![CDATA[Accumulate payload bytes into `buf`, computing a running XOR checksum, until `#` is received.]]></step>
              <step>Read the two ASCII hex checksum bytes that follow `#` and parse them to a single byte value.</step>
              <step><![CDATA[Compare computed checksum to received checksum. On match, write `+` to `fd` and return payload length. On mismatch, write `-` to `fd` and return -1.]]></step>
            </logic>
          </function>
          <function signature="int rsp_send_packet(int fd, const char *payload)"
                    summary="Frame payload as an RSP packet and transmit; return 0 or -1."/>
          <function signature="int rsp_dispatch(int fd, const char *packet, RspHandlers *h)">
            <purpose>Identify the RSP packet type from the first character (and optionally subsequent characters) of packet and invoke the matching handler in h. Returns 0 on success or -1 if the selected handler reports a write error.</purpose>
            <logic>
              <step><![CDATA[Match the leading characters of `packet` against the handler table using a switch on `packet[0]` with secondary string comparisons for multi-character commands (`qS`, `qA`, `qf`, `qs`, `qR`, `QS`, `vC`, `Hg`, `Hc`, `Z0`, `z0`).]]></step>
              <step>Invoke the matching handler function pointer from `h`, passing `fd`, `packet`, and the shared application context pointer.</step>
              <step><![CDATA[If no handler matches, call `rsp_send_packet(fd, "")` to send the mandatory empty response `$#00`.]]></step>
            </logic>
            <notes><![CDATA[The following packets are handled inline (not via the `RspHandlers` table) because their responses are fixed strings that require no target interaction: `qSupported` (feature string), `qAttached` (returns `1`), `QStartNoAckMode` (calls `rsp_set_noack(true)` and returns `OK`), and `vCont?` (returns the supported vCont actions string `vCont;c;s`).]]></notes>
          </function>
          <function signature="void rsp_set_noack(bool enabled)"
                    summary="Toggle no-ack mode after a successful QStartNoAckMode negotiation; suppresses both the sending and the expectation of +/- ack bytes."/>
          <function signature="bool rsp_get_noack(void)"
                    summary="Returns the current no-ack mode flag; primarily for tests and diagnostics."/>
          <function signature="void rsp_default_handlers(RspHandlers *h, RspContext *ctx)"
                    summary="Populate every slot in *h with the production handler implementations and bind the shared session state pointer ctx to h->ctx."/>
        </functions>
        <algorithm><![CDATA[**RSP packet codec:**

*Reception* (`rsp_recv_packet`): reads bytes until `$`, then accumulates payload until `#`, then reads two hex checksum digits. Computes XOR of all payload bytes. Sends `+` on match, `-` on mismatch. After `QStartNoAckMode` is negotiated, the server stops sending `+`/`-` as the client will not retransmit.

*Transmission* (`rsp_send_packet`): computes XOR checksum over the payload string, formats the frame as `$<payload>#<XX>` and writes it in a single `write()` call to minimise TCP segment fragmentation.

**Capability negotiation:** On the `qSupported` packet the server replies with the feature string `"PacketSize=800;QStartNoAckMode+;multiprocess-;vContSupported+"`. The server enables no-ack mode immediately upon receiving `QStartNoAckMode+`.

**Thread context:** GDB selects a thread with `Hg<tid>` and `Hc<tid>`. The server stores the selected thread IDs in `RspHandlers.g_thread` and `RspHandlers.c_thread`. Register reads use `g_thread`; continue/step use `c_thread`.

**Static memory layout of `src/gdb_rsp.c`:**

| Variable | Type | Size | Purpose |
| -------- | ---- | ---- | ------- |
| `pkt_buf[RSP_PACKET_MAX]` | `char[2048]` | 2 KiB | Receive buffer for one RSP packet payload. |
| `rsp_buf[RSP_PACKET_MAX+8]` | `char[2056]` | 2 KiB | Transmit buffer: framed `$payload#XX`. |
| `g_thread`, `c_thread` | `int` | 8 B | Selected GDB thread IDs for register and continue operations. |

(The previous 96-byte software-breakpoint table has been removed; the two hardware-breakpoint shadow slots now live in `RspContext.hw_bp_addr[]` and are session-scoped.)

Total static BSS in `src/gdb_rsp.c`: approximately 4.2 KiB.

**TCP socket options applied by `rsp_listen()`:**

*   `SO_REUSEADDR` — allows rapid server restart without waiting for TIME_WAIT to expire.
*   `TCP_NODELAY` — disables Nagle's algorithm; each `write()` is sent immediately, which is critical for GDB round-trip latency (GDB sends many small packets and waits for each response before continuing).

**Test approach for `src/gdb_rsp.c`:** Unit-testable using a pair of connected loopback sockets (`socketpair()`) with all `updi_*` and `fsm_*` symbols replaced by `ld --wrap` stubs that record call sequences and return canned values. Test cases cover: packet reception with correct and incorrect checksums; ACK/NAK behaviour before and after `QStartNoAckMode`; `rsp_dispatch()` routing to each handler; empty-response for unknown packets; `G`/`P` writes routed per slot to `updi_ocd_write_gpr/sreg/sp/pc`; `g` reads served from the OCD register file for the active thread; `Z0`/`Z1` insert programming an OCD comparator via `updi_ocd_set_hw_bp()`; `z0`/`z1` releasing via `updi_ocd_clear_hw_bp()`; a third unique-address insert returning `E08`; Ctrl-C (`\x03`) on the client socket halting the target during `c` and the reply carrying signal `T02`; `D` (detach) calling `hw_bp_clear_all()` before `updi_run()` so silicon comparators are released; and the `UPDI_FAIL_MAX = 8` retry bound preventing `c` from spinning on a degraded UPDI link.]]></algorithm>
        <dependencies>
          <dep><![CDATA[`src/updi.c` — execution control and memory access.]]></dep>
          <dep><![CDATA[`src/fsm_mapper.c` — virtual thread enumeration and per-thread register state.]]></dep>
          <dep><![CDATA[`src/monitor.c` — custom `monitor avros` commands.]]></dep>
          <dep>POSIX socket API.</dep>
        </dependencies>
        <error_handling>
          <case name="Checksum mismatch"><![CDATA[Send `-` (NAK) and await retransmit from the GDB client.]]></case>
          <case name="Unrecognized packet type"><![CDATA[Send the empty response `$#00` as required by the RSP specification.]]></case>
          <case name="Client disconnect">Close the client fd, reopen the listener, and wait for the next GDB connection.</case>
          <case name="Breakpoint comparators exhausted"><![CDATA[Return GDB error packet `E08`; no comparator is programmed. The AVR-Dx provides only two HW comparators (`BP0`/`BP1`), so GDB receives `E08` on the third unique-address `Z0`/`Z1` insert.]]></case>
        </error_handling>
      </module>

      <module path="src/elf_parser.c">
        <purpose><![CDATA[parses the AVR ELF binary to extract the FLASH addresses and sizes of the avrOS system tables and to produce the `AvrOsSymbolIndex` consumed by the FSM mapper.]]></purpose>
        <responsibility>Open and validate the ELF file header and target architecture.</responsibility>
        <responsibility>Locate the symbol table section and iterate over symbol entries.</responsibility>
        <responsibility>Apply the Harvard architecture offset to convert ELF virtual addresses to physical FLASH word addresses.</responsibility>
        <responsibility><![CDATA[Populate an `AvrOsSymbolIndex` with the FLASH address, SRAM status address, and entry count for each detected avrOS table.]]></responsibility>
        <responsibility>Provide ELF program-header metadata to the RSP layer for memory-map query responses.</responsibility>
        <interfaces>
          <interface title="Public C API (src/elf_parser.h)"><![CDATA[All functions exported from `src/elf_parser.c` and declared in `src/elf_parser.h`:

```c
/* Lifecycle */
int  elf_open (const char *path, ElfContext *ctx);
    /* Validates magic/arch, loads .symtab and .strtab into ctx. */
    /* Returns: 0 on success; -1 on I/O error, bad magic, or malloc failure. */
void elf_close(ElfContext *ctx);
    /* Frees ctx->symtab and ctx->strtab; closes ctx->fd. */

/* Symbol resolution */
int      elf_find_avros_tables(ElfContext *ctx, AvrOsSymbolIndex *idx);
    /* Returns: 0 (partial or full success); -1 on internal ELF read error. */
uint32_t elf_flash_addr(const ElfContext *ctx, uint32_t vma);
    /* Applies: physical_word_addr = (vma - ctx->flash_base) / 2 */
```

The `ElfContext` and `AvrOsSymbolIndex` struct definitions are also declared in `src/elf_parser.h`.  Callers must zero-initialise `AvrOsSymbolIndex` before passing it to `elf_find_avros_tables()`.]]></interface>
        </interfaces>
        <data_structures><![CDATA[**`ElfContext`** — All ELF parsing state maintained between `elf_open()` and `elf_close()`.

| Field | Type | Description |
| ----- | ---- | ----------- |
| `fd`          | `int`         | Open file descriptor for the ELF binary (kept open for lazy section reads). |
| `ehdr`        | `Elf32_Ehdr`  | Cached ELF32 file header (magic, machine, entry point, section count). |
| `symtab`      | `Elf32_Sym *` | Heap-allocated copy of the `.symtab` section contents. |
| `sym_count`   | `size_t`      | Number of `Elf32_Sym` entries in `symtab`. |
| `strtab`      | `char *`      | Heap-allocated copy of the `.strtab` string table used for symbol names. |
| `strtab_size` | `size_t`      | Byte size of the `strtab` buffer. |
| `flash_base`  | `uint32_t`    | ELF VMA of the first `PT_LOAD` segment (FLASH); used as Harvard offset base. |
| `flash_size`  | `uint32_t`    | Byte size of the FLASH load segment. |
| `sram_base`   | `uint32_t`    | ELF VMA of the SRAM segment. |
| `sram_size`   | `uint32_t`    | Byte size of the SRAM segment. |

Harvard address conversion formula applied by `elf_flash_addr()`:
`physical_word_addr = (vma − flash_base) / 2`

Both `.symtab` and `.strtab` are loaded fully into heap memory at `elf_open()` time and freed by `elf_close()`, so no file seeks are required during symbol lookup.]]></data_structures>
        <functions>
          <function signature="int elf_open(const char *path, ElfContext *ctx)"
                    summary="Open and parse the ELF file; populate ctx; return 0 or -1."/>
          <function signature="void elf_close(ElfContext *ctx)"
                    summary="Release all resources held by ctx."/>
          <function signature="int elf_find_avros_tables(ElfContext *ctx, AvrOsSymbolIndex *idx)">
            <purpose>Search the loaded symbol table for the avrOS system table boundary symbols and populate idx with resolved FLASH and SRAM addresses.</purpose>
            <pre><![CDATA[`ctx` has been successfully initialised by `elf_open()`; `idx` points to a zero-initialised `AvrOsSymbolIndex`.]]></pre>
            <post>All fields of `idx` that correspond to symbols found in the ELF are populated. Fields for missing symbols retain their zero-initialised values.</post>
            <returns>0 if at least the FSM table symbols were found and resolved; 0 with `idx` partially populated if only some symbols were found; -1 only on an internal ELF read error.</returns>
            <logic>
              <step>Iterate over all `sym_count` entries in `ctx->symtab`; skip entries with `st_name == 0` or `st_shndx == SHN_UNDEF`.</step>
              <step><![CDATA[Look up each symbol's name in `ctx->strtab` at offset `sym->st_name` and compare against the avrOS sentinel names (see algorithm section for the full symbol name table).]]></step>
              <step><![CDATA[For `_start` symbols (FLASH-resident table boundaries), call `elf_flash_addr()` to convert the VMA to a physical word address and store in the corresponding `idx` field.]]></step>
              <step><![CDATA[For `__stop_<NAME>` symbols, subtract the paired `__start_<NAME>` VMA and divide by the per-entry struct size to compute the entry count (e.g., `fsm_table_count`).]]></step>
              <step><![CDATA[For the `currStateMachine` symbol (SRAM-resident variable), store its VMA directly without the Harvard offset.]]></step>
            </logic>
          </function>
          <function signature="uint32_t elf_flash_addr(const ElfContext *ctx, uint32_t vma)"
                    summary="Convert an ELF virtual memory address to a physical FLASH word address."/>
        </functions>
        <algorithm><![CDATA[`elf_open()` validates and loads the ELF as follows:

1.  Read the first 4 bytes; verify the ELF magic (`0x7F 'E' 'L' 'F'`). Verify `e_ident[EI_CLASS] == ELFCLASS32`.
2.  Verify `e_machine == EM_AVR` (0x0053); reject non-AVR ELF files with a warning.
3.  Scan `e_phnum` program headers at offset `e_phoff`; for each `PT_LOAD` segment, record the segment VMA (`p_vaddr`) and size (`p_filesz`) to populate `flash_base` / `flash_size` (first LOAD segment) and `sram_base` / `sram_size` (second LOAD segment).
4.  Scan `e_shnum` section headers at offset `e_shoff`; find the section with `sh_type == SHT_SYMTAB`. Read its `sh_size / sizeof(Elf32_Sym)` entries into a heap buffer.
5.  Load the associated string table section (index given by `sh_link` on the `.symtab` section header) into a second heap buffer.

`elf_find_avros_tables()` scans the loaded symbol array for the following names:

| Symbol name             | `AvrOsSymbolIndex` field | Address space     | Per-entry stride |
| ----------------------- | ------------------------ | ----------------- | ---------------- |
| `__start_FSM_TABLE`     | `fsm_table_addr`         | FLASH (word addr) | 9 bytes (`fsmStateMachineDescr_t`)        |
| `__stop_FSM_TABLE`      | (size computation)       | FLASH             | — |
| `__start_QUE_TABLE`     | `queue_table_addr`       | FLASH (word addr) | 10 bytes (`queDescriptor_t`)              |
| `__stop_QUE_TABLE`      | (size computation)       | FLASH             | — |
| `__start_EVNT_TABLE`    | `event_table_addr`       | FLASH (word addr) | 4 bytes (`evntDescriptor_t`)              |
| `__stop_EVNT_TABLE`     | (size computation)       | FLASH             | — |
| `currStateMachine`      | `current_fsm_addr`       | SRAM (byte addr)  | — |

The boundary symbols `__start_<NAME>` and `__stop_<NAME>` are emitted by the avrOS application's custom linker script (`avrOS.x`) and bracket the input section that holds the corresponding registration table. Entry counts (e.g., `fsm_table_count`) are derived by computing `(__stop − __start) / sizeof(per_entry_descriptor)`, where the descriptor sizes match the 1-byte-packed AVR layout shown above.

**Heap memory budget for `elf_open()`:**

| Buffer | Formula | Typical size (1000-symbol AVR ELF) |
| ------ | ------- | ---------------------------------- |
| `ctx->symtab` | `sym_count x sizeof(Elf32_Sym)` = `sym_count x 16` B | ~16 KiB |
| `ctx->strtab` | `.strtab` section `sh_size` | ~8 KiB |
| Total | | ~24 KiB per session |

Both buffers are freed by `elf_close()`. Peak heap usage occurs between `elf_open()` and the first `elf_close()`.

**Parse complexity:** `elf_open()` performs O(e_phnum + e_shnum) file seeks plus two sequential block reads. `elf_find_avros_tables()` performs a single O(sym_count) linear scan with 8 string comparisons per symbol entry.

**Test approach for `src/elf_parser.c`:** Unit-testable using pre-built AVR ELF fixtures. Test cases cover: magic validation rejection, `EM_AVR` check, correct `flash_base` / `sram_base` extraction, all 7 avrOS sentinel symbols found with correct address conversions, partial symbol set (graceful degradation), `malloc` failure via injection shim, and `elf_close()` resource-free correctness.]]></algorithm>
        <dependencies>
          <dep>Standard C file I/O.</dep>
          <dep><![CDATA[ELF header definitions from `<elf.h>` (Linux) or a bundled `elf.h` for macOS portability.]]></dep>
        </dependencies>
        <error_handling>
          <case name="Non-ELF or wrong architecture magic">Log a warning message and return -1.</case>
          <case name="Missing avrOS symbols"><![CDATA[Log an informational message and return 0 with an empty `AvrOsSymbolIndex`; the server continues in bare-metal stub mode.]]></case>
          <case name="Memory allocation failure"><![CDATA[Return -1; the caller falls back to bare-metal stub mode.]]></case>
        </error_handling>
      </module>

      <module path="src/fsm_mapper.c">
        <purpose><![CDATA[translates the runtime state of avrOS FSM registration tables — read from the target via UPDI — into GDB virtual threads, providing per-thread synthetic register frames to the RSP layer.]]></purpose>
        <responsibility><![CDATA[Use the `AvrOsSymbolIndex` to read FSM state bytes from the target's SRAM over UPDI.]]></responsibility>
        <responsibility>Assign a stable GDB thread ID to each registered FSM entry.</responsibility>
        <responsibility>Identify and report which FSM is currently executing as the active GDB thread.</responsibility>
        <responsibility>Synthesize a minimal GDB register frame for each virtual thread, setting PC to the FSM's current state function pointer.</responsibility>
        <responsibility>Cache the thread list and invalidate it whenever the CPU resumes.</responsibility>
        <interfaces>
          <interface title="Public C API (src/fsm_mapper.h)"><![CDATA[All functions exported from `src/fsm_mapper.c` and declared in `src/fsm_mapper.h`:

```c
/* Thread-list management */
int  fsm_build_thread_list(FsmContext *ctx,
                           const AvrOsSymbolIndex *idx,
                           int updi_fd);
    /* Requires: CPU halted. Returns: thread count (>= 0); -1 on UPDI error. */
void fsm_invalidate(FsmContext *ctx);
    /* Marks ctx->valid = false; called immediately after updi_run()/updi_step(). */

/* Per-thread queries */
int  fsm_get_active_thread(const FsmContext *ctx);
    /* Returns: GDB thread ID of active FSM; 0 if not identified. */
int  fsm_get_registers(const FsmContext *ctx, int thread_id, char *reg_buf);
    /* Fills reg_buf with a 78-character hex string (39 bytes binary, hex-encoded: R0-R31, SREG, SPL, SPH, PC in g-packet order) plus NUL terminator; buf must be >= 79 bytes. */
    /* Returns: 0 on success; -1 if thread_id out of range. */
```

The `FsmContext` struct is declared in `src/fsm_mapper.h` and must be zero-initialised by the caller before the first `fsm_build_thread_list()` call.]]></interface>
        </interfaces>
        <data_structures><![CDATA[**`FsmThread`** — Descriptor for a single avrOS FSM virtual thread. One per registered FSM entry; stored in the static array `FsmContext.threads`.

| Field | Type | Description |
| ----- | ---- | ----------- |
| `gdb_id`    | `int`      | Stable GDB thread ID (1-based); assigned in the order FSM entries appear in the FLASH table and never re-assigned within a session. |
| `name`      | `char[32]` | Null-terminated human-readable FSM name, read from the FLASH-resident `name_ptr` field of the avrOS FSM table entry. |
| `state_fn`  | `uint32_t` | Current state function pointer as a FLASH word address; set to the value of the FSM's SRAM state variable at thread-list build time; used directly as the virtual PC. |
| `is_active` | `bool`     | True when this FSM is the currently executing task, as identified by comparing the FSM's `stateMachine` SRAM pointer against the avrOS `currStateMachine` scheduler variable. |

**`FsmContext`** contains a statically allocated array of `FSM_MAX_THREADS` (32) `FsmThread` entries, eliminating heap allocation in the thread build path.]]></data_structures>
        <functions>
          <function signature="int fsm_build_thread_list(FsmContext *ctx, const AvrOsSymbolIndex *idx, int updi_fd)">
            <purpose>Read the avrOS FSM registration table and SRAM state variables from the target via UPDI and populate the virtual thread cache.</purpose>
            <pre><![CDATA[Target CPU is halted; `idx->fsm_table_addr` and `idx->fsm_table_count` are valid; `updi_fd` is a valid UPDI serial fd.]]></pre>
            <post><![CDATA[`ctx->threads[0..thread_count-1]` contain populated `FsmThread` descriptors; `ctx->active_id` identifies the running FSM; `ctx->valid` is set to true.]]></post>
            <returns>Number of virtual threads populated on success; -1 on UPDI read failure.</returns>
            <logic>
              <step><![CDATA[Read `idx->fsm_table_count` consecutive 9-byte `fsmStateMachineDescr_t` records from FLASH at `idx->fsm_table_addr` using `updi_mem_read()`. Each descriptor contains, in order: `name` (2-byte FLASH ptr), `stateMachine` (2-byte SRAM ptr), `handler` (2-byte fn ptr), `priority` (1 byte), `instance` (2-byte u16).]]></step>
              <step><![CDATA[If a descriptor's `stateMachine` pointer is NULL (0x0000), treat the entry as an avrOS initializer slot and skip it without consuming a GDB thread ID.]]></step>
              <step><![CDATA[For each non-NULL `stateMachine` pointer, read 2 bytes from `stateMachine + 9` (SRAM); this is the `currState` field of `fsmStateMachine_t` and yields the current state function pointer (FLASH word address), which is zero-extended into `thread->state_fn`.]]></step>
              <step><![CDATA[Read the null-terminated FSM name from the FLASH address given by the descriptor's `name` pointer (up to 31 characters) and store in `thread->name`.]]></step>
              <step><![CDATA[Read the 2-byte SRAM value at `idx->current_fsm_addr` (avrOS `currStateMachine`); compare it to each retained entry's `stateMachine` pointer to identify the active FSM and set `thread->is_active` and `ctx->active_id`. If no entry matches, `ctx->active_id` is 0.]]></step>
              <step><![CDATA[Assign `thread->gdb_id = produced_index + 1` for each retained thread (GDB thread IDs are 1-based and never reuse a slot from a NULL-skipped initializer entry).]]></step>
            </logic>
          </function>
          <function signature="int fsm_get_active_thread(const FsmContext *ctx)"
                    summary="Return the GDB thread ID of the currently executing FSM."/>
          <function signature="int fsm_get_registers(const FsmContext *ctx, int thread_id, char *reg_buf)">
            <purpose>Synthesize a GDB g-packet register frame for the requested virtual thread and write it into reg_buf.</purpose>
            <pre><![CDATA[`ctx->valid` is true; `thread_id` is in the range [1, ctx->thread_count]; `reg_buf` points to a caller-allocated buffer of at least 79 bytes (39 binary bytes × 2 hex chars + NUL).]]></pre>
            <post><![CDATA[`reg_buf` contains a 78-character hex string representing the 36 AVR GDB register values (R0–R31, SREG, SPL, SPH, PC) in GDB `g`-packet order.]]></post>
            <returns>0 on success; -1 if thread_id is out of range.</returns>
            <logic>
              <step>Zero-initialise all 79 bytes of the register buffer.</step>
              <step><![CDATA[Set the PC field (GDB register index 35, 4-byte little-endian) at hex positions 70–77 of the buffer to `thread->state_fn`.]]></step>
              <step><![CDATA[If `thread->is_active` is true, read SREG (register 32), SPL (register 33), and SPH (register 34) from the target SRAM via `updi_mem_read()` and write them at hex positions 64–65, 66–67, and 68–69, respectively.]]></step>
            </logic>
            <notes>Non-active threads return zeroed R0–R31 and SREG; only PC and SP are meaningful for suspended FSMs in the avrOS cooperative model.</notes>
          </function>
          <function signature="void fsm_invalidate(FsmContext *ctx)"
                    summary="Clear the cached thread list; called on every CPU resume."/>
        </functions>
        <algorithm><![CDATA[**Thread list build sequence:**

Each avrOS FSM is represented in FLASH by a 9-byte `fsmStateMachineDescr_t` entry placed into the `FSM_TABLE` linker section by the `FSM_STATE_MACHINE(...)` registration macro:

```c
typedef struct {
    char                  *name;          /* FLASH pointer to display name string */
    fsmStateMachine_t     *stateMachine;  /* SRAM pointer to the FSM instance (NULL = init slot) */
    fsmStateHandler_t      handler;       /* FLASH function pointer */
    uint8_t                priority;
    uint16_t               instance;
} fsmStateMachineDescr_t;  /* 9 bytes, no padding */
```

`fsm_build_thread_list()` reads `idx->fsm_table_count` consecutive entries starting at `idx->fsm_table_addr`. Entries with a NULL `stateMachine` pointer are skipped (these are avrOS initializer slots, not user FSMs). For each retained entry, the function dereferences `stateMachine + 9` in SRAM to obtain the current state function pointer (the `currState` field of `fsmStateMachine_t`) and reads the FLASH `name` pointer to obtain the FSM display name. All UPDI reads are performed while the CPU is halted.

**Cache invalidation:** `fsm_invalidate()` sets `ctx->valid = false`. The RSP layer calls `fsm_invalidate()` immediately after `updi_run()` or `updi_step()` so that the stale thread list is not served to GDB after execution resumes. The cache is rebuilt on the next `fsm_build_thread_list()` call, which the RSP layer triggers when GDB halts the target again.

**AVR GDB register layout (g-packet order):**

| GDB register index | AVR register | Size | Notes |
| ------------------ | ------------ | ---- | ----- |
| 0-31 | R0-R31 | 1 byte each | General-purpose; zeroed for non-active threads. |
| 32 | SREG | 1 byte | Status register; zeroed for non-active threads. |
| 33 | SPL | 1 byte | Stack pointer low byte; live value for active thread only. |
| 34 | SPH | 1 byte | Stack pointer high byte; live value for active thread only. |
| 35 | PC | 4 bytes LE | Set to `thread->state_fn` (FLASH word address) for all threads. |

Total g-packet payload: 39 bytes × 2 hex chars = 78 hex characters + NUL. (Registers 0–34 each contribute 1 byte; register 35 (PC) contributes 4 bytes.)

**UPDI read budget per `fsm_build_thread_list()` call (N retained threads):**

| Operation | UPDI transactions | Data volume |
| --------- | ----------------- | ----------- |
| Read `currStateMachine` (2 bytes) | 1 read | 2 bytes |
| Read FSM descriptor per entry (9 bytes each) | N reads | 9N bytes |
| Read `currState` per retained entry (2 bytes each) | N reads | 2N bytes |
| Read FLASH name string per retained entry (up to 32 chars) | N reads | <=32N bytes |
| **Total** | 3N+1 transactions | <=43N+2 bytes |

**Test approach for `src/fsm_mapper.c`:** Unit-testable by providing a pre-populated `AvrOsSymbolIndex` and a mock `updi_mem_read()` returning canned byte sequences. Test cases cover: GDB thread ID assignment (1-based), correct `state_fn` FLASH word-address derivation, active thread identification, `FSM_MAX_THREADS` cap with warning, cache-invalidation round-trip, and `fsm_get_registers()` PC encoding verification.]]></algorithm>
        <dependencies>
          <dep><![CDATA[`src/updi.c` — `updi_mem_read()` for SRAM reads.]]></dep>
          <dep><![CDATA[`src/elf_parser.c` — `AvrOsSymbolIndex` for table addresses and entry counts.]]></dep>
        </dependencies>
        <error_handling>
          <case name="UPDI read failure during thread build">Return -1; the RSP layer reports a GDB error packet to the client.</case>
          <case name="Thread ID out of range"><![CDATA[Return -1 and cause the RSP layer to send error response `E01`.]]></case>
          <case name="FSM count exceeds FSM_MAX_THREADS"><![CDATA[Cap the build at `FSM_MAX_THREADS` entries and log a warning; excess FSMs are silently omitted from the thread list.]]></case>
        </error_handling>
      </module>

      <module path="src/monitor.c">
        <purpose><![CDATA[implements the custom `monitor avros` sub-commands that provide non-intrusive, human-readable visibility into the state of avrOS system objects (named events and queues) via UPDI background reads.]]></purpose>
        <responsibility><![CDATA[Parse the `monitor avros <subcommand>` argument string.]]></responsibility>
        <responsibility>Issue UPDI background reads to retrieve the relevant avrOS data structures without halting the CPU.</responsibility>
        <responsibility>Format the decoded state as human-readable text and deliver it to the GDB client as a console-output packet.</responsibility>
        <interfaces>
          <interface title="Public C API (src/monitor.h)"><![CDATA[Single function exported from `src/monitor.c` and declared in `src/monitor.h`:

```c
int monitor_dispatch(int rsp_fd, int updi_fd,
                     const AvrOsSymbolIndex *idx,
                     const char *cmd);
```

Parameters:
*   `rsp_fd` — connected GDB client socket fd; used to send O-packet responses via `rsp_send_packet()`.
*   `updi_fd` — UART serial fd; used for `updi_mem_read()` background reads.
*   `idx` — resolved avrOS symbol index from `elf_find_avros_tables()`; must not be NULL.
*   `cmd` — raw ASCII-hex-encoded payload string from a `qRcmd` RSP packet (not yet decoded).

Return values: 0 command recognised and executed; -1 UPDI read failure; -2 unrecognised sub-command.]]></interface>
          <interface title="GDB Monitor Sub-commands"><![CDATA[Two sub-commands are supported:

*   `monitor avros events` — list each named event registered in `EVNT_TABLE` with its current status flag value.
*   `monitor avros queues` — list each registered queue's capacity and per-element size from `QUE_TABLE`.]]></interface>
        </interfaces>
        <functions>
          <function signature="int monitor_dispatch(int rsp_fd, int updi_fd, const AvrOsSymbolIndex *idx, const char *cmd)">
            <purpose>Hex-decode the qRcmd payload, match the sub-command token, and invoke the appropriate handler.</purpose>
            <pre><![CDATA[`cmd` is the raw ASCII-hex-encoded command body extracted from a `qRcmd` RSP packet (not yet decoded).]]></pre>
            <returns>0 if the command was recognised and the handler returned without error; -1 on UPDI failure; -2 on unrecognised sub-command.</returns>
            <logic>
              <step>Hex-decode the ASCII hex pairs in `cmd` into a plain text command string.</step>
              <step><![CDATA[Verify the string starts with the prefix `"avros "` (case-sensitive); if not, send a usage hint O-packet and return -2.]]></step>
              <step>Extract the token following the prefix and compare it to `"events"` and `"queues"`.</step>
              <step>Invoke the matching static handler or send an unknown-subcommand error O-packet.</step>
            </logic>
          </function>
          <function signature="static int cmd_events(int rsp_fd, int updi_fd, const AvrOsSymbolIndex *idx)">
            <purpose>Iterate the avrOS `EVNT_TABLE` and report each named event's current status flag to the GDB console.</purpose>
            <logic>
              <step><![CDATA[Read `idx->event_count` consecutive `evntDescriptor_t` records (4 bytes each: `char *name`, `event_t *status`) from FLASH at `idx->event_table_addr` using `updi_mem_read()`.]]></step>
              <step><![CDATA[For each descriptor, follow the FLASH `name` pointer to read the human-readable event name (NUL-terminated) and the SRAM `status` pointer to read the current 1-byte status value.]]></step>
              <step><![CDATA[Append a line `"  <name>: <status>\n"` to an output buffer, then hex-encode and send as one or more RSP O-packets via `rsp_send_packet()`.]]></step>
            </logic>
          </function>
          <function signature="static int cmd_queues(int rsp_fd, int updi_fd, const AvrOsSymbolIndex *idx)">
            <purpose>Read each registered avrOS queue's capacity and per-element size and send a formatted table to the GDB console.</purpose>
            <logic>
              <step><![CDATA[Read `idx->queue_count` consecutive `queDescriptor_t` records (10 bytes each: queue/buffer/event pointers + 2-byte capacity + 2-byte sizeOfElement) from FLASH at `idx->queue_table_addr` using `updi_mem_read()`.]]></step>
              <step>For each descriptor, extract the `capacity` and `sizeOfElement` fields and format a one-line entry.</step>
              <step>Hex-encode and send the formatted table as RSP O-packets.</step>
            </logic>
          </function>
        </functions>
        <algorithm><![CDATA[**O-packet encoding:** All console output is delivered to the GDB client via RSP O-packets. The RSP specification requires the human-readable text to be hex-encoded: each ASCII character is converted to two hex digits. For example, the character `'A'` (0x41) is encoded as the two-character string `"41"`. `monitor_dispatch()` builds the human-readable text into a temporary 512-byte buffer, then hex-encodes it before calling `rsp_send_packet()` with the `O` prefix.

**Non-intrusive reads:** All three sub-commands use `updi_mem_read()` exclusively; none call `updi_halt()`. The UPDI background read mechanism allows SRAM to be sampled while the CPU is running, at the cost of a possible read-during-write data race for multi-byte fields. This is acceptable for the observational, non-critical nature of the monitor commands.

**avrOS runtime data structure layouts read by monitor commands:**

*Queue descriptor* (read by `cmd_queues()`, located in FLASH `QUE_TABLE`):
```c
typedef struct {
    void    *queue;          /* SRAM ring buffer pointer */
    void    *buffer;         /* SRAM element-storage pointer */
    event_t *event;          /* SRAM event flag pointer */
    uint16_t capacity;       /* maximum items the queue can hold */
    uint16_t sizeOfElement;  /* bytes per element */
} queDescriptor_t;  /* 10 bytes, FLASH */
```

*Event descriptor* (read by `cmd_events()`, located in FLASH `EVNT_TABLE`):
```c
typedef struct {
    char    *name;    /* FLASH pointer to event name string */
    event_t *status;  /* SRAM pointer to 1-byte status flag  */
} evntDescriptor_t;  /* 4 bytes, FLASH */
```

**O-packet size constraint:** A single RSP O-packet payload is limited to `RSP_PACKET_MAX` bytes (2048). For typical `cmd_events()` and `cmd_queues()` output the 512-byte text buffer is hex-encoded in chunks that each fit within one O-packet before calling `rsp_send_packet()`.

**Test approach for `src/monitor.c`:** Unit-testable by providing a mock `updi_mem_read()` returning canned bytes for the `EVNT_TABLE`/`QUE_TABLE` regions and capturing O-packet strings via a pipe replacing `rsp_fd`. Test cases cover: hex-decode of `qRcmd` payload, prefix rejection, both sub-command dispatch paths, correct event-name + status decoding, correct queue-field extraction, and O-packet encoding of all output characters.]]></algorithm>
        <dependencies>
          <dep><![CDATA[`src/updi.c` — `updi_mem_read()` for non-intrusive SRAM reads.]]></dep>
          <dep><![CDATA[`src/elf_parser.c` — `AvrOsSymbolIndex` for data structure addresses.]]></dep>
        </dependencies>
        <error_handling>
          <case name="Unrecognized sub-command">Return a human-readable error message to the GDB console output packet.</case>
          <case name="UPDI read failure">Report partial data alongside an error message to the GDB console; do not abort the server.</case>
        </error_handling>
      </module>
    </modules>

    <data_dictionary>
      <type name="AppConfig"
            header="src/main.c"
            summary="Application-wide configuration populated by parse_args().">
        <field name="serial_device" type="const char *" desc="Path to the UART serial device."/>
        <field name="elf_path"      type="const char *" desc="Path to the AVR ELF binary."/>
        <field name="gdb_port"      type="uint16_t"     desc="TCP port for the GDB listener (default 1234)."/>
        <field name="baud_rate"     type="int"          desc="UART baud rate (default 115200)."/>
        <field name="load_flash"    type="bool"         desc="When true, flash the ELF to the target before attaching."/>
      </type>
      <type name="AvrOsSymbolIndex"
            header="src/elf_parser.c"
            summary="Index of avrOS system table addresses resolved from the ELF symbol table.">
        <field name="fsm_table_addr"     type="uint32_t" desc="FLASH word address of the avrOS FSM registration table (`__start_FSM_TABLE`)."/>
        <field name="fsm_table_count"    type="uint8_t"  desc="Number of `fsmStateMachineDescr_t` entries in `FSM_TABLE` (size / 9)."/>
        <field name="queue_table_addr"   type="uint32_t" desc="FLASH word address of the queue registration table (`__start_QUE_TABLE`)."/>
        <field name="queue_count"        type="uint8_t"  desc="Number of `queDescriptor_t` entries in `QUE_TABLE` (size / 10)."/>
        <field name="event_table_addr"   type="uint32_t" desc="FLASH word address of the named-event registration table (`__start_EVNT_TABLE`)."/>
        <field name="event_count"        type="uint8_t"  desc="Number of `evntDescriptor_t` entries in `EVNT_TABLE` (size / 4)."/>
        <field name="current_fsm_addr"   type="uint32_t" desc="SRAM byte address of the avrOS scheduler `currStateMachine` pointer variable; used by fsm_mapper to identify the active thread."/>
      </type>
      <type name="FsmThread"
            header="src/fsm_mapper.c"
            summary="Descriptor for a single avrOS FSM virtual thread, stored in the FsmContext thread array.">
        <field name="gdb_id"   type="int"      desc="Stable GDB thread ID (1-based); assigned at thread-list build time and never re-used within a session."/>
        <field name="name"     type="char[32]" desc="Null-terminated human-readable FSM name read from the FLASH-resident table entry name pointer."/>
        <field name="state_fn" type="uint32_t" desc="Current state function pointer as a FLASH word address; used directly as the virtual PC in the synthesized register frame."/>
        <field name="is_active" type="bool"    desc="True when this FSM is the currently executing task as identified from the avrOS current_fsm scheduler variable."/>
      </type>
      <type name="ElfContext"
            header="src/elf_parser.c"
            summary="All ELF parsing state maintained between elf_open() and elf_close().">
        <field name="fd"           type="int"         desc="Open file descriptor for the ELF binary."/>
        <field name="ehdr"         type="Elf32_Ehdr"  desc="Cached ELF32 file header (magic, machine type, entry point, section/program header counts)."/>
        <field name="symtab"       type="Elf32_Sym *" desc="Heap-allocated copy of the .symtab section."/>
        <field name="sym_count"    type="size_t"      desc="Number of Elf32_Sym entries in symtab."/>
        <field name="strtab"       type="char *"      desc="Heap-allocated copy of the .strtab string table used for symbol name lookup."/>
        <field name="strtab_size"  type="size_t"      desc="Byte size of the strtab buffer."/>
        <field name="flash_base"   type="uint32_t"    desc="ELF VMA of the first PT_LOAD segment; used as the Harvard FLASH base for address conversion."/>
        <field name="flash_size"   type="uint32_t"    desc="Byte size of the FLASH load segment."/>
        <field name="sram_base"    type="uint32_t"    desc="ELF VMA of the SRAM segment."/>
        <field name="sram_size"    type="uint32_t"    desc="Byte size of the SRAM segment."/>
      </type>
      <type name="FsmContext"
            header="src/fsm_mapper.c"
            summary="Runtime cache of the avrOS virtual thread list.">
        <field name="threads"      type="FsmThread[FSM_MAX_THREADS]" desc="Statically allocated array of virtual thread descriptors (one per FSM entry)."/>
        <field name="thread_count" type="int"         desc="Number of valid entries populated in the threads array."/>
        <field name="active_id"    type="int"         desc="GDB thread ID of the currently executing FSM; 0 if none identified."/>
        <field name="valid"        type="bool"        desc="True when the cache reflects current target state; cleared to false by fsm_invalidate() on every CPU resume."/>
      </type>
      <constants header="src/updi.c">
        <constant name="UPDI_SYNCH"          value="0x55"  purpose="UPDI synchronisation byte transmitted at the start of each command."/>
        <constant name="UPDI_ACK"            value="0x40"  purpose="Acknowledgement byte returned by the target after each received data byte."/>
        <constant name="UPDI_MAX_BLOCK"      value="256"   purpose="Maximum byte count for a single REPEAT+LD/ST burst transaction."/>
        <constant name="UPDI_BREAK_BAUD"     value="300"   purpose="Temporary baud rate used to assert the BREAK condition during link initialisation."/>
        <constant name="UPDI_ERR_WP"         value="-2"    purpose="Distinct error code returned by updi_nvm_write_flash() when FLASH write protection is active."/>
      </constants>
      <other><![CDATA[*   **Compile-time constants** (in [src/gdb_rsp.c](../src/gdb_rsp.c)):

    | Name | Value | Purpose |
    | ---- | ----- | ------- |
    | `RSP_PACKET_MAX`      | `2048` | Maximum RSP payload size in bytes; sized for a full AVR register dump plus `qXfer` XML overhead. |
    | `RSP_MAX_BREAKPOINTS` | `2`    | Number of AVR-Dx OCD hardware-breakpoint comparators tracked by the RSP session context (`BP0` and `BP1`). |

*   **Compile-time constants** (in [src/fsm_mapper.c](../src/fsm_mapper.c)):

    | Name | Value | Purpose |
    | ---- | ----- | ------- |
    | `FSM_MAX_THREADS` | `32` | Maximum number of virtual threads supported in a single session; caps the static `FsmContext` thread array. |

---

### Build Toolchain and Host Environment

**Host compiler requirements:**

| Requirement | Minimum | Notes |
| ----------- | ------- | ----- |
| C standard | C99 | `--std=c99`; no GNU extensions required. |
| GCC | 4.8 | Or Clang ≥ 3.4. |
| POSIX API | POSIX.1-2008 | `-D_POSIX_C_SOURCE=200809L` |
| ELF headers | `<elf.h>` | Provided by `glibc-headers` on Linux; bundled `elf.h` on macOS. |
| libc | any POSIX libc | No additional shared libraries required. |

**Recommended compile flags:**

```
CFLAGS = -std=c99 -D_POSIX_C_SOURCE=200809L -Wall -Wextra -Wpedantic
         -Wstrict-prototypes -Wmissing-prototypes -Wshadow
         -O2 -g
```

Debug builds add `-fsanitize=address,undefined` for runtime error detection.

**Supported host platforms:**

| Platform | Status | Notes |
| -------- | ------ | ----- |
| Linux (x86-64, ARM64) | Primary target | Tested on Ubuntu 22.04 and Raspberry Pi OS. |
| macOS (Intel, Apple Silicon) | Secondary target | Requires bundled `elf.h`; uses `/dev/cu.usbserial-*` device paths. |
| Windows (native) | Out of scope | See `doc/PVD.md §7.2`; WSL2 may work but is not supported. |

**Build targets (Makefile):**

| Target | Description |
| ------ | ----------- |
| `all` | Build the `avrOSdb` binary (default). |
| `clean` | Remove build artefacts. |
| `test` | Build and run the unit test suite. |
| `install` | Copy the binary to `$(PREFIX)/bin` (default `/usr/local/bin`). |

---

### Target MCU Memory Constraints

The following constraints govern the AVR DA/DB-family target assumed by the implementation:

| Parameter | Value | Notes |
| --------- | ----- | ----- |
| FLASH word size | 2 bytes | AVR is a 16-bit instruction machine; word addresses = byte addresses / 2. |
| FLASH page size | 512 bytes | Page boundary alignment required for `updi_nvm_write_flash()`. |
| Maximum FLASH | 128 KiB | AVR128DA/DB; upper bound for `ElfContext.flash_size`. |
| Maximum SRAM | 16 KiB | AVR128DA/DB; upper bound for `ElfContext.sram_size`. |
| FLASH base VMA | `0x0000` | ELF virtual address of the first instruction word. |
| SRAM base VMA | `0x0200` | Lowest SRAM address on AVR DA/DB (I/O registers below this). |
| UPDI base address | `0x1000` | NVM controller register base used for flash programming. |
| AVR BREAK opcode | `0x9598` | Two-byte instruction used for software breakpoints. |

---

### Static Memory Footprint Summary

Approximate static (BSS + data) memory consumed by each module in the host process:

| Module | Major static buffers | Approximate size |
| ------ | -------------------- | ---------------- |
| `src/main.c` | `AppConfig` struct (stack-allocated in `main`) | ~32 bytes |
| `src/updi.c` | `termios` structs, internal staging buffer | ~256 bytes |
| `src/gdb_rsp.c` | Receive + transmit packet buffers, breakpoint table | ~4.2 KiB |
| `src/elf_parser.c` | Heap: `.symtab` + `.strtab` (per session) | ~24 KiB (heap) |
| `src/fsm_mapper.c` | `FsmContext` static thread array (32 x `FsmThread`) | ~2.1 KiB |
| `src/monitor.c` | Output text staging buffer | ~512 bytes |
| **Total static** | | **~7 KiB static + ~24 KiB heap per session** |

---

### Module Integration Sequence

Modules should be integrated and verified in the following order to isolate failures:

| Step | Modules | Integration milestone | Verification method |
| ---- | ------- | --------------------- | ------------------- |
| 1 | `src/updi.c` alone | UPDI link initialisation and basic memory read/write | Loopback pseudo-terminal test; compare read-back bytes against written values. |
| 2 | `src/elf_parser.c` alone | ELF open, symbol scan, `AvrOsSymbolIndex` population | Offline test against a known AVR ELF binary; compare resolved addresses against `avr-nm` output. |
| 3 | `src/updi.c` + `src/elf_parser.c` | Halt, read FLASH/SRAM at resolved symbol addresses, resume | Live hardware: read FSM table bytes, verify against expected avrOS registration. |
| 4 | `src/fsm_mapper.c` | Thread-list build and register frame synthesis | Offline test with mock UPDI reads; live hardware to verify correct FSM names and state pointers. |
| 5 | `src/gdb_rsp.c` alone | TCP accept, packet codec, dispatch to stub handlers | Loopback socket: drive with `avr-gdb`'s `target extended-remote localhost:1234`, issue `info threads`. |
| 6 | `src/monitor.c` | `monitor avros events/queues` output | Live hardware: run target, issue monitor commands from GDB, verify output format and values. |
| 7 | All modules | Full session: attach, set breakpoint, halt, inspect threads, resume | End-to-end GDB script driven test against live AVR hardware. |

---

### Error Propagation Model

Errors are propagated upward through the module stack without retrying:

| Layer | Error type | Propagation rule |
| ----- | ---------- | ---------------- |
| `src/updi.c` | UART timeout, framing error, UPDI NAK | Return -1 to caller. No retry at this layer. |
| `src/elf_parser.c` | I/O error, bad ELF, malloc failure | Return -1; server enters bare-metal stub mode (no FSM threads). |
| `src/fsm_mapper.c` | UPDI read failure | Return -1; RSP layer sends `E02` error packet to GDB. |
| `src/gdb_rsp.c` | Socket error, checksum mismatch | NAK on mismatch; close socket on disconnect; send `E` packet on UPDI errors. |
| `src/monitor.c` | UPDI read failure | Send partial output with error message as O-packet; return -1 but do not abort server. |
| `src/main.c` | Fatal startup error | `exit(1)` with message to stderr. Mid-session UPDI loss: close client, await next connection. |]]></other>
    </data_dictionary>

    <traceability>
      <theme name="CLI and Initialization"            sections="§3 (src/main.c)"/>
      <theme name="UPDI Physical Layer"               sections="§4 (src/updi.c)"/>
      <theme name="GDB RSP Server"                    sections="§5 (src/gdb_rsp.c)"/>
      <theme name="ELF Parsing and Symbol Resolution" sections="§6 (src/elf_parser.c)"/>
      <theme name="FSM Virtual Thread Mapping"        sections="§7 (src/fsm_mapper.c)"/>
      <theme name="System Introspection"              sections="§8 (src/monitor.c)"/>
      <theme name="Flash Programming"                 sections="§4 (src/updi.c)"/>
      <theme name="Console Bridge"                    sections="§4 (src/updi.c)"/>
    </traceability>
  </sdd>

  <!-- Software Test Plan payload (see tools/Developers_Guide.md §4). -->
  <stp>
  </stp>

  <!-- High-Level Requirements (see tools/Developers_Guide.md §5). -->
  <hlrs>

    <section number="1" title="System Startup and Command-Line Interface">
      <intro><![CDATA[Requirements in this section govern how `avrOSdb` is invoked, how it initialises its resources, and how quickly it becomes ready for a GDB connection.]]></intro>

      <hlr id="HLR-001" name="CLI Argument Parsing">
        <text><![CDATA[The application shall accept the following command-line arguments: a required positional `<serial-device>` path, a required positional `<elf-file>` path, an optional `--port <port>` TCP port (default `1234`), an optional `--baud <baud>` UART baud rate (default `115200`), and an optional `--load` flag. Any unrecognised argument shall cause the application to print a usage message to `stderr` and exit with a non-zero status.]]></text>
        <traces>
          <trace target="SDD" ref="3.2.2"/>
        </traces>
      </hlr>

      <hlr id="HLR-002" name="Serial Device Initialisation">
        <text><![CDATA[The application shall open the specified UART serial device and configure it for UPDI communication (8N2, half-duplex) at the specified baud rate before accepting any GDB client connection.]]></text>
        <traces>
          <trace target="SDD" ref="3.1"/>
          <trace target="SDD" ref="4.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-003" name="GDB Listener Startup">
        <text><![CDATA[The application shall bind and listen on the configured TCP port for incoming GDB client connections. Exactly one GDB client connection shall be active at a time; after a client detaches the server shall re-enter the listening state.]]></text>
        <traces>
          <trace target="SDD" ref="3.1"/>
          <trace target="SDD" ref="5.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-004" name="ELF Load Option">
        <text><![CDATA[When the `--load` flag is provided, the application shall classify every `PT_LOAD` segment of the supplied ELF binary by virtual address against the AVR-Dx unified UPDI address windows (FLASH, EEPROM, USERROW, FUSES, LOCK, SIGROW) and dispatch each segment to the matching UPDI NVM write routine before entering the GDB listener loop. Segments whose virtual addresses fall outside every programmable window shall cause `--load` to fail with a diagnostic; SIGROW segments shall be silently skipped (signature row is read-only); SRAM-resident segments shall be skipped without programming.]]></text>
        <traces>
          <trace target="SDD" ref="3.2.2"/>
          <trace target="SDD" ref="4.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-005" name="Attach Time Constraint">
        <text><![CDATA[The server shall complete initialisation, locate avrOS system tables in FLASH, and be ready to respond to GDB packets within 2.0 seconds of the GDB client connection being accepted.]]></text>
        <traces>
          <trace target="SDD" ref="2.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-035" name="Graceful Shutdown and Resource Release">
        <text><![CDATA[On receipt of SIGINT, SIGTERM, a GDB `k` (kill) packet, or a GDB `D` (detach) packet, the application shall release all allocated resources in the following order: restore any patched FLASH breakpoint locations via UPDI NVM write, free the ELF symbol table heap allocation, close the GDB client socket file descriptor, close the UPDI serial device file descriptor, and close the TCP listener socket file descriptor. The server shall exit with status 0 on a clean shutdown and status 1 on a fatal initialisation failure.]]></text>
        <traces>
          <trace target="SDD" ref="3.3.3"/>
        </traces>
      </hlr>
    </section>

    <section number="2" title="UPDI Physical Layer">
      <intro><![CDATA[Requirements in this section govern the hardware connection to the AVR target and all UPDI protocol operations.]]></intro>

      <hlr id="HLR-006" name="UPDI Hardware Connection">
        <text><![CDATA[The application shall communicate with the AVR target exclusively over a TTL-level UART serial link using UPDI framing. The connection shall require only a 1 kΩ resistor between the host adapter TX/RX and the target UPDI pin; no JTAG controller, proprietary programmer, or external hardware debugger shall be required.]]></text>
        <traces>
          <trace target="SDD" ref="4.2.2"/>
        </traces>
      </hlr>

      <hlr id="HLR-007" name="Target Memory Read">
        <text><![CDATA[The application shall read arbitrary byte ranges from both the SRAM and FLASH address spaces of the connected AVR target via UPDI LD commands.]]></text>
        <traces>
          <trace target="SDD" ref="4.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-008" name="Target Memory Write">
        <text><![CDATA[The application shall write arbitrary byte ranges to the SRAM address space of the connected AVR target via UPDI ST commands.]]></text>
        <traces>
          <trace target="SDD" ref="4.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-009" name="FLASH Programming">
        <text><![CDATA[The application shall erase and program pages of the target FLASH using the UPDI NVM controller write-page sequence (NVMCTRL command `ERWP` = `0x03`, page-aligned address, 512-byte pages padded with `0xFF`, per-page `FBUSY` poll on `NVMSTATUS` bit 0). FLASH programming is one of several NVM kinds dispatched by `--load`; see HLR-046 for the EEPROM/USERROW/FUSES/LOCK kinds and HLR-047 for the lockbit safety interlock. FLASH programming also underpins software breakpoint insertion.]]></text>
        <traces>
          <trace target="SDD" ref="4.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-010" name="Execution Control">
        <text><![CDATA[The application shall halt, resume, and single-step the AVR CPU core via the AVR On-Chip Debug (OCD) interface layered on top of UPDI, mapping directly to GDB continue, step, and stop operations. `updi_enter_debug()` shall arm OCD mode at session start by sending the `OCD ` 8-byte KEY followed by an `ASI_RESET_REQ` pulse, after which `updi_halt()`, `updi_run()`, `updi_step()`, `updi_ocd_poll_halted()`, and `updi_ocd_read_halt_status()` provide the primitives that the RSP layer composes into GDB stop/continue/step operations.]]></text>
        <traces>
          <trace target="SDD" ref="4.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-011" name="Non-Intrusive Background Memory Read">
        <text><![CDATA[The application shall read target memory regions via UPDI background reads while the CPU core is running, without issuing a halt. This mechanism shall be used for all system introspection operations that do not require a stopped core.]]></text>
        <traces>
          <trace target="SDD" ref="4.1"/>
          <trace target="SDD" ref="4.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-012" name="UPDI Console Bridge">
        <text><![CDATA[The application shall poll the avrOS software UART console channel via UPDI and forward any received bytes to host `stdout`, enabling interactive CLI sessions with the running firmware and automated test output capture without halting the CPU.]]></text>
        <traces>
          <trace target="SDD" ref="4.1"/>
          <trace target="SDD" ref="4.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-036" name="UPDI Cold-Start Contention Handshake">
        <text><![CDATA[The UPDI link initialisation sequence shall make a fresh power-on AVR-Dx target reachable on host wirings that combine TX and RX onto the single UPDI line via a passive resistor (notably Raspberry Pi PL011 and FTDI-based serial adapters), where the host's push-pull idle-high TX otherwise drives the target's UPDI collision detector and silences responses. `updi_open()` shall execute up to three cold-start attempts that mirror avrdude's `serialupdi` programmer (the reference implementation against which behaviour has been verified by `strace`): attempt 0 (fast path, succeeds against an already-prepped target) shall transmit one 0x00 wake byte at session baud followed by `tcdrain()` + `tcflush(TCIFLUSH)`, then `STCS ASI_CTRLB=CCDETDIS`, `STCS ASI_CTRLA=IBDLY`, and `LDCS ASI_STATUSA`; attempts 1 and 2 (slow path, required on cold power-on) shall instead generate a multi-millisecond line-low BREAK by switching the kernel baud to 300 with one stop bit (`CSTOPB` cleared), writing two 0x00 bytes each followed by `tcdrain()` (each byte ≈33 ms line-low = ≥60 ms total — enough to reset the target's UPDI clock and contention detector) and consuming their half-duplex echoes, then waiting ≈50 ms for any in-flight line bytes to settle, performing `tcflush(TCIFLUSH)`, restoring session baud (`tcsetattr(TCSADRAIN)` + `tcflush(TCIFLUSH)`), and repeating the STCS+STCS+LDCS sequence. After a successful `LDCS ASI_STATUSA` the link probe is followed by a 32-byte System Information Block read (`SYNCH` + `0xE6`) which both confirms the link end-to-end and wakes any target left in UPDI SLEEP from a prior session — without this step a sleeping target accepts link-layer probes but rejects every memory access. After three consecutive failed attempts `updi_open()` shall return -1.]]></text>
        <traces>
          <trace target="SDD" ref="4.3.3"/>
        </traces>
      </hlr>

      <hlr id="HLR-037" name="UPDI Operation Timeout Bounds">
        <text><![CDATA[All UPDI polling loops shall implement explicit timeouts to prevent indefinite blocking: each `updi_mem_read()` `read()` call shall be guarded by a `select()` with a 100 ms deadline; the NVMPROG mode entry poll shall time out after 100 iterations (≈ 100 ms); the per-page NVM BUSY poll shall time out after 20 iterations (≈ 20 ms) per page. On any timeout the failing UPDI function shall return -1 rather than blocking or spinning.]]></text>
        <traces>
          <trace target="SDD" ref="4.3.3"/>
        </traces>
      </hlr>
    </section>

    <section number="3" title="GDB Remote Serial Protocol Server">
      <intro><![CDATA[Requirements in this section govern the GDB RSP server behaviour, covering packet handling, register and memory access, breakpoint management, and session lifecycle.]]></intro>

      <hlr id="HLR-013" name="RSP Server Accessibility">
        <text><![CDATA[The application shall implement a GDB Remote Serial Protocol server accessible via a standard `target extended-remote <host>:<port>` command from `avr-gdb` or any GDB-compatible front-end (VS Code Cortex-Debug, Zed DAP adapter).]]></text>
        <traces>
          <trace target="SDD" ref="5.2.2"/>
        </traces>
      </hlr>

      <hlr id="HLR-014" name="Register Read and Write">
        <text><![CDATA[The application shall respond to GDB register read (`g`) and write (`G`, `P`) packets, returning or updating the current AVR CPU register state (32 general-purpose registers, PC, SP, and status register) obtained from or written to the target via the AVR-Dx OCD register file at UPDI base `0x0F80`. Reads against the active GDB thread (or against any thread when no FSM context is established) shall return live OCD values; reads against a non-active virtual FSM thread shall return the synthesized register frame from the FSM mapper.]]></text>
        <traces>
          <trace target="SDD" ref="5.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-015" name="Memory Read and Write">
        <text><![CDATA[The application shall respond to GDB memory read (`m`) and write (`M`, `X`) packets, mapping each request to the corresponding UPDI memory operation on the target. Requests addressing the FLASH region shall be routed to NVM read or write sequences as appropriate.]]></text>
        <traces>
          <trace target="SDD" ref="5.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-016" name="Breakpoints">
        <text><![CDATA[The application shall implement code breakpoints by programming the two AVR-Dx OCD hardware comparators (`BP0`, `BP1`). Both `Z0` (software breakpoint) and `Z1` (hardware breakpoint) requests from GDB shall be routed to the same two hardware comparators, since installing the AVR `BREAK` opcode at runtime would require exiting OCD mode, entering NVMPROG (which resets the CPU and destroys live register/SREG/SP state), patching the FLASH page, and re-entering OCD — a sequence whose state-preservation cost outweighs the benefit on parts with only 32 KiB of FLASH per session. The shadow of the two comparator slots shall live in the RSP session context so that detach/reattach cycles leave silicon in a known state. Attempting to install a third breakpoint shall return GDB error reply `E08`; a duplicate insert at an already-installed address shall return `OK` without re-programming the comparator.]]></text>
        <traces>
          <trace target="SDD" ref="4.3.1"/>
          <trace target="SDD" ref="5.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-017" name="Single-Step Execution">
        <text><![CDATA[The application shall respond to the GDB single-step (`s`/`S`) packet by executing exactly one AVR instruction on the target CPU via the UPDI step primitive and reporting the resulting stop reason.]]></text>
        <traces>
          <trace target="SDD" ref="4.3.1"/>
          <trace target="SDD" ref="5.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-018" name="Continue Execution">
        <text><![CDATA[The application shall respond to the GDB continue (`c`/`C`) packet by resuming target CPU execution via `updi_run()` and then polling for a stop condition. While polling, the server shall remain responsive to the GDB Ctrl-C (`0x03`) async-interrupt byte on the client socket and shall halt the target via `updi_halt()` when one is received. The stop-reason packet shall report SIGINT (`T02`) when the halt was caused by Ctrl-C and SIGTRAP (`T05`) for every other halt cause (hardware breakpoint, BREAK opcode, single-step, external break). To bound recovery time on a flaky UPDI link, the run-poll loop shall give up after a fixed number of consecutive `updi_ocd_poll_halted()` failures and return GDB error reply `E01` rather than spinning indefinitely.]]></text>
        <traces>
          <trace target="SDD" ref="4.3.1"/>
          <trace target="SDD" ref="5.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-019" name="RSP Capability Negotiation and Lifecycle">
        <text><![CDATA[The application shall negotiate RSP capabilities with the GDB client via `qSupported` and shall handle the standard lifecycle packets `qAttached`, `vAttach`, `D` (detach), and `k` (kill). On detach the server shall re-enter the listen state without requiring a restart.]]></text>
        <traces>
          <trace target="SDD" ref="5.1"/>
          <trace target="SDD" ref="5.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-020" name="Editor-Agnostic Protocol">
        <text><![CDATA[The server shall communicate exclusively over standard GDB RSP. No IDE-specific protocol extensions (DAP, Cortex-Debug custom packets, or similar) shall be implemented inside the server; all IDE integration shall be the responsibility of the client-side adapter.]]></text>
        <traces>
          <trace target="SDD" ref="2.2"/>
          <trace target="SDD" ref="5.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-038" name="GDB Session Response Latency">
        <text><![CDATA[The GDB client socket shall be configured with `TCP_NODELAY` to disable Nagle's algorithm, ensuring that each RSP response packet is transmitted without artificial buffering delay. The listener socket shall be configured with `SO_REUSEADDR` so that the server may be restarted on the same port immediately after process exit without waiting for the TCP TIME_WAIT state to expire.]]></text>
        <traces>
          <trace target="SDD" ref="5.3.3"/>
        </traces>
      </hlr>
    </section>

    <section number="4" title="ELF Parsing and Symbol Resolution">
      <intro><![CDATA[Requirements in this section govern how the server locates avrOS system tables in the target firmware and handles missing or incomplete debug information.]]></intro>

      <hlr id="HLR-021" name="ELF Binary Parsing">
        <text><![CDATA[The application shall parse the AVR ELF binary provided at launch to validate its architecture, locate the symbol table section, and build an index of avrOS system table symbols (`FSM_TABLE`, `QUE_TABLE`, `EVNT_TABLE`, and `currStateMachine`).]]></text>
        <traces>
          <trace target="SDD" ref="6.1"/>
          <trace target="SDD" ref="6.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-022" name="Harvard Architecture Address Mapping">
        <text><![CDATA[The application shall apply the AVR Harvard architecture offset when converting ELF virtual memory addresses to physical FLASH word addresses, ensuring that all symbol references resolve to correct target locations regardless of the ELF's load segment layout.]]></text>
        <traces>
          <trace target="SDD" ref="6.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-023" name="Fail-Safe Degradation on Missing Symbols">
        <text><![CDATA[When the ELF binary does not contain the expected avrOS system table symbols (e.g., firmware compiled without debug symbols, or non-avrOS firmware), the application shall log an informational diagnostic and degrade gracefully to a standard bare-metal GDB stub, rather than aborting the session or returning an error to the GDB client.]]></text>
        <traces>
          <trace target="SDD" ref="2.2"/>
          <trace target="SDD" ref="6.5"/>
        </traces>
      </hlr>
    </section>

    <section number="5" title="FSM Virtual Thread Mapping">
      <intro><![CDATA[Requirements in this section govern how the server translates the avrOS cooperative scheduler state into GDB virtual threads visible in the IDE's call-stack and thread panes.]]></intro>

      <hlr id="HLR-024" name="FSM Thread Enumeration">
        <text><![CDATA[Upon halting the target, the application shall read the avrOS FSM registration table from target SRAM via UPDI and present each registered FSM entry as a distinct GDB virtual thread, identified by a stable thread ID.]]></text>
        <traces>
          <trace target="SDD" ref="7.1"/>
          <trace target="SDD" ref="7.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-025" name="Active Thread Identification">
        <text><![CDATA[The application shall identify the currently executing avrOS FSM from the runtime state read via UPDI and report it to the GDB client as the active thread. All register reads and memory operations issued without an explicit thread context shall operate on the active thread's context.]]></text>
        <traces>
          <trace target="SDD" ref="7.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-026" name="Virtual Thread Register Frame">
        <text><![CDATA[Each virtual thread shall present a synthesized GDB register frame in which the program counter (PC) is set to the function pointer of the FSM's current state handler. For the active virtual thread, SP and SREG shall be read live from the target's hardware registers and inserted into the register frame. For all non-active virtual threads, SP and SREG shall be set to zero, as dormant FSMs do not maintain independent stacks in the avrOS cooperative scheduler model.]]></text>
        <traces>
          <trace target="SDD" ref="7.1"/>
          <trace target="SDD" ref="7.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-027" name="Complete FSM Thread Coverage">
        <text><![CDATA[All FSMs registered in the avrOS FSM table shall appear as virtual threads in the GDB client thread pane upon halting at or after `main()`, up to a maximum of `FSM_MAX_THREADS` (32) entries. If more than `FSM_MAX_THREADS` FSMs are registered, the server shall silently cap the virtual thread list at 32 and log a diagnostic warning; no registered FSM within the cap shall be omitted.]]></text>
        <traces>
          <trace target="SDD" ref="7.1"/>
          <trace target="SDD" ref="7.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-028" name="Stack-Free Thread Model">
        <text><![CDATA[The application shall not attempt to unwind stack frames for suspended FSMs. The execution context of a non-active virtual thread shall be defined solely by its state function pointer; the server shall never dereference a dormant stack pointer to infer thread state.]]></text>
        <traces>
          <trace target="SDD" ref="2.2"/>
          <trace target="SDD" ref="7.1"/>
        </traces>
      </hlr>
    </section>

    <section number="6" title="System Introspection">
      <intro><![CDATA[Requirements in this section govern the custom `monitor avros` commands that expose avrOS runtime object state to the developer without halting the target CPU.]]></intro>

      <hlr id="HLR-029" name="Monitor Events Command">
        <text><![CDATA[The application shall implement a `monitor avros events` command that iterates the avrOS `EVNT_TABLE` and, for each registered named event, reads the current 1-byte status flag from SRAM via UPDI background read and returns a human-readable `<name>: <status>` listing to the GDB console, without halting the CPU core.]]></text>
        <traces>
          <trace target="SDD" ref="8.2.2"/>
          <trace target="SDD" ref="8.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-030" name="Monitor Queues Command">
        <text><![CDATA[The application shall implement a `monitor avros queues` command that iterates the avrOS `QUE_TABLE` and reports each registered queue's `capacity` and `sizeOfElement` from the FLASH descriptor via UPDI background read, returning a human-readable table to the GDB console without halting the CPU core.]]></text>
        <traces>
          <trace target="SDD" ref="8.2.2"/>
          <trace target="SDD" ref="8.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-032" name="Introspection Reliability">
        <text><![CDATA[The `monitor avros events` command shall accurately reflect the current event status flags across at least 10,000 consecutive invocations of `monitor_dispatch()` with the `avros events` sub-command without producing a UPDI protocol desync, a corrupted response, or a server crash.]]></text>
        <traces>
          <trace target="SDD" ref="4.3.1"/>
          <trace target="SDD" ref="8.1"/>
        </traces>
      </hlr>
    </section>

    <section number="7" title="Platform and Build">
      <intro><![CDATA[Requirements in this section govern the host platforms the server must support and the constraints on its runtime dependencies.]]></intro>

      <hlr id="HLR-033" name="Native Linux and macOS Build">
        <text><![CDATA[The application shall compile and execute natively on Linux (x86-64 and ARM) and macOS (x86-64 and Apple Silicon) using a C99-conforming toolchain. The build shall not require any platform-specific preprocessor workarounds beyond what the C99 standard and POSIX.1-2008 provide.]]></text>
        <traces>
          <trace target="SDD" ref="2.2"/>
        </traces>
      </hlr>

      <hlr id="HLR-034" name="Lean Host Runtime Dependencies">
        <text><![CDATA[The application runtime shall depend only on the host C standard library and POSIX serial and socket APIs. No Java runtime, Python interpreter, Electron-based framework, or other heavyweight dependency shall be required to launch or operate the server.]]></text>
        <traces>
          <trace target="SDD" ref="2.2"/>
        </traces>
      </hlr>

      <hlr id="HLR-039" name="Single-Threaded Event Loop Architecture">
        <text><![CDATA[The server shall multiplex all I/O using a single POSIX `select()`-based event loop with no POSIX threads. All module entry points shall be called synchronously from the event loop and shall return to it promptly. Long-blocking operations (NVM flash programming and UPDI link initialisation) shall be permitted only during server startup or in direct response to an explicit GDB command, and shall not block the event loop in any other context.]]></text>
        <traces>
          <trace target="SDD" ref="2.1"/>
          <trace target="SDD" ref="2.2"/>
          <trace target="SDD" ref="3.3.2"/>
        </traces>
      </hlr>

      <hlr id="HLR-040" name="Bounded Heap Allocation">
        <text><![CDATA[The server shall not allocate heap memory on any hot path (packet processing, UPDI reads, FSM enumeration, monitor commands). Heap allocation via `malloc` shall be used exclusively inside `elf_open()` to load the ELF symbol and string table sections; this allocation shall be bounded by the size of the ELF binary and shall occur at most once per GDB session. All heap memory shall be freed by `elf_close()` at session end or on error.]]></text>
        <traces>
          <trace target="SDD" ref="2.2"/>
          <trace target="SDD" ref="9"/>
        </traces>
      </hlr>
    </section>

    <section number="8" title="Installation and Documentation">
      <intro><![CDATA[Requirements in this section govern the Makefile installation targets and the user-facing documentation deliverables that accompany the compiled binary.]]></intro>

      <hlr id="HLR-041" name="Makefile Install and Uninstall Targets">
        <text><![CDATA[The Makefile shall provide `install` and `uninstall` targets that accept a `PREFIX` variable (defaulting to `/usr/local`). The `install` target shall copy the compiled `avrOSdb` binary to `$(PREFIX)/bin/` and the man page to `$(PREFIX)/share/man/man1/`. The `uninstall` target shall remove exactly those files. A `check-tools` target shall verify that all required host tools (`gcc`, `make`, `avr-gcc`, `avr-nm`) are available before any build step, printing a diagnostic and exiting non-zero if any required tool is absent.]]></text>
        <traces>
          <trace target="SDD" ref="2.2"/>
        </traces>
      </hlr>

      <hlr id="HLR-042" name="User Manual and Unix Man Page">
        <text><![CDATA[The project shall provide a user manual (`doc/UserManual.md`) and a Unix man page (`doc/avrOSdb.1`). The man page shall be parseable by the standard `man` utility and shall document the command synopsis, all options, operands, exit codes, and at least one usage example. The `doc/UserManual.md` shall document prerequisites, build instructions, usage, CLI options, and connection wiring for the UPDI adapter.]]></text>
        <traces>
          <trace target="SDD" ref="2.2"/>
        </traces>
      </hlr>

      <hlr id="HLR-043" name="Distribution Package Bundle">
        <text><![CDATA[The Makefile shall provide a `bundle` target that produces native distribution packages for three target platforms: a Debian binary package (`dist/avrOSdb_$(VERSION)_amd64.deb`) for Debian/Ubuntu Linux, an RPM binary package (`dist/avrOSdb-$(VERSION)-1.x86_64.rpm`) for Red Hat/Fedora Linux, and a Homebrew formula (`dist/avrOSdb.rb`) for macOS. Each package shall include the `avrOSdb` binary and the man page. All output artefacts shall be written under the `dist/` directory. A `VERSION` variable (defaulting to the value extracted from `git describe`) shall parameterise every package version string.]]></text>
        <traces>
          <trace target="SDD" ref="2.2"/>
        </traces>
      </hlr>
    </section>

    <section number="9" title="Diagnostics and Bring-up">
      <intro><![CDATA[Requirements in this section govern operator-facing diagnostic features intended for hardware bring-up and connection troubleshooting. They are activated by explicit CLI flags and never run on the GDB session hot path.]]></intro>

      <hlr id="HLR-044" name="Device-Signature Diagnostic Mode">
        <text><![CDATA[The server shall provide a one-shot diagnostic mode, activated by the `--device` command-line flag, that opens the UPDI link, reads the target SIGROW (`DEVICEID0..2`, 16-byte `SERNUM`), the SYSCFG.REVID byte, and the UPDI ASI status registers (`ASI_SYS_STATUS`, `ASI_KEY_STATUS`, `ASI_STATUSB`), prints a verbose human-readable report to `stdout`, and exits with status 0 on success or status 1 on any failure. In this mode the `<elf-file>` operand shall be optional, the GDB TCP listener shall not be bound, and no event loop shall be entered. Every failure shall produce a diagnostic on `stderr` that names the failed UPDI step (BREAK, SYNCH, SIGROW read, ASI read) so an operator can distinguish wiring, power, and fuse problems without external instrumentation. `--device` shall be mutually exclusive with `--load`; specifying both shall trigger a usage error and exit status 1.]]></text>
        <traces>
          <trace target="SDD" ref="2.1"/>
          <trace target="SDD" ref="3.1"/>
          <trace target="SDD" ref="4.1"/>
        </traces>
      </hlr>
    </section>

    <section number="10" title="Hardware Integration Testing">
      <intro><![CDATA[Requirements in this section govern the on-target hardware integration test harness (`tests/hw/hw_test.c`). These tests link against the project's UPDI driver and exercise live AVR-Dx silicon over a serial UPDI link. They are manual-only and are explicitly excluded from `make test` so that unit-test execution remains hardware-independent and CI-safe.]]></intro>

      <hlr id="HLR-046" name="Non-FLASH NVM Programming">
        <text><![CDATA[The application shall program the target's EEPROM, USERROW, FUSES, and (subject to HLR-047) LOCK memories from ELF `PT_LOAD` segments whose virtual addresses fall in the corresponding UPDI unified-address windows of the runtime-selected device (see HLR-048). For the AVR-DA reference target the windows are EEPROM `0x814000`–`0x8143FF` (512 B), USERROW `0x810080`–`0x8100FF` (32 B), FUSES `0x820000`–`0x82001F` (32 B), and LOCK `0x820040`–`0x820043` (4 B). The AVR-DB family shares the AVR-DA layout. The AVR-DD family widens USERROW to 128 B (`0x810080`–`0x8100FF` window, 128-byte page) and shrinks EEPROM to 256 B (`0x814000`–`0x8140FF`). The AVR-DU and AVR-SD families relocate USERROW to `0x811200`–`0x8113FF` (512 B) and likewise expose 256 B of EEPROM at `0x814000`–`0x8140FF`. FUSES and LOCK occupy the same windows on all five families. Each NVM kind shall use the byte-granular NVMCTRL command `EEERWR` (`0x13`) — erase-and-write per byte — and shall poll `NVMSTATUS.EEBUSY` (bit 1) clear between bytes. Segments whose virtual addresses fall in the SIGROW window (`0x811080`–`0x8110FF` on AVR-DA/DB/DD, `0x811080`–`0x8113FF` on AVR-DU/SD) shall be silently skipped because SIGROW is factory-programmed and read-only. Segments outside every programmable window for the selected device shall be rejected with a diagnostic. After all non-FLASH writes complete, the application shall re-enter OCD mode (`updi_enter_debug`) so the subsequent RSP session has a halted, OCD-addressable CPU. The AVR-EA and AVR-EB families are out of scope for this requirement because they use the incompatible NVMCTRLv3 controller (different command opcodes — FLPERW `0x05`, EEPERW `0x15`).]]></text>
        <traces>
          <trace target="SDD" ref="3.2.2"/>
          <trace target="SDD" ref="4.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-048" name="Multi-Family Runtime Device Dispatch">
        <text><![CDATA[The application shall maintain a compile-time table of supported AVR Dx/Du/Sd device families (AVR-DA, AVR-DB, AVR-DD, AVR-DU, AVR-SD) where each entry records the family name, the base address and size of every programmable NVM window (EEPROM, USERROW, FUSES, LOCK), the SIGROW base address, and a boolean flag indicating whether the family has been hardware-validated on real silicon. On startup, after `updi_open()` and before any NVM operation, the application shall select an active device descriptor by one of two paths: (a) if the user supplied `--force-device=<family>` on the command line, the named entry shall be selected by case-insensitive match and a warning shall be emitted on `stderr` when the entry's `hw_tested` flag is false; (b) otherwise the application shall read the 3-byte SIGROW DEVICEID0..2 triplet at the AVR-DA/DB/DD canonical address (`0x1100`) and look it up against the table's autodetect signatures, failing with a diagnostic that names all five supported families and the `--force-device` option when DEVICEID0 is not `0x1E` or the signature is unrecognised. The AVR-DU and AVR-SD families place SIGROW at `0x1080` rather than `0x1100`, so they cannot be auto-detected from the AVR-DA probe address and shall always be selected via `--force-device`. Once selected, all subsequent NVM read/write call sites and the ELF-VMA window classifier in `load_segments()` shall consult the runtime-selected descriptor for window bases and sizes; no hard-coded address constant shall be used for the per-family fields. AVR-DA is the only family currently validated against live silicon; AVR-DB/DD/DU/SD entries are declared from the published datasheet memory maps and are marked untested-on-silicon. The diagnostic device mode (`--device`) shall tolerate unknown silicon — when no `--force-device` override is supplied it shall not invoke the autodetect probe so the report can still display raw SIGROW bytes for triage.]]></text>
        <traces>
          <trace target="SDD" ref="3.2.2"/>
          <trace target="SDD" ref="4.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-047" name="Lockbit Programming Safety Interlock">
        <text><![CDATA[Writing the LOCK byte (UPDI window `0x820040`–`0x820043`) shall be subject to two mandatory safety interlocks. First, the application shall refuse to program the LOCK window unless `--erase` was also supplied on the command line, because the silicon requires a chip-erase to clear `LOCKSTATUS` before lockbits can be re-programmed and partial writes brick the part. Second, the application shall refuse any 4-byte LOCK payload other than the unlock pattern `0x5CC5C55C` (little-endian) unless the operator additionally supplies `--allow-lock-updi`, because every other 4-byte value risks setting the `UPDIDIS` lockbit which permanently disables the UPDI debug interface. When `LOCKSTATUS` (`ASI_SYS_STATUS` bit 1) is asserted at the time of the LOCK write, the application shall abort with the new error code `UPDI_ERR_LOCKED` (-3). With both interlocks satisfied (`--erase` present, payload is the unlock pattern OR `--allow-lock-updi` present), the application shall write the LOCK byte(s) using the same EEPROM `EEERWR` sequence as the other non-FLASH windows.]]></text>
        <traces>
          <trace target="SDD" ref="3.2.2"/>
          <trace target="SDD" ref="4.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-045" name="On-Target Hardware Integration Test Harness">
        <text><![CDATA[The project shall provide a standalone, manual-only on-target hardware integration test harness, separate from the unit-test suite, that validates the UPDI driver, device-info report, SRAM and FLASH memory access, and the GDB RSP server against live AVR-Dx silicon. The harness shall be organised into four opt-in groups: Group A (read-only link and device-info probes), Group B (non-destructive SRAM round-trips with size and address-mode variants), Group C (destructive FLASH erase/write/verify, gated by an explicit confirmation variable), and Group D (RSP server spawn-and-probe over TCP, gated by an opt-in variable). The harness shall be configurable via environment variables and command-line flags (serial port, expected device ID, SRAM probe address, FLASH page address, RSP TCP port, verbosity), and shall accept an explicit `--addr-24bit` switch to validate 24-bit UPDI addressing on parts whose FLASH lies above 64 KiB. The Makefile shall expose dedicated targets `hw-test` (Groups A+B), `hw-test-nvm` (Groups A+B+C), `hw-test-rsp` (Groups A+B+D), and `hw-test-all` (all groups); none of these targets shall ever be invoked by `make test`.]]></text>
        <traces>
          <trace target="SDD" ref="2.2"/>
          <trace target="SDD" ref="4.1"/>
          <trace target="SDD" ref="4.2.1"/>
        </traces>
      </hlr>
    </section>

    <section number="11" title="CI-Grade Loader and Link Diagnostics">
      <intro><![CDATA[Requirements in this section govern the Phase 9 features that turn the loader into a CI-trustable build/flash step and elevate the `--device` mode into a first-line link-health probe. They were tracked as GitHub issue #28 and implement the design narrative from the Phase 9 overview paragraph in the SDD.]]></intro>

      <hlr id="HLR-049" name="Read-Back Verify After Load">
        <text><![CDATA[After every successful `--load` write, the application shall re-read each programmed NVM range through the existing UPDI read path and compare the read-back bytes against the ELF payload. Verify shall cover the FLASH, EEPROM, USERROW, and FUSES windows; segments classified as SIGROW (read-only) or LOCK (read-once / write-only-after-erase) shall be skipped. On mismatch the application shall print one diagnostic line per failing page to `stderr` carrying the window name, page-aligned VMA, expected and actual IEEE 802.3 CRC32 values; shall not call `updi_enter_debug()`; shall not call `rsp_listen()`; and shall exit with status `UPDI_EXIT_VERIFY_FAIL` (`2`, distinct from the existing exit code `1` reserved for I/O / configuration failures). Verify shall be enabled by default for `--load` and `--prog`; the operator may disable it with the new `--no-verify` flag, in which case the application shall behave exactly as before Phase 9.]]></text>
        <traces>
          <trace target="SDD" ref="2.1"/>
          <trace target="SDD" ref="3.2.2"/>
          <trace target="SDD" ref="4.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-050" name="Program-and-Exit Mode">
        <text><![CDATA[The application shall provide a new operating mode, activated by the `--prog` command-line flag and mutually exclusive with both `--device` and `--load`, that programs the supplied ELF into NVM, verifies the write per HLR-049, and exits — no GDB TCP listener shall be bound, no `updi_enter_debug()` shall be issued, and the target shall be left running with the UPDI link closed cleanly. On start-up the mode shall emit a single `baud=<N>` line on `stdout` reporting the negotiated UART rate. During programming the mode shall render an in-place single-line progress indicator (`[####....] %% page N/M phase window`) when `stdout` is a TTY (detected by `isatty(STDOUT_FILENO)`), and shall degrade to one line per page (or per phase transition) when `stdout` is piped or redirected. On verify success the mode shall print `verify: OK` and exit with status 0; on verify failure it shall exit with status `UPDI_EXIT_VERIFY_FAIL` (`2`); on any I/O or NVM failure it shall exit with status 1. Intended use is `avrOSdb --prog <serial-device> <elf-file>` from a CI script or a Makefile `flash:` target.]]></text>
        <traces>
          <trace target="SDD" ref="2.1"/>
          <trace target="SDD" ref="3.2.2"/>
          <trace target="SDD" ref="4.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-051" name="Fuses Pretty-Printer in Device Mode">
        <text><![CDATA[The `--device` diagnostic mode shall, after printing the SIGROW / REVID / ASI status report defined by HLR-044, read the target's full FUSES window and the 4-byte LOCK window through the UPDI NVM read path and emit a human-readable decode to `stdout`. The decode shall consist of a header line (`Fuses (<family>, <N> bytes):`), one indented line per known fuse byte showing the offset, fuse name, and raw hex value, followed by indented sub-lines naming each documented bit-field (e.g. `WDTCFG.PERIOD = 8K`, `BODCFG.SLEEP = SAMPLED`, `OSCCFG.CLKSEL = OSCHF`, `SYSCFG0.CRCSRC`, `SYSCFG1.SUT`, `CODESIZE`, `BOOTSIZE`) using human-readable enum names rather than raw hex. Fuse bytes for which no bit-field decoder is registered shall be shown as `(reserved) raw=0x..`. The lock byte shall be shown on a final line `Lock: 0xXXXXXXXX (UNLOCKED|LOCKED)`, where `UNLOCKED` is the 4-byte little-endian value `0x5CC5C55C` and any other value is reported as `LOCKED`. The decode tables shall live alongside `g_device_table[]` in `src/updi.c` so they are selected by the same `updi_select_device()` runtime path. Phase 9 ships the AVR-DA / AVR-DB family table; other families fall back to raw hex with no per-bit decoding.]]></text>
        <traces>
          <trace target="SDD" ref="2.1"/>
          <trace target="SDD" ref="3.2.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-052" name="Auto-Baud Link-Quality Probe in Device Mode">
        <text><![CDATA[The `--device` diagnostic mode shall, before opening the UPDI session, walk a fixed baud-rate ladder (`230400, 200000, 150000, 115200, 57600, 38400, 19200`) and at each rung open the UART at the candidate rate, issue a small fixed UPDI transaction sequence (`LDCS ASI_STATUSA`, repeated for a small sample window — default 8 samples), and record the byte error rate. Each rung result shall be reported to `stdout` as one line `  baud=<N> errors=<K>/<total>`. The application shall select the highest-rate rung that observed zero errors over the sample window; if no rung observes zero errors the probe shall report failure and the application shall fall back to the user-supplied or default baud rate. The probe shall be read-only — no NVM, no fuses, no system-reset paths, no `updi_enter_debug()` — and shall close every interim UART file descriptor it opens. The operator may disable the probe with the new `--no-autobaud` flag, in which case `--device` shall behave exactly as before Phase 9 and use `cfg.baud_rate` directly. The probe is invoked from `--device` mode only; `--load` and `--prog` continue to use the explicit `--baud` setting.]]></text>
        <traces>
          <trace target="SDD" ref="2.1"/>
          <trace target="SDD" ref="3.2.1"/>
        </traces>
      </hlr>
    </section>

    <section number="12" title="GDB Protocol Completion (avarice Feature Parity)">
      <intro><![CDATA[Requirements in this section govern the Phase 10 features that bring `avrOSdb`'s GDB Remote Serial Protocol surface up to functional parity with the legacy `avarice` JTAG/dW stub: equivalent monitor verbs, equivalent `(gdb) load`, equivalent watchpoint and software-breakpoint experience, and equivalent extended-remote lifecycle. Wire-level drop-in compatibility with `avarice`-specific scripts is an explicit non-goal. Tracked as GitHub issue #31; implements the design narrative from the Phase 10 overview paragraph in the SDD.]]></intro>

      <hlr id="HLR-053" name="Flash Programming via GDB load">
        <text><![CDATA[The application shall implement the GDB `vFlashErase:addr,length`, `vFlashWrite:addr:XX...`, and `vFlashDone` packets so that the standard `(gdb) load` command programs the AVR FLASH directly from the running GDB session without exiting to an external programmer. On `vFlashErase` the server shall reject any address range that falls outside the FLASH window of the runtime-selected device (HLR-048) with reply `E22`; ranges that fall in EEPROM, USERROW, FUSES, LOCK, or SIGROW shall be refused for the same reason because `gdb load` carries no per-window verb to disambiguate. Erase shall be performed page-aligned via `updi_nvm_erase_flash_page()`; `vFlashWrite` shall accumulate the binary payload into the corresponding page buffer and program it with `updi_nvm_write_flash()`; `vFlashDone` shall flush any pending page, call `updi_enter_debug()` to leave the CPU halted at reset, and reply `OK`. The session's hardware-breakpoint shadow (`RspContext.hw_bp_addr[]`) and SW-breakpoint shadow (HLR-054) shall be cleared on `vFlashDone` because their underlying instructions may have been overwritten by the load. While a `vFlash*` transaction is in progress the server shall not service `g`/`G`/`m`/`M`/`c`/`s` packets; if such a packet arrives between `vFlashErase` and `vFlashDone` the server shall reply `E22` and discard the in-progress page buffers.]]></text>
        <traces>
          <trace target="SDD" ref="5.3.1"/>
          <trace target="SDD" ref="4.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-054" name="True Software Breakpoints via Flash BREAK Opcode">
        <text><![CDATA[The application shall support an unbounded number of simultaneous software breakpoints by patching the AVR `BREAK` opcode (`0x9598`, little-endian) into the FLASH word at the target address on `Z0` and restoring the original 2-byte instruction on `z0`. A per-session shadow map shall record `(gdb_addr, flash_byte_addr, original_opcode_lo, original_opcode_hi, page_aligned_base)` for every active SW breakpoint so removal is exact. The patch sequence shall preserve live CPU state across the unavoidable OCD → NVMPROG → OCD transition: before NVMPROG entry the server shall snapshot R0–R31, SREG, SP, and PC via the OCD register file; after re-entering OCD it shall restore them with the existing `updi_ocd_write_*` primitives. SW breakpoints in the SIGROW, FUSES, USERROW, EEPROM, or LOCK windows shall be refused with `E22` because the AVR `BREAK` opcode is only meaningful when fetched as an instruction. The operator may opt out of true SW breakpoints with `monitor avros bp-mode hw-only` (HLR-055), in which case `Z0` shall continue to alias to the two HW comparators (the Phase 1–8 behaviour mandated by HLR-016); the mode shall persist across detach/reattach for the lifetime of the server process. On `vFlashDone` (HLR-053) and on `monitor reset` / `monitor chip-erase` (HLR-055) every SW breakpoint shadow entry shall be discarded because the underlying instruction is no longer present in FLASH.]]></text>
        <traces>
          <trace target="SDD" ref="5.3.1"/>
          <trace target="SDD" ref="4.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-055" name="avarice-Compatible Monitor Commands">
        <text><![CDATA[The application shall expose a top-level set of `monitor` verbs that mirror the `avarice` command surface, so that GDB scripts written for `avarice` work unchanged: `monitor reset` (issue `updi_reset()` and leave the CPU halted at the reset vector, then invalidate the FSM thread cache so the next `qfThreadInfo` reflects post-reset state), `monitor halt` (call `updi_halt()` and reply with the same `T05thread:<id>;` packet as a step boundary), `monitor go` (call `updi_run()` and reply `OK` — the subsequent stop-reason packet is emitted when the CPU next halts), `monitor erase` and `monitor chip-erase` (issue `updi_chip_erase()`, but only when the server was launched with the new `--allow-erase` flag — without that flag the verb shall reply with an O-packet diagnostic and `E22`), `monitor version` (one O-packet line carrying the server version, git short SHA, build date, and the active device family name from HLR-048), `monitor bp-mode <hw-only|sw>` (configure the breakpoint installation policy per HLR-054), and `monitor help` (one O-packet per recognised verb summarising syntax and side-effects, terminating with `OK`). The dispatcher in `src/monitor.c` shall continue to route the `avros ` prefix to the existing avrOS-specific handlers (`events`, `queues`); unknown verbs shall reply with the existing usage hint plus `E22`. None of the new verbs shall alter the FSM thread mapping path other than via the explicit cache invalidation noted above.]]></text>
        <traces>
          <trace target="SDD" ref="8.1"/>
          <trace target="SDD" ref="5.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-056" name="Hardware Data Watchpoints">
        <text><![CDATA[The application shall implement GDB watchpoint packets `Z2`/`z2` (write), `Z3`/`z3` (read), and `Z4`/`z4` (access) by programming the AVR-Dx OCD data-address comparator pair `DABP0`/`DABP1`. Up to two simultaneous watchpoints shall be supported, mirroring the two-comparator hardware limit; a third unique-address insert shall reply `E08`. A per-session shadow array `RspContext.hw_wp[]` shall record `(addr, length, kind)` for each programmed comparator so `z*` is exact and detach restores silicon to the unarmed state. The new UPDI primitives `updi_ocd_set_data_bp(slot, addr, length, kind)` and `updi_ocd_clear_data_bp(slot)` shall live alongside the existing `updi_ocd_set_hw_bp()` / `_clear_hw_bp()` so the OCD register accesses are kept in one module. On a watchpoint trigger the stop-reply packet shall carry the standard GDB stop-key `watch:<addr>;` (write), `rwatch:<addr>;` (read), or `awatch:<addr>;` (access) appended to the `T05thread:<id>;` payload so the IDE highlights the responsible address; the address shall be the GDB-side data-space address (UPDI byte address ORed with `0x800000`).]]></text>
        <traces>
          <trace target="SDD" ref="5.3.1"/>
          <trace target="SDD" ref="4.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-058" name="Extended-Remote Lifecycle Packets">
        <text><![CDATA[The application shall support the `target extended-remote` connection mode by implementing the `vRun;<arg>;<arg>...` packet (treat the first argument as a path to a new `.elf` file, swap it into the existing `AppConfig`, re-run ELF parsing, re-build the FSM context, and reply with a `T05` stop at the new reset vector), the `vAttach;<pid>` packet (reply `OK` followed by the standard stop-reply; `pid` is informational only on a single-target stub), and the `vKill;<pid>` packet (call `updi_run()` to release the target, close the GDB client fd, and return to the listen state without exiting the server process). Existing `D` (detach) and `k` (kill) handlers shall remain operational; `k` shall continue to mean process-exit so that scripts that hard-stop the server keep working. The `qSupported` feature string shall be extended with `multiprocess+;vRun+;vAttach+;vKill+` and the `multiprocess-` entry removed so GDB treats the connection as multi-target capable.]]></text>
        <traces>
          <trace target="SDD" ref="5.1"/>
          <trace target="SDD" ref="5.3.1"/>
        </traces>
      </hlr>

      <hlr id="HLR-059" name="Protocol Cleanup — qC, qOffsets, T, R">
        <text><![CDATA[The application shall stop replying with the empty packet for the standard small queries that `avarice` answers but Phase 1–8 leaves unimplemented, because some IDE configurations promote the resulting `unsupported feature` warnings to fatal errors. Specifically: `qC` shall return `QC<tid>` where `<tid>` is the currently selected `c`-thread (or `0` when no thread is selected); `qOffsets` shall return `Text=0;Data=0;Bss=0` because the AVR reset vector is fixed at address zero and no position-independent loading is performed; `T<tid>` (is-thread-alive) shall return `OK` for any `<tid>` present in the current `FsmContext.threads[]` array and `E01` otherwise; and `R<XX>` (RCmd restart) shall behave as `monitor reset` followed by `c` and reply with the resulting stop packet. None of these handlers shall alter execution state beyond the explicit semantics above.]]></text>
        <traces>
          <trace target="SDD" ref="5.3.1"/>
        </traces>
      </hlr>
    </section>

  </hlrs>

  <!-- Low-Level Requirements (see tools/Developers_Guide.md §6). -->
  <llrs>
    <function number="1" title="Introduction" name="intro">
      <intro><![CDATA[Each LLR ID follows the pattern `LLR-XXX-NN`, where `XXX` is a module code and `NN` is a zero-padded two-digit sequence number. Module codes: `MAIN` (`src/main.c`), `UPDI` (`src/updi.c`), `RSP` (`src/gdb_rsp.c`), `ELF` (`src/elf_parser.c`), `FSM` (`src/fsm_mapper.c`), `MON` (`src/monitor.c`), `INST` (`Makefile`, `doc/avrOSdb.1`, `doc/UserManual.md`), `HWTEST` (`tests/hw/hw_test.c`). IDs are permanent contracts and may not be renumbered or reused once allocated.]]></intro>
    </function>

    <function number="2" title="src/main.c — Entry Point and Event Loop" name="main" source="src/main.c">
      <intro><![CDATA[Requirements for `main()`, `parse_args()`, and `event_loop()`. These functions own the application lifecycle from argument parsing through resource cleanup.]]></intro>

      <llr id="LLR-MAIN-01">
        <text><![CDATA[`parse_args()` shall populate an `AppConfig` struct from `argc`/`argv`. If any unrecognised argument is encountered, it shall print a usage message to `stderr` and call `exit(1)`.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-001" name="CLI Argument Parsing"/>
        </traces>
      </llr>

      <llr id="LLR-MAIN-02">
        <text><![CDATA[When not supplied on the command line, `parse_args()` shall apply the following defaults to the `AppConfig` fields: `gdb_port` = 1234, `baud_rate` = 115200, `load_flash` = false. The `<serial-device>` and `<elf-file>` positional arguments are mandatory; absence of either shall trigger the usage error path.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-001" name="CLI Argument Parsing"/>
        </traces>
      </llr>

      <llr id="LLR-MAIN-03">
        <text><![CDATA[`main()` shall call `updi_open()` and validate the returned file descriptor before calling `rsp_listen()`. A failure from either shall cause `main()` to release any already-open resources and return exit code 1.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-002" name="Serial Device Initialisation"/>
          <trace target="HLR" ref="HLR-003" name="GDB Listener Startup"/>
        </traces>
      </llr>

      <llr id="LLR-MAIN-04">
        <text><![CDATA[When the `--load` flag is set, `main()` shall iterate over all `PT_LOAD` ELF program headers and call `updi_nvm_write_flash()` for each segment whose `p_filesz > 0` and whose virtual address lies below the SRAM base (i.e. flash-resident segments only). Segments with `p_vaddr >= ctx.sram_base` shall be skipped. All qualifying segments shall be written before entering `event_loop()`. A flash write failure shall cause `main()` to print an error to `stderr` and return exit code 1.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-004" name="ELF Flash Load Option"/>
        </traces>
      </llr>

      <llr id="LLR-MAIN-05">
        <text><![CDATA[`event_loop()` shall multiplex `listen_fd`, `gdb_fd`, and `updi_fd` using a single POSIX `select()` call with no timeout. On each iteration it shall: accept a GDB client on `listen_fd` if `gdb_fd` is -1; forward console bytes from `updi_fd` to `stdout` via `updi_console_poll()`; dispatch RSP packets from `gdb_fd` via `rsp_recv_packet()` and `rsp_dispatch()`. No POSIX threads shall be created.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-003" name="GDB Listener Startup"/>
          <trace target="HLR" ref="HLR-039" name="Single-Threaded Event Loop Architecture"/>
        </traces>
      </llr>

      <llr id="LLR-MAIN-06">
        <text><![CDATA[`main()` shall register a `SIGINT`/`SIGTERM` handler that sets the global `volatile sig_atomic_t g_quit` flag to 1. `event_loop()` shall test `g_quit` at the top of each loop iteration and return immediately when the flag is set, without closing any file descriptors itself.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-035" name="Graceful Shutdown and Resource Release"/>
          <trace target="HLR" ref="HLR-039" name="Single-Threaded Event Loop Architecture"/>
        </traces>
      </llr>

      <llr id="LLR-MAIN-07">
        <text><![CDATA[On all exit paths, `main()` shall release resources in this exact order: (1) `rsp_close(cfg.gdb_fd)` if `gdb_fd >= 0`, (2) `rsp_close(cfg.listen_fd)`, (3) `elf_close(&ctx)`, (4) `updi_close(cfg.updi_fd)`. The process shall exit with code 0 on normal termination and code 1 on any fatal initialisation or flash-write failure.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-035" name="Graceful Shutdown and Resource Release"/>
        </traces>
      </llr>

      <llr id="LLR-MAIN-08">
        <text><![CDATA[`parse_args()` shall recognise the `--device` long option, set `cfg->device_info = true`, and accept the absence of the `<elf-file>` operand without error. Specifying both `--device` and `--load` on the same command line shall cause `parse_args()` to print `"error: --device is mutually exclusive with --load"` to `stderr`, print the usage synopsis, and call `exit(1)`.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-044" name="Device-Signature Diagnostic Mode"/>
        </traces>
      </llr>

      <llr id="LLR-MAIN-09">
        <text><![CDATA[When `cfg.device_info` is true, `main()` shall: (1) call `updi_open()`; (2) on success call `updi_read_device_info()`; (3) on success format and print to `stdout` a multi-line report containing `Serial device:`, `Baud rate:`, `Signature:` (3 hex bytes), `Family:` (looked up via a static `device_family[]` table indexed by signature \u2014 unknown signatures shall print `unknown device`), `Revision:`, `Serial:` (10 hex bytes), and `UPDI status: SYS_STATUS=0x.. KEY_STATUS=0x.. STATUSB=0x..`; (4) call `updi_close()` and return. The function shall **not** call `rsp_listen()`, `elf_find_avros_tables()`, `fsm_build_thread_list()`, or `event_loop()`. On any UPDI failure the function shall print a verbose diagnostic to `stderr` that names the failed step (e.g. `"updi-open"`, `"sigrow"`, `"sys-status"`) and return exit code 1.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-044" name="Device-Signature Diagnostic Mode"/>
        </traces>
      </llr>

      <llr id="LLR-MAIN-10">
        <text><![CDATA[Before entering `event_loop()`, `main()` shall call `updi_enter_debug(cfg.updi_fd)`, after `updi_open()` and any optional `updi_nvm_write_flash()` (`--load`) steps and before `rsp_listen()`. A non-zero return shall be treated as a fatal initialisation failure and shall trigger the LLR-MAIN-07 teardown with exit code 1. This arms AVR-Dx OCD mode for the lifetime of the server so that all RSP `g`/`G`/`P`/`s`/`c`/`Z0`/`Z1` operations have a halted, OCD-addressable CPU register file available on first use.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-010" name="Execution Control"/>
        </traces>
      </llr>

      <llr id="LLR-MAIN-11">
        <text><![CDATA[When `cfg.load_flash` is true, `load_segments()` shall walk every `PT_LOAD` segment of the open ELF context and classify it by its UPDI virtual address (after folding ELF VMAs below `UPDI_FLASH_BASE` into the FLASH window via `vaddr | 0x800000`). SRAM-resident segments shall be skipped silently. The classifier shall obtain its EEPROM, USERROW, FUSES, LOCK, and SIGROW window bases and sizes from the runtime-selected device descriptor (`updi_get_device()`, see LLR-UPDI-27); no per-family address shall be hard-coded in the classifier. SIGROW segments shall be skipped with a `"SIGROW segment @0x%06x ignored (read-only)"` notice on `stdout`. FLASH segments (everything below the selected device's USERROW base in the UPDI space) shall be padded with `0xFF` to a 512-byte page boundary and dispatched to `updi_nvm_write_flash()` — the established Phase 2/3 path is unchanged. EEPROM, USERROW, and FUSES segments shall be dispatched verbatim (no padding) to `updi_nvm_write_eeprom()`, `updi_nvm_write_userrow()`, and `updi_nvm_write_fuses()` respectively. LOCK segments (`[dev->lock_base, dev->lock_base + dev->lock_size)`) shall additionally require `cfg.erase_chip` to be true (else the function shall return -1 with a diagnostic naming the missing `--erase` flag) and shall be dispatched to `updi_nvm_write_lockbits(..., cfg.allow_lock_updi)`. Any segment whose UPDI address is in none of these windows shall cause `load_segments()` to return -1 with `"error: segment vaddr 0x%06x not in any programmable NVM window"` on `stderr`. The `--allow-lock-updi` CLI flag shall set `cfg.allow_lock_updi` and is forwarded to `updi_nvm_write_lockbits()` unchanged.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-004" name="ELF Load Option"/>
          <trace target="HLR" ref="HLR-046" name="Non-FLASH NVM Programming"/>
          <trace target="HLR" ref="HLR-047" name="Lockbit Programming Safety Interlock"/>
          <trace target="HLR" ref="HLR-048" name="Multi-Family Runtime Device Dispatch"/>
        </traces>
      </llr>

      <llr id="LLR-MAIN-12">
        <text><![CDATA[`parse_args()` shall recognise the option `--force-device=<family>` (case-insensitive value) and store the value in `cfg.force_device` (default `NULL`). The `--help` / `usage()` output shall list the option along with the five supported family names (`AVR-DA`, `AVR-DB`, `AVR-DD`, `AVR-DU`, `AVR-SD`). In normal startup (`app_main()`), after `updi_open()` succeeds, the application shall call `updi_select_device(cfg.updi_fd, cfg.force_device)` and shall treat a non-zero return as a fatal initialisation failure, exiting via the LLR-MAIN-07 teardown with exit code 1. In diagnostic mode (`run_device_mode()`, `--device`), `updi_select_device()` shall be called only when `cfg.force_device` is non-NULL and non-empty, so that unknown silicon can still be probed and its raw SIGROW bytes reported.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-048" name="Multi-Family Runtime Device Dispatch"/>
        </traces>
      </llr>

      <llr id="LLR-MAIN-13">
        <text><![CDATA[`app_main()` shall select the per-family device map between `elf_open()` and the first NVM access by calling `updi_select_device(cfg.updi_fd, cfg.force_device)` exactly once, where `cfg.force_device` is either the user-supplied `--force-device=<family>` string or `NULL`. When `cfg.force_device` is `NULL`, `updi_select_device()` shall determine the active family by SIGROW autodetect (LLR-UPDI-27). The ELF `.note.gnu.avr.deviceinfo` part-name shall NOT be used as a selection input; using it would defeat the mismatch check below by comparing the ELF family against the family the application just told the UPDI layer to be. After `updi_select_device()` returns 0, when `elf_ctx.device_name` is a recognised Microchip part-name (`updi_family_from_partname()` returns non-NULL) AND the user did NOT supply `--force-device`, the application shall verify that `updi_get_device()->family` matches the ELF-derived family string; on mismatch the application shall print `"error: ELF was built for <partname> (family <X>) but target reports <Y> (use --force-device=<Y> to override)"` to `stderr`, exit via the LLR-MAIN-07 teardown with code 1, and shall not call `updi_enter_debug()`, `load_segments()`, or `rsp_listen()`. The `--force-device=<family>` option is the documented escape hatch when the user knowingly wants to debug an ELF against a different silicon family (e.g. a generic AVR-DA build run on AVR-DB hardware) or when SIGROW autodetect cannot identify the part (AVR-DU/AVR-SD relocate SIGROW to a different base).]]></text>
        <traces>
          <trace target="HLR" ref="HLR-046" name="Non-FLASH NVM Programming"/>
          <trace target="HLR" ref="HLR-048" name="Multi-Family Runtime Device Dispatch"/>
        </traces>
      </llr>

      <llr id="LLR-MAIN-14">
        <text><![CDATA[`parse_args()` shall recognise three new long-form flags and store them as bool fields on `AppConfig`: `--prog` sets `cfg.prog_mode = true`, `--no-verify` sets `cfg.no_verify = true`, and `--no-autobaud` sets `cfg.no_autobaud = true`. The usage banner shall document all three. The flag combination shall be validated: `--prog` is mutually exclusive with both `--device` and `--load` (specifying any pair shall print a usage error to `stderr` and call `exit(1)`); `--no-verify` is a no-op when `--device` is also given (no NVM writes happen in device mode); `--no-autobaud` is meaningful only in `--device` mode. Defaults are `false` for all three flags, preserving pre-Phase-9 behaviour for callers that supply none of them.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-049" name="Read-Back Verify After Load"/>
          <trace target="HLR" ref="HLR-050" name="Program-and-Exit Mode"/>
          <trace target="HLR" ref="HLR-052" name="Auto-Baud Link-Quality Probe in Device Mode"/>
        </traces>
      </llr>

      <llr id="LLR-MAIN-15">
        <text><![CDATA[`verify_segments(cfg, elf_ctx)` shall mirror the segment classification logic of `load_segments()` (FLASH / EEPROM / USERROW / FUSES / LOCK / SIGROW) but shall use `updi_nvm_read()` to read each programmed range back from the target in `CHUNK = 256`-byte windows and compare it against the ELF payload. Segments classified as SIGROW (read-only) and LOCK (read-once / lockbits become read-only post-write) shall be skipped. On a window mismatch the function shall compute `updi_crc32()` over the expected and actual buffers and print one `stderr` line per failing page reporting the window name, page-aligned VMA, and both CRC32 values. The function shall call `progress_render()` once per page and `progress_finish()` once per segment to drive the progress indicator. It shall return 0 when every byte of every verifiable segment matches and -1 otherwise. The caller is responsible for translating -1 into the process exit code `UPDI_EXIT_VERIFY_FAIL` (`2`).]]></text>
        <traces>
          <trace target="HLR" ref="HLR-049" name="Read-Back Verify After Load"/>
          <trace target="HLR" ref="HLR-050" name="Program-and-Exit Mode"/>
        </traces>
      </llr>

      <llr id="LLR-MAIN-16">
        <text><![CDATA[`run_prog_mode(cfg)` shall implement the `--prog` operating mode. It shall: (1) open the ELF binary via `elf_open()`; (2) open the UPDI link via `updi_open(cfg->serial_device, cfg->baud_rate)` and call `updi_select_device(cfg->updi_fd, cfg->force_device)`; (3) print one `baud=<N>` line to `stdout` reporting the negotiated UART rate; (4) when `cfg->erase` is true, call `updi_chip_erase(cfg->updi_fd)`; (5) call `load_segments(cfg, &elf_ctx)` to program FLASH/EEPROM/USERROW/FUSES/LOCK; (6) unless `cfg->no_verify` is set, call `verify_segments(cfg, &elf_ctx)`; (7) on verify success print `verify: OK` to `stdout`; (8) close UPDI and ELF resources and return. The function shall NOT call `updi_enter_debug()` and shall NOT call `rsp_listen()` — the target is left running. Return-value mapping: 0 on success, 1 on any I/O / NVM failure, and `UPDI_EXIT_VERIFY_FAIL` (`2`) when verify reports a mismatch. The main dispatcher shall route to this function immediately after the device-info short-circuit when `cfg->prog_mode` is true.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-050" name="Program-and-Exit Mode"/>
          <trace target="HLR" ref="HLR-049" name="Read-Back Verify After Load"/>
        </traces>
      </llr>

      <llr id="LLR-MAIN-17">
        <text><![CDATA[The progress indicator shall be implemented by two static helpers `progress_render(phase, window, page, total_pages, percent)` and `progress_finish(void)`. When `isatty(STDOUT_FILENO)` is true `progress_render()` shall emit an in-place ANSI-friendly single-line bar of the form `[####....] PPP%% page N/M PHASE WINDOW` followed by carriage return (no newline) and `progress_finish()` shall emit a final newline to terminate the line. When `stdout` is not a TTY `progress_render()` shall emit one full newline-terminated line per call and `progress_finish()` shall be a no-op. Both helpers are called from `load_segments()`, `verify_segments()`, and `run_prog_mode()`. Additionally, `run_device_mode()` shall, after the existing SIGROW / REVID / ASI status print and unless `cfg->no_autobaud` is true, call `updi_probe_baud(cfg->serial_device, 8, autobaud_report_cb, NULL)` BEFORE opening the UPDI link and pass the selected rate to `updi_open()`; and shall, after the existing report, read the full FUSES window and the 4-byte LOCK window via `updi_nvm_read()` and emit the decode via `updi_format_fuses()` into a 2 KiB stack buffer.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-050" name="Program-and-Exit Mode"/>
          <trace target="HLR" ref="HLR-051" name="Fuses Pretty-Printer in Device Mode"/>
          <trace target="HLR" ref="HLR-052" name="Auto-Baud Link-Quality Probe in Device Mode"/>
        </traces>
      </llr>
    </function>

    <function number="3" title="src/updi.c — UPDI Physical Layer" name="updi" source="src/updi.c">
      <intro><![CDATA[Requirements for all public functions in `src/updi.c`: link initialisation, memory access, execution control, NVM programming, and console bridging.]]></intro>

      <llr id="LLR-UPDI-01">
        <text><![CDATA[`updi_open()` shall open the UART device and configure it for 8E2 framing (8 data bits, even parity, 2 stop bits) in raw half-duplex mode using `termios`. `cfmakeraw()`, `CS8`, `CSTOPB`, `PARENB` (with `PARODD` cleared), and explicit baud setting via `cfsetispeed()`/`cfsetospeed()` shall be applied. If `tcsetattr()` returns `EINVAL` when setting `PARENB` (Linux PTY slaves strip parity from virtual terminals), the function shall retry with `PARENB` cleared so the link still works against PTY-backed test harnesses. The function shall return the open file descriptor on success or -1 on failure.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-006" name="UPDI Hardware Connection"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-02">
        <text><![CDATA[`updi_open()` shall execute up to three cold-start attempts. Attempt 0 (fast path) shall write a single 0x00 wake byte at the configured session baud rate, followed by `tcdrain()` and `tcflush(fd, TCIFLUSH)` to discard the half-duplex echo. Attempts 1 and 2 (slow path) shall instead generate a multi-millisecond line-low pulse by temporarily switching the kernel baud rate to 300 baud with `CSTOPB` cleared (1 stop bit) via `tcsetattr(TCSADRAIN)`, then writing two 0x00 bytes each followed immediately by `tcdrain()` to guarantee the byte is actually clocked out of the UART FIFO before the next operation. Each byte at 300 baud holds the TX line low for ≈33 ms (≥60 ms total — sufficient to reset the target's UPDI clock and contention detector on a cold power-on with passive single-wire combiners), and the half-duplex echoes are read-and-discarded at 300 baud. After both BREAK bytes, `updi_open()` shall sleep ≈50 ms (`nanosleep()`) so that any echo bytes still in flight on the line bus reach the kernel RX queue at 300-baud framing, then `tcflush(TCIFLUSH)` discards them. Only then shall the session baud rate be restored (`tcsetattr(TCSADRAIN)` followed by a second `tcflush(TCIFLUSH)`). Without `tcdrain()` after each write, the subsequent baud switch flushes or re-clocks the queued BREAK byte, destroying the line-low pulse; without the 50 ms settle, late echo bytes arrive AFTER the baud restore, get framed at 115200 baud, and desynchronise every subsequent frame echo. This mirrors avrdude's `serialupdi` programmer exactly (verified by `strace`) and is necessary because no portable POSIX API generates a multi-ms BREAK on demand — `tcsendbreak()` is too short and not honoured by all kernel UART drivers (notably the RPi PL011).]]></text>
        <traces>
          <trace target="HLR" ref="HLR-006" name="UPDI Hardware Connection"/>
          <trace target="HLR" ref="HLR-036" name="UPDI Cold-Start Contention Handshake"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-03">
        <text><![CDATA[After the wake byte (attempt 0) or 300-baud double-break (attempts 1–2), `updi_open()` shall transmit `STCS ASI_CTRLB = ASI_CTRLB_CCDETDIS` (0x08) to disable the target's collision/contention detector, then `STCS ASI_CTRLA = ASI_CTRLA_IBDLY` (0x80) to enable inter-byte delay on the target's responses, then probe the link with `LDCS ASI_STATUSA` (opcode `0x80 | 0x00 = 0x80`). `ASI_STATUSA` carries UPDIREV in its upper nibble and is always readable while UPDI is enabled, including before any reset. If any STCS write or the LDCS probe fails, `updi_open()` shall advance to the next attempt. After 3 consecutive failed attempts the function shall return -1 and the caller shall treat the link as unavailable.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-036" name="UPDI Cold-Start Contention Handshake"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-04">
        <text><![CDATA[`updi_mem_read()` and `updi_mem_write()` shall transfer up to `UPDI_MAX_BLOCK` (256) bytes per UPDI burst using a three-frame sequence per datasheet §35.3.3.4: (1) a pointer-set frame — `ST_PTR_WORD` (`0x69`) followed by the 16-bit target address in little-endian order when `addr ≤ 0xFFFF`, or `ST_PTR_LONG` (`0x6A`) followed by the 24-bit address in little-endian order when `addr > 0xFFFF` — with a single ACK readback; (2) `REPEAT` (`0xA0`) followed by `(block_len - 1)`; (3) `LD ptr++` (`0x24`) for reads or `ST ptr++` (`0x64`) for writes. The selection between the 16-bit and 24-bit pointer-set forms shall be automatic based on the requested address, with the 24-bit form required for access to the AVR128DA/DB mapped-Flash region above 64 KiB. If `len` exceeds `UPDI_MAX_BLOCK`, the function shall split the request into consecutive bursts automatically, with no size restriction imposed on the caller.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-007" name="Target Memory Read"/>
          <trace target="HLR" ref="HLR-008" name="Target Memory Write"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-05">
        <text><![CDATA[`updi_mem_read()` shall use `select()` with a 100 ms timeout before each `read()` call on the UART file descriptor to enforce a per-byte inactivity deadline. If `select()` returns zero (timeout) before a byte is available, the function shall abandon the current transaction and return -1.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-007" name="Target Memory Read"/>
          <trace target="HLR" ref="HLR-037" name="UPDI Operation Timeout Bounds"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-06">
        <text><![CDATA[`updi_mem_write()` shall write arbitrary byte ranges to the SRAM address space of the target using UPDI ST commands and return 0 on success or -1 on UART framing error, timeout, or UPDI NAK.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-008" name="Target Memory Write"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-07">
        <text><![CDATA[`updi_nvm_write_flash()` shall require `word_addr` to be aligned to a 512-byte FLASH page boundary and `len` to be a non-zero multiple of 512. It shall enter NVM programming mode by transmitting the `KEY` opcode (`0xE0`) with the 8-byte string `"NVMProg "`, asserting reset with `STCS ASI_RESET_REQ = 0x59`, releasing reset with `STCS ASI_RESET_REQ = 0x00`, then polling `ASI_SYS_STATUS` via LDCS for bit 3 (NVMPROG, mask `0x08`) up to 100 iterations (≈ 100 ms). For each page it shall write the page data via `updi_mem_write()`, write `0x03` (ERWP) to `NVMCTRL_CTRLA`, and poll `NVMCTRL_STATUS` bit 0 (BUSY) clear up to 20 iterations (≈ 20 ms). On exit it shall toggle `ASI_RESET_REQ` `0x59` then `0x00` to release the target. Timeout of either poll shall return -1; a `NVMCTRL_STATUS` WRERR (bit 2) shall return `UPDI_ERR_WP`.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-009" name="FLASH Programming"/>
          <trace target="HLR" ref="HLR-037" name="UPDI Operation Timeout Bounds"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-08">
        <text><![CDATA[`updi_halt()` shall halt the CPU by writing `ASI_OCD_CTRLA_STOP` (0x01) to `ASI_OCD_CTRLA` via STCS, then polling `ASI_OCD_STATUS` via LDCS for bit `ASI_OCD_STATUS_STOPPED` (0x01) set. The function shall return 0 once the STOPPED bit is observed and -1 on UPDI failure or if the bit does not become set within the LDCS poll budget.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-010" name="Execution Control"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-09">
        <text><![CDATA[`updi_step()` shall single-step the halted CPU by setting `OCD_CTRL0_STEP` (0x04) in `OCD_CTRL0` (UPDI byte address `0x0F88`) via STS, then writing `ASI_OCD_CTRLA_RUN` (0x02) to `ASI_OCD_CTRLA` via STCS to release the CPU for exactly one instruction, then polling `ASI_OCD_STATUS.STOPPED` until set. Returns 0 on success, -1 on UPDI failure.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-010" name="Execution Control"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-10">
        <text><![CDATA[`updi_run()` shall resume the halted CPU by writing `ASI_OCD_CTRLA_RUN` (0x02) to `ASI_OCD_CTRLA` via STCS and shall return 0 immediately without polling — callers that need to wait for a subsequent halt shall use `updi_ocd_poll_halted()` directly. Returns -1 on UPDI failure.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-010" name="Execution Control"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-11">
        <text><![CDATA[`updi_mem_read()` shall be callable while the target CPU is in the RUNNING state (not halted). The function shall not call `updi_halt()` internally, permitting non-intrusive SRAM sampling for the monitor sub-commands and console polling without interrupting firmware execution.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-011" name="Non-Intrusive Background Memory Read"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-12">
        <text><![CDATA[`updi_console_poll()` shall perform a background `updi_mem_read()` of the avrOS software UART output buffer and return any pending bytes to the caller without halting the CPU. The function shall return the number of bytes read (0 if none pending) or -1 on UPDI error.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-012" name="UPDI Console Bridge"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-13">
        <text><![CDATA[`updi_read_device_info(fd, info)` shall populate the caller-supplied `UpdiDeviceInfo` struct with: 3 SIGROW DEVICEID bytes from physical address `0x1100`-`0x1102` (datasheet §7.6.1, Table 7-4), the SYSCFG.REVID byte at `0x0F01` (datasheet §8.3.2.1; SYSCFG base `0x0F00`), 16 SERNUM bytes at `0x1110`-`0x111F` (datasheet §7.6.2.3), and the three ASI registers `ASI_SYS_STATUS`, `ASI_KEY_STATUS`, `ASI_STATUSB` read via `LDCS`. The function shall be non-destructive (no halt, no NVM activity). On any UPDI read failure it shall set `info->fail_op` to a short ASCII tag (`"sigrow"`, `"revid"`, `"sernum"`, `"sys-status"`, `"key-status"`, `"asi-statusb"`) and return -1; on success it shall set `info->fail_op = NULL` and return 0.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-044" name="Device-Signature Diagnostic Mode"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-14">
        <text><![CDATA[After each successful `LDCS ASI_STATUSA` link probe inside `updi_open()`, the function shall read the target's 32-byte System Information Block by transmitting `SYNCH` (`0x55`) followed by opcode `UPDI_OP_KEY_SIB` (`0xE6`) and reading exactly `UPDI_SIB_LEN` (32) bytes of response — looped over `read()` until the full block is collected. The SIB read serves two purposes: (1) it confirms the link end-to-end (target must be actively responding to return non-echo bytes), and (2) it wakes a target that was left in UPDI SLEEP from a prior debug session. Without this step a sleeping target will appear to accept every link-layer probe (STCS, LDCS) — those frames merely echo on the half-duplex line — yet reject every memory access, manifesting as `updi_mem_read()` returning -1 immediately after a successful `updi_open()`. avrdude's `serialupdi` issues this exact 32-byte SIB read on every connect; the response payload (e.g. `"    AVR P:2D:1-3M2 (A7.KV001.0)\0"`) is discarded by `updi_open()` since the caller currently only needs the wake side-effect. If the SIB read times out or returns fewer than 32 bytes the attempt shall be abandoned and the next cold-start attempt shall begin.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-006" name="UPDI Hardware Connection"/>
          <trace target="HLR" ref="HLR-036" name="UPDI Cold-Start Contention Handshake"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-15">
        <text><![CDATA[`updi_enter_debug()` shall arm the AVR-Dx OCD by transmitting the UPDI `KEY` opcode (`0xE0`) followed by the 8-byte key string `"OCD     "` (ASCII `0x20 0x20 0x20 0x20 0x20 0x44 0x43 0x4F` — `"OCD"` reversed and right-padded with spaces, per the AVR-Dx datasheet KEY-string LSB-first convention), then pulsing `ASI_RESET_REQ` with `0x59` followed by `0x00`, then polling `ASI_OCD_STATUS` for the STOPPED bit. It shall be called once per session by `main.c` after `updi_open()` and before `rsp_listen()`. Returns 0 on success, -1 on UPDI failure or if STOPPED is never observed.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-010" name="Execution Control"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-16">
        <text><![CDATA[`updi_ocd_poll_halted(fd, timeout_ms)` shall poll `ASI_OCD_STATUS` via LDCS at ≈1 ms intervals until either `ASI_OCD_STATUS_STOPPED` (0x01) is set or `timeout_ms` elapses. Returns 0 if STOPPED is observed, +1 if the deadline elapses with the CPU still running, and -1 on UPDI failure. The RSP `c` handler uses this primitive with a short (≈1 ms) deadline to multiplex OCD polling with select() on the GDB client socket.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-010" name="Execution Control"/>
          <trace target="HLR" ref="HLR-018" name="Continue Execution"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-17">
        <text><![CDATA[`updi_ocd_read_halt_status(fd, *st0, *st1)` shall read the two OCD halt-cause bytes from `OCD_STATUS0` (`0x0F8C`) and `OCD_STATUS1` (`0x0F8D`) via UPDI LDS and store them in the caller's buffers. The bits in `OCD_STATUS1` distinguish the halt cause: `OCD_STATUS1_EXTBRK` (0x10) is set when the CPU halted due to an external break (STOP via STCS or Ctrl-C), `OCD_STATUS1_BP1`/`BP0STEP`/`SWBP`/`JMP`/`INT` indicate hardware-breakpoint/step/BREAK-opcode/change-of-flow/interrupt halts. Returns 0 on success, -1 on UPDI failure.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-010" name="Execution Control"/>
          <trace target="HLR" ref="HLR-018" name="Continue Execution"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-18">
        <text><![CDATA[`updi_ocd_read_gpr(fd, n, *val)` and `updi_ocd_write_gpr(fd, n, val)` shall read or write byte register `r<n>` (`n` in `0..31`) via UPDI LDS/STS to `OCD_REGFILE + n` (`0x0FA0 + n`). The CPU must be halted; behaviour against a running CPU is undefined. Returns 0 on success, -1 on UPDI failure or out-of-range `n`.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-014" name="Register Read and Write"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-19">
        <text><![CDATA[`updi_ocd_read_sreg(fd, *val)` and `updi_ocd_write_sreg(fd, val)` shall read or write the AVR status register via UPDI LDS/STS to `OCD_SREG` (`0x0F9C`). The CPU must be halted. Returns 0 on success, -1 on UPDI failure.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-014" name="Register Read and Write"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-20">
        <text><![CDATA[`updi_ocd_read_sp(fd, *val)` and `updi_ocd_write_sp(fd, val)` shall read or write the 16-bit AVR stack pointer (little-endian SPL|SPH) via UPDI LDS/STS at `OCD_SP` (`0x0F98`). `updi_ocd_read_pc(fd, *byte_addr)` and `updi_ocd_write_pc(fd, byte_addr)` shall read or write the 16-bit OCD program-counter register at `OCD_PC` (`0x0F94`), converting between the silicon's word-aligned PC+1 storage and the GDB byte address — read shifts the stored 16-bit word left by one and subtracts the AVR `+1` post-increment; write performs the inverse. The CPU must be halted. Returns 0 on success, -1 on UPDI failure.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-014" name="Register Read and Write"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-21">
        <text><![CDATA[`updi_ocd_set_hw_bp(fd, idx, byte_addr)` shall program hardware breakpoint comparator `idx` (0 or 1) to the supplied AVR byte address by writing the 3-byte little-endian address to `OCD_BP0A` (`0x0F80`) for slot 0 or `OCD_BP1A` (`0x0F84`) for slot 1 via UPDI STS, then setting the matching `OCD_CTRL1_BP0` / `OCD_CTRL1_BP1` enable bit and the global `OCD_CTRL0_HWBP` enable bit. Returns 0 on success, -1 on UPDI failure or invalid `idx`.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-016" name="Breakpoints"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-23">
        <text><![CDATA[`updi_nvm_write_eeprom(fd, addr, data, len)` shall write `len` bytes from `data` to the EEPROM window `[UPDI_EEPROM_BASE, UPDI_EEPROM_BASE + UPDI_EEPROM_SIZE)` (`0x814000`–`0x8143FF`). The function shall reject zero-length writes, an `addr` below the window, or `addr + len` past the window end with -1 and without issuing any UPDI traffic. It shall enter NVM programming mode via the standard NVMPROG KEY + reset pulse + `ASI_SYS_STATUS.NVMPROG` poll, then for each byte: (1) `STS NVMCTRL_CTRLA = EEERWR` (`0x13`); (2) `STS addr+i = data[i]`; (3) poll `NVMSTATUS.EEBUSY` (bit 1) clear up to 200 ms. After the final byte it shall write `NVMCTRL_CTRLA = NOCMD` to release the controller. Returns 0 on success, -1 on UPDI failure or BUSY timeout.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-046" name="Non-FLASH NVM Programming"/>
          <trace target="HLR" ref="HLR-037" name="UPDI Operation Timeout Bounds"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-24">
        <text><![CDATA[`updi_nvm_write_userrow(fd, addr, data, len)` shall write the USERROW window `[UPDI_USERROW_BASE, UPDI_USERROW_BASE + UPDI_USERROW_SIZE)` (`0x810080`–`0x8100FF`, 128 bytes) using the identical NVMPROG + `EEERWR` per-byte sequence as LLR-UPDI-23, with `EEBUSY` polling between bytes. Out-of-window addresses or oversize writes shall return -1 without UPDI traffic. Returns 0 on success, -1 on failure.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-046" name="Non-FLASH NVM Programming"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-25">
        <text><![CDATA[`updi_nvm_write_fuses(fd, addr, data, len)` shall write the FUSES window `[UPDI_FUSES_BASE, UPDI_FUSES_BASE + UPDI_FUSES_SIZE)` (`0x820000`–`0x82001F`, 32 bytes) using the same NVMPROG + `EEERWR` per-byte sequence with `EEBUSY` polling. Out-of-window addresses or oversize writes shall return -1 without UPDI traffic. Returns 0 on success, -1 on failure.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-046" name="Non-FLASH NVM Programming"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-26">
        <text><![CDATA[`updi_nvm_write_lockbits(fd, addr, data, len, allow_updi_disable)` shall write the LOCK window `[UPDI_LOCK_BASE, UPDI_LOCK_BASE + UPDI_LOCK_SIZE)` (`0x820040`–`0x820043`, 4 bytes) using the same NVMPROG + `EEERWR` per-byte + `EEBUSY` poll sequence. Two safety interlocks gate the write: (a) it shall LDCS-read `ASI_SYS_STATUS` and return `UPDI_ERR_LOCKED` (-3) if `LOCKSTATUS` (bit 1, mask `0x02`) is asserted — the device must already have been chip-erased to clear LOCKSTATUS; (b) when `allow_updi_disable` is false and `len == 4`, it shall reject any payload whose little-endian 4-byte value is not the unlock pattern `UPDI_LOCK_UNLOCKED` (`0x5CC5C55C`), returning `UPDI_ERR_LOCKED` — this prevents accidentally setting bit patterns that would assert `UPDIDIS` and permanently disable the UPDI interface. Out-of-window addresses shall return -1 without UPDI traffic. Returns 0 on success, -1 on UPDI failure or BUSY timeout, `UPDI_ERR_LOCKED` on safety-interlock rejection.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-047" name="Lockbit Programming Safety Interlock"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-22">
        <text><![CDATA[`updi_ocd_clear_hw_bp(fd, idx)` shall disable hardware breakpoint comparator `idx` (0 or 1) by clearing the matching `OCD_CTRL1_BP0` / `OCD_CTRL1_BP1` enable bit via UPDI LDS/STS read-modify-write on `OCD_CTRL1` (`0x0F89`). The function shall not clear the global `OCD_CTRL0_HWBP` bit (the other comparator may still be armed). Returns 0 on success, -1 on UPDI failure or invalid `idx`.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-016" name="Breakpoints"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-27">
        <text><![CDATA[The UPDI module shall publish a `UpdiDeviceMap` struct (family name; USERROW base/size; EEPROM base/size; FUSES base/size; LOCK base/size; SIGROW base; `hw_tested` flag) and a static `g_device_table[]` with one entry per supported family populated from the published datasheet memory maps: AVR-DA (USERROW `0x1080`/32 B, EEPROM `0x1400`/512 B, SIGROW `0x1100`, hw-tested), AVR-DB (identical to DA, untested), AVR-DD (USERROW `0x1080`/128 B, EEPROM `0x1400`/256 B, SIGROW `0x1100`, untested), AVR-DU (USERROW `0x1200`/512 B, EEPROM `0x1400`/256 B, SIGROW `0x1080`, untested), AVR-SD (identical to AVR-DU, untested). FUSES `0x1050`/16 B and LOCK `0x1040`/4 B are identical across all five families. The module shall expose two entry points: (a) `int updi_select_device(int fd, const char *force_family)` — when `force_family` is non-NULL and non-empty, find the entry whose `family` matches `force_family` case-insensitively and set the module's `g_device` pointer to it (emitting a `stderr` warning when `hw_tested` is false); otherwise read SIGROW DEVICEID0..2 at the canonical address `0x1100` and look up the matching autodetect signature for AVR-DA/DB, returning -1 with a diagnostic that lists all five families and the `--force-device` option when DEVICEID0 is not `0x1E` or the signature is unknown; (b) `const UpdiDeviceMap *updi_get_device(void)` — return the current `g_device` pointer (defaulting to the AVR-DA entry so callers that bypass `updi_select_device()` still see the historical AVR-DA constants). All four NVM writer functions (`updi_nvm_write_eeprom`, `updi_nvm_write_userrow`, `updi_nvm_write_fuses`, `updi_nvm_write_lockbits`) shall obtain their window base and size from `updi_get_device()` rather than the compile-time `UPDI_*_BASE/_SIZE` macros. The USERROW writer shall use a stack buffer of size `UPDI_USERROW_ROW_LEN_MAX` (512 B, sized for AVR-DU/SD) and the actual row length from `dev->userrow_size`. Returns 0 on success, -1 on autodetect failure or unknown family name.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-048" name="Multi-Family Runtime Device Dispatch"/>
          <trace target="HLR" ref="HLR-046" name="Non-FLASH NVM Programming"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-28">
        <text><![CDATA[The UPDI module shall publish `const char *updi_family_from_partname(const char *partname)`, a table-driven classifier that maps a lowercase Microchip AVR part-name string (the form emitted by the ELF `.note.gnu.avr.deviceinfo` note, e.g. `"avr128da28"`, `"avr64dd32"`, `"avr32sd20"`) onto one of the five family strings already published by `g_device_table[]`. The classifier shall accept the prefix `"avr"` followed by one or more ASCII digits (the flash-size field), followed by exactly two lowercase family-letters: `da`→`AVR-DA`, `db`→`AVR-DB`, `dd`→`AVR-DD`, `du`→`AVR-DU`, `sd`→`AVR-SD`. Any other input — `NULL`, the empty string, a string not beginning with `"avr<digit>"`, or a family-letter pair not in the table (notably `ea`/`eb`, which are the unsupported NVMCTRL v3 families) — shall return `NULL`, signalling the caller that the ELF carries no usable family identification and the SIGROW autodetect fallback in `updi_select_device()` must run. The classifier is intentionally co-located with `g_device_table[]` so that adding a new family requires updating both in the same commit.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-048" name="Multi-Family Runtime Device Dispatch"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-29">
        <text><![CDATA[`updi_nvm_read(fd, addr, buf, len)` shall read `len` bytes from the supplied UPDI unified-address `addr` into the caller-supplied `buf` using the existing `updi_mem_read()` block-transfer primitive (LLR-UPDI-04). It is a thin wrapper that exists so that Phase 9 verify and fuse-decode call sites read against an API named for the abstraction (NVM read) rather than the transport (UPDI memory read), and so it can be wrapped in unit tests independently of `updi_mem_read()`. The function shall not enter NVMPROG mode (NVM read does not require it) and shall not halt the CPU. Returns 0 on success, -1 on UPDI failure or timeout.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-049" name="Read-Back Verify After Load"/>
          <trace target="HLR" ref="HLR-051" name="Fuses Pretty-Printer in Device Mode"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-30">
        <text><![CDATA[`updi_crc32(buf, len)` shall compute the IEEE 802.3 CRC-32 of the supplied byte buffer using the reflected polynomial `0xEDB88320`, an initial value of `0xFFFFFFFF`, and a final XOR with `0xFFFFFFFF`. The implementation shall be a portable bit-wise loop (no precomputed table) because it is invoked only on verify-mismatch paths where speed is not critical. The function is published purely for the verify diagnostic line so that operators can correlate read-back failures against externally computed checksums (e.g. `crc32 < file`). Returns the computed 32-bit checksum.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-049" name="Read-Back Verify After Load"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-31">
        <text><![CDATA[The UPDI module shall publish a `UpdiBaudReport` typedef carrying `int baud, int errors, int samples`, the candidate baud ladder `const int updi_baud_ladder[] = {230400, 200000, 150000, 115200, 57600, 38400, 19200}` with `updi_baud_ladder_count = 7`, and the probe function `int updi_probe_baud(const char *serial_device, int samples, void (*visitor)(const UpdiBaudReport *, void *), void *user)`. The probe shall, for each rung in `updi_baud_ladder[]` in order, open the UART at the candidate rate (full cold-start handshake per LLR-UPDI-02 not required — a minimal `updi_open()` is sufficient), issue exactly `samples` `LDCS ASI_STATUSA` probes, count as an error each probe whose response byte is negative (read failure) or whose upper nibble is zero (UPDIREV must be non-zero on a live link), close the UART, and invoke `visitor(&report, user)` with the captured `{baud, errors, samples}`. After walking the entire ladder the function shall return the first rung that observed zero errors (highest-rate first because the ladder is ordered high-to-low), or `-1` if no rung observed zero errors. The probe is read-only: it issues no STCS write, no NVM operation, no system-reset pulse, and does not call `updi_enter_debug()`.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-052" name="Auto-Baud Link-Quality Probe in Device Mode"/>
        </traces>
      </llr>

      <llr id="LLR-UPDI-32">
        <text><![CDATA[`updi_format_fuses(family, fuse_buf, fuse_len, lock_buf, lock_len, out, out_size)` shall format a human-readable decode of the supplied FUSES window and 4-byte LOCK window into the caller-supplied `out` buffer (NUL-terminated, truncating at `out_size - 1` characters). The decode shall consist of: (1) a header line `Fuses (<family>, <fuse_len> bytes):`; (2) for each byte covered by the per-family fuse table, one indented line `  0x%02X  <FUSE_NAME>%-22s = 0x%02X` followed by indented sub-lines naming each documented bit-field with human-readable enum names (e.g. `WDTCFG.PERIOD = 8K`); (3) for each fuse byte without a registered decoder, one indented line `  0x%02X  (reserved) raw=0x%02X`; (4) a final line `Lock: 0x%08X (UNLOCKED|LOCKED)` where `UNLOCKED` is the canonical 4-byte little-endian value `UPDI_LOCK_UNLOCKED = 0x5CC5C55C` and any other value is reported as `LOCKED`. The decoder tables (`FuseField` struct and `g_avr_dx_fuses[]`) shall live alongside `g_device_table[]` in `src/updi.c` so they are selected by the same `updi_select_device()` runtime path. Phase 9 ships the AVR-DA/AVR-DB table; for other families the function shall emit only the reserved-style lines for every byte (no bit-field decoding). Returns the number of bytes written to `out` (excluding the terminating NUL).]]></text>
        <traces>
          <trace target="HLR" ref="HLR-051" name="Fuses Pretty-Printer in Device Mode"/>
        </traces>
      </llr>
    </function>

    <function number="4" title="src/gdb_rsp.c — GDB Remote Serial Protocol Server" name="rsp" source="src/gdb_rsp.c">
      <intro><![CDATA[Requirements for RSP packet framing, dispatch, GDB packet handlers, session lifecycle, and socket configuration.]]></intro>

      <llr id="LLR-RSP-01">
        <text><![CDATA[`rsp_listen()` shall set `SO_REUSEADDR` on the listener socket before calling `bind()`. After `rsp_accept()` returns a connected client socket, the client socket shall have `TCP_NODELAY` set to disable Nagle's algorithm.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-003" name="GDB Listener Startup"/>
          <trace target="HLR" ref="HLR-038" name="GDB Session Response Latency"/>
        </traces>
      </llr>

      <llr id="LLR-RSP-02">
        <text><![CDATA[`rsp_recv_packet()` shall scan incoming bytes discarding pre-packet ACK/NAK characters until a `$` delimiter is received. It shall accumulate the payload with a running XOR checksum until `#` is received, then compare the computed checksum against the two ASCII-hex checksum bytes that follow. On match it shall write `+` to the socket and return the payload length; on mismatch it shall write `-` and return -1.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-013" name="RSP Server Accessibility"/>
        </traces>
      </llr>

      <llr id="LLR-RSP-03">
        <text><![CDATA[The `on_read_regs` handler for the `g` packet shall produce a 78-character hex string in GDB's AVR register-block layout: R0-R31 at hex positions 0-63, SREG at 64-65, SPL at 66-67, SPH at 68-69, and PC as 4-byte little-endian byte-address at positions 70-77. Reads against the active virtual thread — or against any thread when the FSM mapper has not identified one (e.g. `--load` without `--device`) — shall use the live OCD register file via `updi_ocd_read_gpr()`, `updi_ocd_read_sreg()`, `updi_ocd_read_sp()`, and `updi_ocd_read_pc()`. Reads against a non-active virtual thread shall delegate to `fsm_get_registers()` for the synthesized FSM register frame.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-014" name="Register Read and Write"/>
          <trace target="HLR" ref="HLR-026" name="Virtual Thread Register Frame"/>
        </traces>
      </llr>

      <llr id="LLR-RSP-04">
        <text><![CDATA[The `on_write_regs` handler shall accept both the full-register `G<78 hex>` and single-register `P<n>=<hex>` packets and shall route each register slot to the matching OCD writer: `updi_ocd_write_gpr()` for registers 0..31, `updi_ocd_write_sreg()` for register 32, `updi_ocd_write_sp()` for register 33 (little-endian 16-bit), and `updi_ocd_write_pc()` for register 34 (little-endian 32-bit byte-address). On success the handler shall reply `OK`; on any OCD failure or hex parse error it shall reply `E01`.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-014" name="Register Read and Write"/>
        </traces>
      </llr>

      <llr id="LLR-RSP-05">
        <text><![CDATA[The `on_read_mem` handler for `m addr,len` shall translate the GDB unified-address `addr` to a UPDI physical address by flipping bit 23 (GDB FLASH at `< 0x800000` maps to UPDI `0x800000+`; GDB data space at `>= 0x800000` maps to UPDI `0x000000+`), then call `updi_mem_read()` and return the result as a hex string. Both FLASH and SRAM addresses shall be accepted.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-015" name="Memory Read and Write"/>
        </traces>
      </llr>

      <llr id="LLR-RSP-06">
        <text><![CDATA[The `on_write_mem` handler for `M addr,len:data` (hex-encoded) and `X addr,len:data` (binary-encoded) packets shall route by the GDB address space: writes with bit 23 set (data space) shall go to `updi_mem_write()` against the GDB-byte address with bit 23 cleared; writes with bit 23 clear (program space) shall go to `updi_nvm_write_flash()` against the GDB address OR'd with `UPDI_FLASH_BASE` (`0x800000`). Returns `OK` on success, `E01` on failure.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-015" name="Memory Read and Write"/>
        </traces>
      </llr>

      <llr id="LLR-RSP-07">
        <text><![CDATA[The `on_insert_bp` handler for `Z0` and `Z1` shall route both kinds to the same two AVR-Dx OCD hardware comparators tracked by `RspContext.hw_bp_addr[0..1]`. If the supplied address is already present in either slot the handler shall reply `OK` without re-programming silicon. Otherwise the handler shall find an empty slot (sentinel `0xFFFFFFFF`), call `updi_ocd_set_hw_bp()` with the GDB-byte address masked to 23 bits, record the address in the shadow, and reply `OK`. The handler shall not call `updi_nvm_write_flash()` — patching the AVR BREAK opcode into FLASH at runtime is incompatible with maintaining live OCD register state.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-016" name="Breakpoints"/>
        </traces>
      </llr>

      <llr id="LLR-RSP-08">
        <text><![CDATA[The `on_remove_bp` handler for `z0` and `z1` shall locate the supplied address in `RspContext.hw_bp_addr[0..1]`, call `updi_ocd_clear_hw_bp()` for that slot, reset the slot to the empty sentinel, and reply `OK`. If the address is not present in either slot (e.g. the server has been restarted mid-session and lost the shadow) the handler shall reply `OK` rather than an error so a GDB resync after reconnect is non-fatal.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-016" name="Breakpoints"/>
        </traces>
      </llr>

      <llr id="LLR-RSP-09">
        <text><![CDATA[When `on_insert_bp` is called with both `RspContext.hw_bp_addr[]` slots occupied and the requested address does not match either slot, the handler shall not call `updi_ocd_set_hw_bp()` and shall reply with the GDB error packet `E08`. This surfaces the AVR-Dx two-comparator hardware limit to the user.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-016" name="Breakpoints"/>
        </traces>
      </llr>

      <llr id="LLR-RSP-10">
        <text><![CDATA[The `on_step` handler for `s`/`vCont;s` shall call `updi_step()`, invalidate the FSM thread cache, rebuild the thread list, and then reply with the stop-reason packet `T<sig>thread:<active_id>;` produced by the `on_halt_reason` handler. The signal returned by `signal_for_halt_status()` shall be `T02` (SIGINT) if `OCD_STATUS1.EXTBRK` is observed (e.g. the step landed back inside a software-halt window) and `T05` (SIGTRAP) otherwise.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-017" name="Single-Step Execution"/>
        </traces>
      </llr>

      <llr id="LLR-RSP-11">
        <text><![CDATA[The `on_continue` handler for `c`/`vCont;c` shall call `updi_run()` and `fsm_invalidate()`, then enter a poll loop that waits for either (a) a spontaneous CPU halt detected by `updi_ocd_poll_halted()` or (b) an async-interrupt byte (`\x03`) on the GDB client socket. The loop shall multiplex the two events using `select()` with a 5 ms timeout on the client socket; on socket-readable it shall consume one byte and break on `0x03`; on select-timeout it shall poll OCD with a 1 ms deadline. After the loop breaks the handler shall call `updi_halt()` to guarantee the CPU is stopped, rebuild the FSM thread list, and reply with `T02thread:<id>;` if Ctrl-C was received or `T05thread:<id>;` otherwise. The handler shall give up after `UPDI_FAIL_MAX` (8) consecutive `updi_ocd_poll_halted()` failures and reply `E01` rather than spinning indefinitely on a degraded UPDI link.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-018" name="Continue Execution"/>
        </traces>
      </llr>

      <llr id="LLR-RSP-12">
        <text><![CDATA[`rsp_dispatch()` shall handle the following packets inline without invoking any `RspHandlers` slot, because each response is a fixed string requiring no target interaction: `qSupported` (responds with `PacketSize=800;QStartNoAckMode+;multiprocess-;vContSupported+`), `qAttached` (responds `1`), `QStartNoAckMode` (calls `rsp_set_noack(true)` and responds `OK`), and `vCont?` (responds `vCont;c;s`).]]></text>
        <traces>
          <trace target="HLR" ref="HLR-019" name="RSP Capability Negotiation and Lifecycle"/>
        </traces>
      </llr>

      <llr id="LLR-RSP-13">
        <text><![CDATA[The `on_detach` handler for the `D` packet shall release both AVR-Dx OCD hardware-breakpoint comparators by calling `updi_ocd_clear_hw_bp()` for every occupied `RspContext.hw_bp_addr[]` slot and resetting the shadow to the empty sentinel, then call `updi_run()` to resume the target, send the `OK` reply, close the GDB client socket, and reset `*ctx->gdb_fd_p` to -1 so the event loop re-enters the listening state. The server process shall not exit. Without the comparator cleanup, BP0/BP1 remain armed in silicon and the next GDB session inherits stale breakpoints from the previous client.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-016" name="Breakpoints"/>
          <trace target="HLR" ref="HLR-019" name="RSP Capability Negotiation and Lifecycle"/>
        </traces>
      </llr>

      <llr id="LLR-RSP-14">
        <text><![CDATA[The `on_detach` handler for the `k` (kill) packet shall set `g_quit = 1` to cause the event loop to exit, triggering the full resource teardown sequence defined by LLR-MAIN-07.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-019" name="RSP Capability Negotiation and Lifecycle"/>
          <trace target="HLR" ref="HLR-035" name="Graceful Shutdown and Resource Release"/>
        </traces>
      </llr>

      <llr id="LLR-RSP-15">
        <text><![CDATA[The `on_monitor` handler for `qRcmd` shall pass the raw ASCII-hex-encoded command body directly to `monitor_dispatch()` without pre-decoding it, and relay the return code to the GDB client as `OK` on success (return 0), an O-packet error message on UPDI failure (return -1), or an empty reply on unrecognised sub-command (return -2).]]></text>
        <traces>
          <trace target="HLR" ref="HLR-029" name="Monitor Events Command"/>
          <trace target="HLR" ref="HLR-030" name="Monitor Queues Command"/>
        </traces>
      </llr>

      <llr id="LLR-RSP-16">
        <text><![CDATA[The `H` (set-thread) packet handler shall store the requested GDB thread ID in the session context and use it to select the virtual thread for subsequent `g`/`G`/`P` register operations. Thread ID -1 (all threads) and 0 (any thread) shall both be interpreted as selecting the active FSM thread.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-025" name="Active Thread Identification"/>
        </traces>
      </llr>

      <llr id="LLR-RSP-17">
        <text><![CDATA[The `signal_for_halt_status()` helper shall read `OCD_STATUS0`/`OCD_STATUS1` via `updi_ocd_read_halt_status()` and return the stop-signal string `"T02"` (SIGINT) if `OCD_STATUS1_EXTBRK` (0x10) is set in `OCD_STATUS1`, or `"T05"` (SIGTRAP) otherwise (covering hardware-breakpoint match, BREAK opcode, single-step, change-of-flow, and interrupt halts). On OCD read failure the helper shall default to `"T05"` so a degraded link still surfaces a non-empty stop reason to GDB.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-017" name="Single-Step Execution"/>
          <trace target="HLR" ref="HLR-018" name="Continue Execution"/>
        </traces>
      </llr>

      <llr id="LLR-RSP-18">
        <text><![CDATA[The `hw_bp_clear_all(ctx)` helper shall iterate `ctx->hw_bp_addr[0..1]` and, for every slot not equal to the empty sentinel (`0xFFFFFFFF`), call `updi_ocd_clear_hw_bp()` and reset the slot. It shall be called by `on_detach` for the `D` packet to guarantee the silicon comparators are released before the target is resumed.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-016" name="Breakpoints"/>
          <trace target="HLR" ref="HLR-019" name="RSP Capability Negotiation and Lifecycle"/>
        </traces>
      </llr>
    </function>

    <function number="5" title="src/elf_parser.c — ELF Binary Parser" name="elf" source="src/elf_parser.c">
      <intro><![CDATA[Requirements for `elf_open()`, `elf_close()`, `elf_find_avros_tables()`, and `elf_flash_addr()`.]]></intro>

      <llr id="LLR-ELF-01">
        <text><![CDATA[`elf_open()` shall read the first 16 bytes of the ELF binary and verify: the 4-byte magic sequence (0x7F `'E'` `'L'` `'F'`), `EI_CLASS == ELFCLASS32`, and `e_machine == EM_AVR` (0x0053). Any validation failure shall cause `elf_open()` to log a warning and return -1 without any heap allocation.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-021" name="ELF Binary Parsing"/>
        </traces>
      </llr>

      <llr id="LLR-ELF-02">
        <text><![CDATA[`elf_open()` shall locate the `.symtab` section (type `SHT_SYMTAB`) and its associated string table section (index given by `sh_link`), and load both into heap-allocated buffers stored in `ctx->symtab` and `ctx->strtab`. `ctx->sym_count` and `ctx->strtab_size` shall be set accordingly. On any `malloc` failure, `elf_open()` shall free all partial allocations and return -1.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-021" name="ELF Binary Parsing"/>
          <trace target="HLR" ref="HLR-040" name="Bounded Heap Allocation"/>
        </traces>
      </llr>

      <llr id="LLR-ELF-03">
        <text><![CDATA[`elf_find_avros_tables()` shall perform a single O(sym_count) linear scan of `ctx->symtab`, compare each symbol name against the 8 avrOS sentinel names defined in SDD §6.3.3, and populate the corresponding fields of `AvrOsSymbolIndex` for each match. The function shall return 0 (success, even if partial) or -1 only on an internal ELF read error.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-021" name="ELF Binary Parsing"/>
        </traces>
      </llr>

      <llr id="LLR-ELF-04">
        <text><![CDATA[`elf_flash_addr()` shall convert an ELF virtual memory address to a physical FLASH word address using the formula: `physical_word_addr = (vma - ctx->flash_base) / 2`. All FLASH-resident avrOS symbol addresses stored in `AvrOsSymbolIndex` shall be computed via this function.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-022" name="Harvard Architecture Address Mapping"/>
        </traces>
      </llr>

      <llr id="LLR-ELF-05">
        <text><![CDATA[When `elf_find_avros_tables()` completes the symbol scan and fewer than the expected avrOS symbols were found, it shall return 0 (not -1). Fields of `AvrOsSymbolIndex` for absent symbols shall retain their zero-initialised values, and the function shall log an informational message identifying which symbols were not found.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-023" name="Fail-Safe Degradation on Missing Symbols"/>
        </traces>
      </llr>

      <llr id="LLR-ELF-06">
        <text><![CDATA[`elf_close()` shall free `ctx->symtab`, free `ctx->strtab`, and close `ctx->fd`. After returning, `ctx->symtab` and `ctx->strtab` shall be set to NULL and `ctx->fd` to -1. `elf_close()` shall be safe to call on a partially initialised `ElfContext` (e.g., after a failed `elf_open()`).]]></text>
        <traces>
          <trace target="HLR" ref="HLR-040" name="Bounded Heap Allocation"/>
        </traces>
      </llr>

      <llr id="LLR-ELF-07">
        <text><![CDATA[`src/elf_parser.c` shall obtain ELF32 type definitions via a platform-conditional include: on Linux (`#ifdef __linux__`) it shall use the system header `<elf.h>`; on all other platforms it shall include the bundled portability shim `src/elf.h`. The shim shall define at minimum: `Elf32_Half`, `Elf32_Word`, `Elf32_Off`, `Elf32_Addr`, `Elf32_Ehdr`, `Elf32_Phdr`, `Elf32_Shdr`, `Elf32_Sym`, the `ELFMAG`/`SELFMAG` magic constants, `EI_CLASS`, `ELFCLASS32`, `EM_AVR` (0x0053), `PT_LOAD`, `SHT_SYMTAB`, `SHT_STRTAB`, `SHN_UNDEF`, and the `ELF32_ST_BIND`/`ELF32_ST_TYPE` accessor macros.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-033" name="Native Linux and macOS Build"/>
        </traces>
      </llr>

      <llr id="LLR-ELF-08">
        <text><![CDATA[`elf_open()` shall iterate over all ELF program headers and identify `PT_LOAD` segments. The first `PT_LOAD` segment encountered shall be treated as the FLASH load segment: its `p_vaddr` and `p_filesz` shall be stored in `ctx->flash_base` and `ctx->flash_size` respectively. The second `PT_LOAD` segment shall be treated as the SRAM load segment: its `p_vaddr` and `p_memsz` shall be stored in `ctx->sram_base` and `ctx->sram_size` respectively. If fewer than two `PT_LOAD` segments are present the absent values shall remain zero. These fields are the sole inputs to `elf_flash_addr()`.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-021" name="ELF Binary Parsing"/>
          <trace target="HLR" ref="HLR-022" name="Harvard Architecture Address Mapping"/>
        </traces>
      </llr>

      <llr id="LLR-ELF-09">
        <text><![CDATA[`elf_open()` shall best-effort extract the lowercase Microchip part-name string from the `.note.gnu.avr.deviceinfo` ELF note emitted by avr-gcc + Microchip device packs (binutils ≥ 2.27). The parser shall walk every section of type `SHT_NOTE`, validate the standard ELF note prologue (`namesz = 4`, `type = 1`, `name = "AVR\0"`), then scan the descriptor bytes for the first NUL-terminated ASCII run starting with `"avr"` followed immediately by an ASCII digit. Up to `ELF_DEVICE_NAME_MAX - 1` (15) bytes shall be copied into the new `ElfContext.device_name` field and NUL-terminated. The note-format heuristic — scanning for `"avr<digit>…"` rather than decoding the descriptor's full struct — is intentional, so the parser survives binutils version drift in the descriptor layout. When the note is absent, malformed, or contains no recognised part-name, `device_name` shall remain the empty string the entry path cleared (`ctx->device_name[0] = '\0'`), which `app_main()` (LLR-MAIN-13) treats as "ELF carries no device identification" and which `updi_family_from_partname()` (LLR-UPDI-28) maps to `NULL`. This LLR shall not cause `elf_open()` to fail on any input that already passes the ELF32/EM_AVR validation in LLR-ELF-01.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-021" name="ELF Binary Parsing"/>
          <trace target="HLR" ref="HLR-048" name="Multi-Family Runtime Device Dispatch"/>
        </traces>
      </llr>
    </function>

    <function number="6" title="src/fsm_mapper.c — FSM Virtual Thread Mapper" name="fsm" source="src/fsm_mapper.c">
      <intro><![CDATA[Requirements for `fsm_build_thread_list()`, `fsm_invalidate()`, `fsm_get_active_thread()`, and `fsm_get_registers()`.]]></intro>

      <llr id="LLR-FSM-01">
        <text><![CDATA[`fsm_build_thread_list()` shall read `idx->fsm_table_count` consecutive 9-byte `fsmStateMachineDescr_t` records from the FLASH address `idx->fsm_table_addr` via `updi_mem_read()`. Each descriptor carries a 2-byte `name` pointer (FLASH), a 2-byte `stateMachine` pointer (SRAM, or NULL for an initializer slot that shall be skipped), a 2-byte `handler` function pointer, a 1-byte `priority`, and a 2-byte `instance`. For each non-NULL `stateMachine` pointer, the function shall read 2 bytes from `stateMachine + 9` (the `currState` field of `fsmStateMachine_t`) to obtain the current state function pointer and store it in `thread->state_fn`. A UPDI read failure at any point shall cause the function to return -1.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-024" name="FSM Thread Enumeration"/>
        </traces>
      </llr>

      <llr id="LLR-FSM-02">
        <text><![CDATA[`fsm_build_thread_list()` shall assign `thread->gdb_id = loop_index + 1` (1-based) to each FSM entry in the order the entries appear in the FLASH table. Thread IDs shall not be reassigned within a debug session, ensuring the same FSM always maps to the same GDB thread ID.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-024" name="FSM Thread Enumeration"/>
        </traces>
      </llr>

      <llr id="LLR-FSM-03">
        <text><![CDATA[`fsm_build_thread_list()` shall read the 2-byte SRAM value at `idx->current_fsm_addr` (avrOS `currStateMachine`) and compare it against each retained FSM entry's `stateMachine` pointer. The matching entry shall have `thread->is_active = true` and its `gdb_id` stored in `ctx->active_id`. If no entry matches, `ctx->active_id` shall be set to 0.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-025" name="Active Thread Identification"/>
        </traces>
      </llr>

      <llr id="LLR-FSM-04">
        <text><![CDATA[`fsm_get_registers()` shall set the PC field (GDB register index 35, 4-byte little-endian at hex positions 70–77 of the 78-character g-packet buffer) to `thread->state_fn`. For non-active threads, R0–R31 (indices 0–31) and SREG (index 32) shall be zero-filled, and SPL (index 33) and SPH (index 34) shall be set to zero. For the active thread, SREG (index 32, hex positions 64–65), SPL (index 33, hex positions 66–67), and SPH (index 34, hex positions 68–69) shall be read from the target SRAM via `updi_mem_read()`.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-026" name="Virtual Thread Register Frame"/>
        </traces>
      </llr>

      <llr id="LLR-FSM-05">
        <text><![CDATA[`fsm_build_thread_list()` shall process at most `FSM_MAX_THREADS` (32) entries from the FSM registration table regardless of `idx->fsm_table_count`. If `idx->fsm_table_count` exceeds `FSM_MAX_THREADS`, the function shall log a diagnostic warning and return `FSM_MAX_THREADS` as the thread count without returning an error to the GDB client.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-027" name="Complete FSM Thread Coverage"/>
        </traces>
      </llr>

      <llr id="LLR-FSM-06">
        <text><![CDATA[`fsm_get_registers()` shall not issue any UPDI memory read targeting a non-active thread's stack region. The suspended FSM register frame shall be constructed entirely from the cached `thread->state_fn` value; the SP field shall be zeroed for non-active threads.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-028" name="Stack-Free Thread Model"/>
        </traces>
      </llr>
    </function>

    <function number="7" title="src/monitor.c — avrOS System Introspection" name="monitor" source="src/monitor.c">
      <intro><![CDATA[Requirements for `monitor_dispatch()` and its static sub-command helpers `cmd_events()` and `cmd_queues()`.]]></intro>

      <llr id="LLR-MON-01">
        <text><![CDATA[`monitor_dispatch()` shall hex-decode the ASCII-hex-encoded `cmd` string (pairs of hex digit characters) into a plain-text command string before any prefix or sub-command matching is attempted. An odd-length or invalid hex-digit sequence shall be treated as an unrecognised command.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-029" name="Monitor Events Command"/>
        </traces>
      </llr>

      <llr id="LLR-MON-02">
        <text><![CDATA[`monitor_dispatch()` shall verify that the decoded command string begins with the prefix `"avros "` (case-sensitive, including the trailing space). If the prefix does not match, the function shall send a usage-hint O-packet to the GDB console and return -2.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-029" name="Monitor Events Command"/>
          <trace target="HLR" ref="HLR-030" name="Monitor Queues Command"/>
        </traces>
      </llr>

      <llr id="LLR-MON-03">
        <text><![CDATA[`cmd_events()` shall call `updi_mem_read()` to read `idx->event_count` consecutive 4-byte `evntDescriptor_t` records from FLASH at `idx->event_table_addr`. For each descriptor it shall dereference the `name` pointer (FLASH) to read the event's NUL-terminated display name (up to 31 chars) and the `status` pointer (SRAM) to read the 1-byte status flag, then append a line of the form `"  <name>: <status>\n"` and send the complete buffer as RSP O-packets.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-029" name="Monitor Events Command"/>
        </traces>
      </llr>

      <llr id="LLR-MON-04">
        <text><![CDATA[`cmd_queues()` shall call `updi_mem_read()` to read `idx->queue_count` consecutive 10-byte `queDescriptor_t` records from FLASH at `idx->queue_table_addr`. For each descriptor it shall extract and format the `capacity` and `sizeOfElement` fields as a one-line entry and send the complete table as RSP O-packets.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-030" name="Monitor Queues Command"/>
        </traces>
      </llr>

      <llr id="LLR-MON-06">
        <text><![CDATA[All monitor output shall be assembled into a temporary buffer of at most 512 bytes, hex-encoded (each ASCII byte converted to 2 hex characters per RSP O-packet spec), and transmitted to the GDB client as one or more RSP O-packets via `rsp_send_packet()`.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-029" name="Monitor Events Command"/>
          <trace target="HLR" ref="HLR-030" name="Monitor Queues Command"/>
        </traces>
      </llr>

      <llr id="LLR-MON-07">
        <text><![CDATA[`monitor_dispatch()` and all of its static helpers (`cmd_events()`, `cmd_queues()`) shall use `updi_mem_read()` exclusively for all target memory access. None of these functions shall call `updi_halt()`, ensuring that monitor commands never interrupt firmware execution.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-011" name="Non-Intrusive Background Memory Read"/>
          <trace target="HLR" ref="HLR-032" name="Introspection Reliability"/>
        </traces>
      </llr>
    </function>

    <function number="8" title="Makefile and Documentation" name="inst">
      <intro><![CDATA[Requirements for the Makefile installation targets (`install`, `uninstall`, `check-tools`, `bundle`) and the documentation deliverables (`doc/UserManual.md`, `doc/avrOSdb.1`). Bundle artefacts are written to `dist/`.]]></intro>

      <llr id="LLR-INST-01">
        <text><![CDATA[The Makefile shall provide a `check-tools` target that tests for the presence of every required host tool: `gcc` (or `cc`), `make`, `avr-gcc`, and `avr-nm`. For each absent tool the target shall print a diagnostic message to `stderr` naming the missing tool and then exit with status 1. The target shall exit with status 0 only when all required tools are found.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-041" name="Makefile Install and Uninstall Targets"/>
        </traces>
      </llr>

      <llr id="LLR-INST-02">
        <text><![CDATA[The Makefile `install` target shall accept a `PREFIX` variable (default `/usr/local`) and install: the compiled `avrOSdb` binary to `$(PREFIX)/bin/avrOSdb` and the man page `doc/avrOSdb.1` to `$(PREFIX)/share/man/man1/avrOSdb.1`. The target shall create any missing intermediate directories using `install -d`. The binary shall be installed with mode 0755 and the man page with mode 0644.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-041" name="Makefile Install and Uninstall Targets"/>
        </traces>
      </llr>

      <llr id="LLR-INST-03">
        <text><![CDATA[The Makefile `uninstall` target shall remove `$(PREFIX)/bin/avrOSdb` and `$(PREFIX)/share/man/man1/avrOSdb.1` using `rm -f`. The target shall be idempotent: running it when the files are already absent shall exit with status 0 without error.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-041" name="Makefile Install and Uninstall Targets"/>
        </traces>
      </llr>

      <llr id="LLR-INST-04">
        <text><![CDATA[The project shall include a user manual at `doc/UserManual.md` documenting: (1) prerequisites (required host tools and their minimum versions), (2) build instructions (`make`, `make test`), (3) connection wiring for the UPDI adapter (1 kΩ resistor on UPDI pin, serial adapter TX/RX orientation), (4) launch invocation and all CLI options, and (5) at least two complete usage examples (one with `--load`, one without).]]></text>
        <traces>
          <trace target="HLR" ref="HLR-042" name="User Manual and Unix Man Page"/>
        </traces>
      </llr>

      <llr id="LLR-INST-05">
        <text><![CDATA[The project shall include a Unix man page at `doc/avrOSdb.1` in `groff`/`troff` format. The man page shall contain at minimum: NAME, SYNOPSIS, DESCRIPTION, OPTIONS (one entry per CLI flag), OPERANDS, EXIT STATUS, EXAMPLES, and SEE ALSO sections. The man page shall be parseable by `man -l doc/avrOSdb.1` without error or warning on Linux.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-042" name="User Manual and Unix Man Page"/>
        </traces>
      </llr>

      <llr id="LLR-INST-06">
        <text><![CDATA[The Makefile `bundle` target shall produce a Debian binary package at `dist/avrOSdb_$(VERSION)_amd64.deb` using `dpkg-deb --build`. The package staging tree shall install the binary to `usr/bin/avrOSdb` (mode 0755) and the man page to `usr/share/man/man1/avrOSdb.1` (mode 0644). The `DEBIAN/control` file shall declare at minimum: `Package: avrOSdb`, `Version`, `Architecture: amd64`, `Maintainer`, and `Description` fields.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-043" name="Distribution Package Bundle"/>
        </traces>
      </llr>

      <llr id="LLR-INST-07">
        <text><![CDATA[The Makefile `bundle` target shall produce an RPM binary package at `dist/avrOSdb-$(VERSION)-1.x86_64.rpm` using `rpmbuild`. A `.spec` file shall be generated in the build tree containing at minimum: `Name`, `Version`, `Release: 1`, `Summary`, `License`, `%description`, `%install`, and `%files` sections. The `%files` section shall list `/usr/bin/avrOSdb` and `/usr/share/man/man1/avrOSdb.1`.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-043" name="Distribution Package Bundle"/>
        </traces>
      </llr>

      <llr id="LLR-INST-08">
        <text><![CDATA[The Makefile `bundle` target shall produce a Homebrew formula at `dist/avrOSdb.rb`. The formula shall be a valid Ruby file usable with `brew install --formula dist/avrOSdb.rb`. It shall define at minimum: `desc`, `homepage`, `url`, `sha256`, `version`, `license`, and an `install` block that places the binary using `bin.install` and the man page using `man1.install`.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-043" name="Distribution Package Bundle"/>
        </traces>
      </llr>
    </function>

    <function number="9" title="tests/hw/hw_test.c — On-Target Hardware Integration Test Harness" name="hwtest" source="tests/hw/hw_test.c">
      <intro><![CDATA[Requirements for the manual-only on-target hardware integration test program. The harness links against `src/updi.c` (and, for Group D, spawns the production `build/avrOSdb` binary via `fork`/`execl`) and runs against live AVR-Dx silicon connected via a UART/UPDI adapter.]]></intro>

      <llr id="LLR-HWTEST-01">
        <text><![CDATA[`tests/hw/hw_test.c` shall accept its configuration from environment variables and from long-form command-line options. Recognised settings: `HW_PORT` / `--port` (serial device path; default `/dev/ttyUSB0`), `HW_DEVICE_ID` / `--device-id` (expected 24-bit SIGROW signature as a hex literal; default `0x1E970A` for AVR128DA48), `HW_SRAM_ADDR` / `--sram-addr` (16-bit SRAM probe address; default `0x7000`), `HW_FLASH_PAGE_ADDR` / `--flash-page` (FLASH page-aligned address used by Group C; default `0x1FE00`), `HW_RSP_PORT` / `--rsp-port` (TCP port for Group D; default `1234`), `HW_TEST_ELF` / `--elf` (optional ELF binary passed to the spawned RSP server in Group D), `HW_ADDR_24BIT` / `--addr-24bit` (boolean; when true, FLASH access uses the 24-bit UPDI addressing path), and `HW_VERBOSE` / `--verbose` (boolean; enables per-byte tracing). CLI flags shall override the corresponding environment values.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-045" name="On-Target Hardware Integration Test Harness"/>
        </traces>
      </llr>

      <llr id="LLR-HWTEST-02">
        <text><![CDATA[Group A (read-only link probes) shall execute the following ordered checks against the live target: **A1** open the serial port and perform the UPDI cold-start handshake (BREAK + SYNCH); **A2** call `updi_read_device_info()` and capture the result for downstream assertions; **A3** verify that the captured `DEVICEID0..2` bytes match the configured `HW_DEVICE_ID`; **A4** verify that the SIGROW signature is not all-zero and not all-`0xFF`; **A5** verify that `SYSCFG.REVID` is non-zero; **A6** verify that the 10-byte `SERNUM` field is not all-zero and not all-`0xFF`; **A7** verify that `ASI_SYS_STATUS.NVMPROG` is clear (target is not stuck in a programming session). Each individual check shall be reported as a separate `[PASS]`/`[FAIL]` line.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-045" name="On-Target Hardware Integration Test Harness"/>
          <trace target="HLR" ref="HLR-044" name="Device-Signature Diagnostic Mode"/>
        </traces>
      </llr>

      <llr id="LLR-HWTEST-03">
        <text><![CDATA[Group B (non-destructive SRAM round-trips) shall execute the following ordered checks at `HW_SRAM_ADDR`: **B0** single-byte write/read; **B1** 64-byte buffer write/read; **B1a** SIGROW read repeated twice and compared (isolates the read path from the write path); **B1b** write 64 bytes of constant `0xAA` then read back (value-independence check); **B1c** write a known pattern once then read it back twice and compare both reads (detects live CPU writes to the probed region — on failure the harness shall hint that the operator pick a different `--sram-addr` / `HW_SRAM_ADDR`); **B2** 600-byte buffer write/read (exercises multi-chunk transfer); **B3** when `HW_ADDR_24BIT` is true, a small write/read at the configured FLASH page address using the 24-bit UPDI addressing path. None of the Group B steps shall modify FLASH.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-045" name="On-Target Hardware Integration Test Harness"/>
          <trace target="HLR" ref="HLR-011" name="Non-Intrusive Background Memory Read"/>
        </traces>
      </llr>

      <llr id="LLR-HWTEST-04">
        <text><![CDATA[Group C (destructive FLASH write/verify) shall execute only when the environment variable `HW_TEST_NVM_CONFIRM=YES` is set; otherwise the group shall be reported as skipped. When confirmed, Group C shall perform: **C1** erase the FLASH page at `HW_FLASH_PAGE_ADDR`, then write a deterministic pattern via the NVM controller; **C2** read the page back and verify a byte-for-byte match against the written pattern. The harness shall enter and leave the NVM programming session cleanly (NVMPROG key in, NVMPROG key cleared on exit) so that subsequent Group A/B checks find the target in a non-programming state.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-045" name="On-Target Hardware Integration Test Harness"/>
          <trace target="HLR" ref="HLR-008" name="Flash Write"/>
        </traces>
      </llr>

      <llr id="LLR-HWTEST-05">
        <text><![CDATA[Group D (RSP server spawn-and-probe) shall execute only when the environment variable `HW_TEST_RSP=YES` is set; otherwise the group shall be reported as skipped. When enabled, Group D shall `fork` and `execl` the production `build/avrOSdb` binary with the configured serial port and optional `HW_TEST_ELF`, wait briefly for the server's TCP listener to bind on `HW_RSP_PORT`, then perform: **D1** connect a TCP client to `127.0.0.1:HW_RSP_PORT` and verify the connection is accepted; **D2** send an RSP `qSupported` packet (`$qSupported#37`) and verify a non-empty, properly-framed RSP reply is received. The harness shall terminate the spawned server cleanly (SIGTERM + `waitpid`) on completion or on test failure.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-045" name="On-Target Hardware Integration Test Harness"/>
          <trace target="HLR" ref="HLR-014" name="GDB RSP TCP Listener"/>
        </traces>
      </llr>

      <llr id="LLR-HWTEST-06">
        <text><![CDATA[The Makefile shall expose four targets that drive `tests/hw/hw_test.c`: `hw-test` runs Groups A+B (read-only); `hw-test-nvm` runs Groups A+B+C and forwards `HW_TEST_NVM_CONFIRM=YES`; `hw-test-rsp` runs Groups A+B+D and forwards `HW_TEST_RSP=YES`; `hw-test-all` runs all four groups. Every hw-test variable (`HW_PORT`, `HW_DEVICE_ID`, `HW_SRAM_ADDR`, `HW_FLASH_PAGE_ADDR`, `HW_RSP_PORT`, `HW_TEST_ELF`, `HW_ADDR_24BIT`, `HW_VERBOSE`) shall be passed through to the child process via the target recipes. None of these targets shall be a dependency of the default `make test` target or be built by `make all`. The `make help` output shall document each variable, its default, and a brief description.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-045" name="On-Target Hardware Integration Test Harness"/>
        </traces>
      </llr>

      <llr id="LLR-HWTEST-07">
        <text><![CDATA[Group F (Phase 9 link diagnostics) shall execute three on-target checks: **F1** call `updi_probe_baud(cfg->port, 8, visitor, NULL)`, record each rung's `{baud, errors, samples}`, and PASS if the chosen rate is ≥ 19200 baud (the slowest rung in the ladder); **F2** with the harness UPDI fd re-opened at the configured baud, read the FUSES window and the 4-byte LOCK window via `updi_nvm_read()`, format them via `updi_format_fuses()` into a 2 KiB buffer, and PASS if the output contains the literal substrings `WDTCFG`, `OSCCFG`, `SYSCFG0`, and `Lock:`; **F3** (destructive, gated by `HW_TEST_NVM_CONFIRM=YES`) fork the production `build/avrOSdb --prog --erase --baud <N> <port> <elf>` against the bench ELF, capture the child's stdout/stderr, and PASS if `WEXITSTATUS == 0` AND the captured output contains the literal `verify: OK`. F1 needs the harness UPDI fd closed before running (so the probe can open its own fd at each rung); the harness shall close fd before F1, re-open at `cfg->baud` for F2, then close again before F3.]]></text>
        <traces>
          <trace target="HLR" ref="HLR-045" name="On-Target Hardware Integration Test Harness"/>
          <trace target="HLR" ref="HLR-049" name="Read-Back Verify After Load"/>
          <trace target="HLR" ref="HLR-050" name="Program-and-Exit Mode"/>
          <trace target="HLR" ref="HLR-051" name="Fuses Pretty-Printer in Device Mode"/>
          <trace target="HLR" ref="HLR-052" name="Auto-Baud Link-Quality Probe in Device Mode"/>
        </traces>
      </llr>
    </function>
  </llrs>

  <!-- Test sources (see tools/Developers_Guide.md §7). -->
  <tests>

    <!-- ============================================================ -->
    <!-- src/main.c unit tests                                        -->
    <!-- ============================================================ -->
    <file path="tests/test_main.c" role="unit" count="32">
      <header><![CDATA[Unit tests for `src/main.c`: argument parsing, event loop, signal handling, and resource teardown. Uses `fork()`/`waitpid()` to test `exit()` paths and a mock UPDI/RSP layer to exercise the event loop without real hardware.]]></header>

      <test name="parse_args_valid_positional_args_populate_config">
        <purpose><![CDATA[Call `parse_args()` with a valid `<serial-device>` and `<elf-file>` and verify that the returned `AppConfig` carries the correct device path and ELF path.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-01"/></traces>
      </test>

      <test name="parse_args_unrecognised_flag_exits_1">
        <purpose><![CDATA[Call `parse_args()` with an unrecognised flag and assert that the process exits with status 1 and that a usage message is present on `stderr`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-01"/></traces>
      </test>

      <test name="parse_args_default_port_baud_load_when_not_supplied">
        <purpose><![CDATA[Call `parse_args()` with only the two mandatory positional arguments and verify that `cfg.gdb_port == 1234`, `cfg.baud_rate == 115200`, and `cfg.load_flash == false`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-02"/></traces>
      </test>

      <test name="parse_args_missing_serial_device_exits_1">
        <purpose><![CDATA[Call `parse_args()` with no positional arguments and assert that the process exits with status 1 and emits a usage message.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-02"/></traces>
      </test>

      <test name="parse_args_missing_elf_file_exits_1">
        <purpose><![CDATA[Call `parse_args()` with only `<serial-device>` and no `<elf-file>` and assert that the process exits with status 1.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-02"/></traces>
      </test>

      <test name="main_updi_open_failure_releases_resources_and_returns_1">
        <purpose><![CDATA[Inject a mock `updi_open()` that returns -1 and verify that `main()` does not call `rsp_listen()`, releases any already-opened resources, and returns exit code 1.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-03"/></traces>
      </test>

      <test name="main_rsp_listen_failure_closes_updi_and_returns_1">
        <purpose><![CDATA[Inject a mock `rsp_listen()` that returns -1 and verify that `main()` calls `updi_close()` before returning exit code 1.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-03"/></traces>
      </test>

      <test name="main_load_flag_writes_all_pt_load_segments_to_flash">
        <purpose><![CDATA[With `cfg.load = true` and a mock ELF context containing two `PT_LOAD` segments, verify that `main()` calls `updi_nvm_write_flash()` exactly twice, once per segment, before entering `event_loop()`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-04"/></traces>
      </test>

      <test name="main_load_nvm_write_failure_prints_error_and_returns_1">
        <purpose><![CDATA[With `cfg.load = true` and a mock `updi_nvm_write_flash()` that returns -1, verify that `main()` prints an error to `stderr` and returns exit code 1 without entering `event_loop()`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-04"/></traces>
      </test>

      <test name="event_loop_uses_single_select_no_pthread_create">
        <purpose><![CDATA[Instrument `event_loop()` with mock file descriptors and verify that `select()` is called with all three fds (`listen_fd`, `gdb_fd`, `updi_fd`) in the read set, and that no calls to `pthread_create()` occur.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-05"/></traces>
      </test>

      <test name="event_loop_accepts_gdb_client_when_gdb_fd_is_minus1">
        <purpose><![CDATA[With `cfg.gdb_fd == -1` and `listen_fd` readable, verify that `event_loop()` calls `accept()` and updates `cfg.gdb_fd`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-05"/></traces>
      </test>

      <test name="sigint_handler_sets_g_quit_to_1">
        <purpose><![CDATA[Send SIGINT to the test process and verify that the signal handler installed by `main()` sets `g_quit` to 1 without performing any other operation.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-06"/></traces>
      </test>

      <test name="event_loop_exits_immediately_when_g_quit_is_1">
        <purpose><![CDATA[Set `g_quit = 1` before calling `event_loop()` and verify that the function returns immediately without calling `select()`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-06"/></traces>
      </test>

      <test name="main_cleanup_closes_gdb_elf_updi_in_order">
        <purpose><![CDATA[On a simulated clean shutdown, verify that `main()` calls `rsp_close(gdb_fd)`, `rsp_close(listen_fd)`, `elf_close()`, and `updi_close()` in that exact order, and returns exit code 0.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-07"/></traces>
      </test>

      <test name="main_calls_updi_enter_debug_after_updi_open_before_rsp_listen">
        <purpose><![CDATA[Run `main()` with mocked `updi_open()`, `updi_enter_debug()`, and `rsp_listen()` recording call order. Verify (a) `updi_enter_debug()` is called exactly once after `updi_open()` succeeds and before `rsp_listen()`, and (b) when `updi_enter_debug()` returns non-zero, `main()` invokes the LLR-MAIN-07 teardown sequence and exits with code 1 without calling `rsp_listen()`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-10"/></traces>
      </test>

      <test name="parse_args_allow_lock_updi_sets_flag">
        <purpose><![CDATA[Call `parse_args()` with `--allow-lock-updi` present in argv and verify that the returned `AppConfig.allow_lock_updi` is `true`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-11"/></traces>
      </test>

      <test name="parse_args_force_device_captures_family">
        <purpose><![CDATA[Call `parse_args()` with `--force-device=AVR-DD` in argv and verify that `AppConfig.force_device` points to the string `"AVR-DD"`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-12"/></traces>
      </test>

      <test name="parse_args_force_device_default_is_null">
        <purpose><![CDATA[Call `parse_args()` with no `--force-device` flag and verify that `AppConfig.force_device` defaults to `NULL` (autodetect path will be used).]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-12"/></traces>
      </test>

      <test name="load_segments_dispatches_eeprom_segment_to_eeprom_writer">
        <purpose><![CDATA[Build a one-segment ELF at vaddr `0x814000` size 8 and run `app_main(... --load ...)`. Verify wrapped `updi_nvm_write_eeprom` is called exactly once with `addr=0x814000` and `len=8`, while no other NVM writer is called.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-11"/></traces>
      </test>

      <test name="load_segments_dispatches_fuses_segment_to_fuses_writer">
        <purpose><![CDATA[Build a one-segment ELF at vaddr `0x820004` size 2 and run `app_main(... --load ...)`. Verify wrapped `updi_nvm_write_fuses` is called exactly once with `addr=0x820004` and `len=2`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-11"/></traces>
      </test>

      <test name="load_segments_dispatches_userrow_segment_to_userrow_writer">
        <purpose><![CDATA[Build a one-segment ELF at vaddr `0x810080` size 16 and run `app_main(... --load ...)`. Verify wrapped `updi_nvm_write_userrow` is called exactly once with `addr=0x810080`. Regression test ensuring the classifier does not mis-route USERROW into the FLASH branch (the USERROW VMA lies between `UPDI_FLASH_BASE` and `UPDI_EEPROM_BASE`).]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-11"/></traces>
      </test>

      <test name="load_segments_lock_requires_erase_else_exit_1">
        <purpose><![CDATA[Build a LOCK segment at vaddr `0x820040` size 4 and run `app_main(... --load ...)` WITHOUT `--erase`. Verify `app_main` returns 1 and that `updi_nvm_write_lockbits` was never called.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-11"/></traces>
      </test>

      <test name="load_segments_lock_with_erase_dispatches_to_lockbits_writer">
        <purpose><![CDATA[Build a LOCK segment at vaddr `0x820040` size 4 and run `app_main(... --erase --load ...)`. Verify wrapped `updi_chip_erase` was called exactly once, then wrapped `updi_nvm_write_lockbits` was called exactly once with `addr=0x820040` and the recorded `allow_updi_disable` flag matching `cfg.allow_lock_updi` (false in this case).]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-11"/></traces>
      </test>

      <test name="load_segments_sigrow_segment_is_skipped">
        <purpose><![CDATA[Build a SIGROW segment at vaddr `0x811080` size 4 and run `app_main(... --load ...)`. Verify NONE of the FLASH/EEPROM/USERROW/FUSES/LOCK writers were called and `app_main` returns 0.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-11"/></traces>
      </test>

      <test name="load_segments_unknown_window_returns_minus1">
        <purpose><![CDATA[Build a segment at vaddr `0x830000` (outside every programmable UPDI window) and run `app_main(... --load ...)`. Verify `app_main` returns 1 (load failure) and that no NVM writer was called.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-11"/></traces>
      </test>

      <test name="main_autoselects_family_from_elf_deviceinfo">
        <purpose><![CDATA[Plant `mk_elf_planted_device_name = "avr64dd32"` (deviceinfo string for an AVR-DD part) and `mk_updi_get_device_family = "AVR-DD"` (silicon agrees). Run `app_main()` without `--force-device`. Verify that the wrapped `updi_select_device()` was called exactly once with `force_family == ""` (the wrap normalises `NULL` to `""`), confirming that the ELF deviceinfo is NOT used as a selection input — SIGROW autodetect runs unbiased — and that `app_main()` returns 0 because the autodetected family agrees with the ELF (no mismatch).]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-13"/></traces>
      </test>

      <test name="main_force_device_overrides_elf_deviceinfo">
        <purpose><![CDATA[Plant `mk_elf_planted_device_name = "avr64dd32"` (would map to AVR-DD) but also pass `--force-device=AVR-DA`. Verify that the wrapped `updi_select_device()` was called with `force_family == "AVR-DA"` and that `app_main()` returned 0, demonstrating the documented precedence `--force-device > deviceinfo`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-13"/></traces>
      </test>

      <test name="main_aborts_on_elf_vs_silicon_family_mismatch">
        <purpose><![CDATA[Plant `mk_elf_planted_device_name = "avr64dd32"` (AVR-DD) and override `mk_updi_get_device_family = "AVR-DA"` so the post-select check sees a family mismatch. Run `app_main()` with no `--force-device`. Verify that `app_main()` returns 1, that the wrapped `updi_select_device()` ran exactly once, and that the wrapped `rsp_listen()` was NOT invoked — i.e. the application aborted before opening the GDB listener.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-13"/></traces>
      </test>

      <test name="main_passes_null_to_select_when_elf_lacks_deviceinfo">
        <purpose><![CDATA[Leave `mk_elf_planted_device_name` empty (`\0`), run `app_main()` with no `--force-device`. Verify the wrapped `updi_select_device()` was called once and that the captured `force_family` argument was the empty string (the wrap normalises `NULL` to `""`), confirming that `app_main()` falls through to the SIGROW autodetect path when the ELF carries no deviceinfo note.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-13"/></traces>
      </test>

      <test name="parse_args_prog_mode_sets_flag">
        <purpose><![CDATA[Call `parse_args()` with argv `{ "prog", "--prog", "/dev/x", "a.elf" }` and assert `cfg.prog_mode == true`, `cfg.device_info == false`, and `cfg.load_flash == false` — confirming `--prog` is recognised, sets only `prog_mode`, and does not implicitly set the other operating-mode flags.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-14"/></traces>
      </test>

      <test name="parse_args_no_verify_sets_flag">
        <purpose><![CDATA[Call `parse_args()` with argv `{ "prog", "--load", "--no-verify", "/dev/x", "a.elf" }` and assert `cfg.no_verify == true` — confirming the verify-suppression flag is recognised and stored on `AppConfig`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-14"/></traces>
      </test>

      <test name="parse_args_no_autobaud_sets_flag">
        <purpose><![CDATA[Call `parse_args()` with argv `{ "prog", "--device", "--no-autobaud", "/dev/x" }` and assert `cfg.no_autobaud == true` — confirming the autobaud-suppression flag is recognised and stored on `AppConfig` so `run_device_mode()` can gate its probe call accordingly.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-17"/></traces>
      </test>
    </file>

    <!-- ============================================================ -->
    <!-- src/updi.c unit tests                                        -->
    <!-- ============================================================ -->
    <file path="tests/test_updi.c" role="unit" count="47">
      <header><![CDATA[Unit tests for `src/updi.c`: UART configuration, BREAK/SYNCH/LDCS link initialisation, three-frame block memory read/write, NVM flash programming, OCD-mode entry, OCD execution-control primitives (halt/run/step + poll_halted), OCD register-file access (GPR/SREG/SP/PC) and hardware-breakpoint comparator programming, and console polling. A loopback PTY pair substitutes for real UART hardware; a `--wrap=select` linker shim lets tests inject `select()` timeouts.]]></header>

      <test name="updi_open_sets_8e2_raw_half_duplex_via_termios">
        <purpose><![CDATA[Open a PTY pair, call `updi_open()` on the slave side, and inspect the resulting `termios` structure to confirm `CS8`, `CSTOPB` (2 stop bits), even-parity intent (`PARODD` clear), and raw mode (no `ICANON`/`ECHO`/`ISIG`/`OPOST`). PARENB itself is not asserted because Linux PTYs strip parity on slave reopen; parity is verified on real serial hardware in Phase 3 integration.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-01"/></traces>
      </test>

      <test name="updi_open_returns_minus1_on_device_open_failure">
        <purpose><![CDATA[Call `updi_open()` with a path that does not exist and verify the return value is -1.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-01"/></traces>
      </test>

      <test name="updi_open_asserts_wake_byte_then_stcs_ctrlb">
        <purpose><![CDATA[Verify that `updi_open()` transmits a single 0x00 wake byte first, then the STCS CTRLB frame (SYNCH `0x55`, opcode `0xC0|ASI_CTRLB = 0xC3`, value `ASI_CTRLB_CCDETDIS = 0x08`) — the first half of the avrdude-matching cold-start sequence (issue #19).]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-02"/></traces>
      </test>

      <test name="updi_open_issues_stcs_ctrla_and_ldcs_statusa">
        <purpose><![CDATA[Verify that after STCS CTRLB the SUT writes STCS CTRLA (`0xC2`, value `ASI_CTRLA_IBDLY = 0x80`) and then probes the link with LDCS STATUSA (`0x80|ASI_STATUSA = 0x80`) — the second half of the cold-start sequence (issue #19).]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-03"/></traces>
      </test>

      <test name="updi_open_restores_session_baud_after_break">
        <purpose><![CDATA[Verify that `updi_open()` leaves the configured session baud rate (B115200) in effect after a successful cold-start sequence — i.e. it does not corrupt the termios baud setting. The PTY responder also feeds the 32-byte SIB payload (`SYNCH`+`0xE6` → `"    AVR P:2D:1-3M2 (A7.KV001.0)\0"`), so a successful run additionally exercises the SIB-read SLEEP-wake step that `updi_open()` issues after the LDCS ASI_STATUSA link probe.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-02"/><trace target="LLR" ref="LLR-UPDI-14"/></traces>
      </test>

      <test name="updi_open_retries_cold_start_3_times_on_no_ack">
        <purpose><![CDATA[Simulate a target that never responds and verify that `updi_open()` runs all three cold-start attempts before giving up: attempt 0 emits 1× 0x00 wake byte then STCS+STCS+LDCS at session baud; attempts 1 and 2 each emit 2× 0x00 BREAK bytes (at 300 baud via `tcsetattr`) then STCS+STCS+LDCS at session baud. With no 0x00 byte present anywhere else in the frames, this yields 1 + 2 + 2 = 5 zero bytes on the wire.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-03"/></traces>
      </test>

      <test name="updi_open_returns_minus1_after_3_consecutive_link_failures">
        <purpose><![CDATA[After 3 failed cold-start attempts (wake + STCS CTRLB + STCS CTRLA + LDCS STATUSA) with no response, verify that `updi_open()` returns -1.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-03"/></traces>
      </test>

      <test name="updi_mem_read_splits_request_larger_than_256_bytes">
        <purpose><![CDATA[Request a read of 512 bytes and verify that `updi_mem_read()` issues two consecutive 256-byte three-frame bursts rather than a single oversized transfer.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-04"/></traces>
      </test>

      <test name="updi_mem_read_uses_repeat_ld_auto_increment_sequence">
        <purpose><![CDATA[Capture the bytes written to the UART and verify the three-frame burst: byte 0 = `ST_PTR_WORD` (`0x69`), bytes 1–2 = address little-endian, frame 2 = `REPEAT` (`0xA0`) + count, frame 3 = `LD ptr++` (`0x24`).]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-04"/></traces>
      </test>

      <test name="updi_mem_read_uses_24bit_addressing_above_64kib">
        <purpose><![CDATA[Request a read at address `0x012345` (above the 64 KiB boundary) and verify that `updi_mem_read()` emits the 24-bit pointer-set frame: `SYNCH`, `ST_PTR_LONG` (`0x6A`), then the address bytes in little-endian order (`0x45`, `0x23`, `0x01`), followed by the standard `REPEAT` + `LD ptr++` frames. Confirms automatic selection of the 24-bit form for AVR128DA/DB mapped-Flash access above 64 KiB.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-04"/></traces>
      </test>

      <test name="updi_mem_read_calls_select_with_100ms_timeout_before_read">
        <purpose><![CDATA[Using a `--wrap=select` shim that records the `timeval` argument, verify that `updi_mem_read()` passes a `timeval` of 0 seconds and 100,000 microseconds before every `read()` call.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-05"/></traces>
      </test>

      <test name="updi_mem_read_returns_minus1_on_select_timeout">
        <purpose><![CDATA[Inject a wrapped `select()` that returns 0 (timeout) and verify that `updi_mem_read()` immediately returns -1 without calling `read()`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-05"/></traces>
      </test>

      <test name="updi_mem_write_returns_0_on_success">
        <purpose><![CDATA[Call `updi_mem_write()` targeting an SRAM address with a loopback PTY that echoes the expected ACK and verify the return value is 0.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-06"/></traces>
      </test>

      <test name="updi_mem_write_returns_minus1_on_updi_nak">
        <purpose><![CDATA[Inject a responder that returns a non-ACK byte instead of `UPDI_ACK` (0x40) and verify that `updi_mem_write()` returns -1.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-06"/></traces>
      </test>

      <test name="updi_nvm_write_flash_rejects_unaligned_address">
        <purpose><![CDATA[Call `updi_nvm_write_flash()` with a `word_addr` that is not a multiple of 512 and verify the function returns -1 without issuing any UART traffic.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-07"/></traces>
      </test>

      <test name="updi_nvm_write_flash_rejects_non_multiple_of_512_length">
        <purpose><![CDATA[Call `updi_nvm_write_flash()` with a `len` that is not a non-zero multiple of 512 and verify the function returns -1.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-07"/></traces>
      </test>

      <test name="updi_nvm_write_flash_nvmprog_poll_timeout_returns_minus1">
        <purpose><![CDATA[Inject a mock target that never sets the NVMPROG bit (`ASI_SYS_STATUS` bit 3) and verify that `updi_nvm_write_flash()` returns -1 after 100 polling iterations.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-07"/></traces>
      </test>

      <test name="updi_nvm_write_flash_per_page_busy_timeout_returns_minus1">
        <purpose><![CDATA[Inject a mock target that never clears `NVMCTRL_STATUS` BUSY (bit 0) after a page write and verify that `updi_nvm_write_flash()` returns -1 after 20 polling iterations.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-07"/></traces>
      </test>

      <test name="updi_enter_debug_writes_key_pulses_reset_and_polls_ocd_stopped">
        <purpose><![CDATA[Drive a PTY responder that emulates an AVR-Dx in OCD mode. Verify that `updi_enter_debug()` (a) issues the UPDI KEY opcode (`SYNCH`+`0xE0|UPDI_KEY_64`) followed by the 8 ASCII bytes `"OCD     "` (reversed-byte order per the UPDI KEY protocol); (b) pulses `STCS ASI_RESET_REQ = 0x59` then `STCS ASI_RESET_REQ = 0x00`; (c) polls CS-space `ASI_OCD_STATUS` (LDCS opcode `0x80|0x05 = 0x85`) until `OCD_STATUS_STOPPED (0x01)` is observed, and returns 0.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-15"/></traces>
      </test>

      <test name="updi_enter_debug_returns_minus1_when_ocd_status_never_stopped">
        <purpose><![CDATA[Drive a PTY responder that completes the KEY and RESET pulses but always returns `OCD_STATUS = 0x00`. Verify that `updi_enter_debug()` polls `ASI_OCD_STATUS` for the LLR-bounded number of iterations and then returns -1.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-15"/></traces>
      </test>

      <test name="updi_halt_writes_ocd_ctrla_stop_via_stcs">
        <purpose><![CDATA[Verify that `updi_halt()` issues `STCS ASI_OCD_CTRLA = OCD_CTRLA_STOP (0x01)` and returns 0 on the responder ACK. No FLASH/SRAM frames must appear on the wire.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-08"/></traces>
      </test>

      <test name="updi_run_writes_ocd_ctrla_run_via_stcs">
        <purpose><![CDATA[Verify that `updi_run()` issues `STCS ASI_OCD_CTRLA = OCD_CTRLA_RUN (0x02)` and returns 0 on responder ACK.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-10"/></traces>
      </test>

      <test name="updi_step_writes_ctrl0_step_bit_then_polls_stopped">
        <purpose><![CDATA[Verify that `updi_step()` writes the STEP bit into the OCD `CTRL0` MMIO register at UPDI 0x0F80+offset and then polls `ASI_OCD_STATUS` for `STOPPED` before returning 0.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-09"/></traces>
      </test>

      <test name="updi_ocd_poll_halted_returns_0_when_stopped_set">
        <purpose><![CDATA[Stage `ASI_OCD_STATUS = OCD_STATUS_STOPPED (0x01)` in the PTY responder and verify that `updi_ocd_poll_halted(fd, deadline_ms=1)` returns 0 on the first poll. With `OCD_STATUS = 0x00` it must return 1 (still running) after the deadline elapses, and -1 on LDCS link failure.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-16"/></traces>
      </test>

      <test name="updi_ocd_read_halt_status_returns_two_status_bytes">
        <purpose><![CDATA[Stage `OCD_STATUS0 = 0x00`, `OCD_STATUS1 = OCD_STATUS1_EXTBRK (0x10)` in the responder's MMIO image. Verify that `updi_ocd_read_halt_status()` reads both bytes from UPDI 0x0F80+STATUS0/STATUS1 and writes them into the caller's out-parameters.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-17"/></traces>
      </test>

      <test name="updi_ocd_read_gpr_reads_regfile_byte_at_ocd_offset">
        <purpose><![CDATA[Verify that `updi_ocd_read_gpr(fd, n=5)` reads exactly one byte from UPDI address `0x0F80 + OCD_REGFILE_OFFSET + 5` and returns the responder-supplied value.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-18"/></traces>
      </test>

      <test name="updi_ocd_write_gpr_writes_single_byte_at_ocd_offset">
        <purpose><![CDATA[Verify that `updi_ocd_write_gpr(fd, n=12, val=0xAB)` issues an ST-byte to UPDI address `0x0F80 + OCD_REGFILE_OFFSET + 12` with value `0xAB` and returns 0 on ACK.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-18"/></traces>
      </test>

      <test name="updi_ocd_read_write_sreg_round_trip">
        <purpose><![CDATA[Verify that `updi_ocd_write_sreg(fd, 0x5A)` writes one byte to UPDI `0x0F80 + OCD_SREG_OFFSET` and that `updi_ocd_read_sreg(fd, &out)` reads it back from the same address.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-19"/></traces>
      </test>

      <test name="updi_ocd_read_write_sp_round_trip_little_endian">
        <purpose><![CDATA[Verify that `updi_ocd_write_sp(fd, 0x3FFE)` writes two bytes little-endian to UPDI `0x0F80 + OCD_SP_OFFSET` and that `updi_ocd_read_sp(fd, &out)` reads them back as a 16-bit LE value.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-20"/></traces>
      </test>

      <test name="updi_ocd_read_write_pc_round_trip_32bit_le_byte_addr">
        <purpose><![CDATA[Verify that `updi_ocd_write_pc(fd, 0x00012345)` writes four bytes little-endian (PC as byte address) to UPDI `0x0F80 + OCD_PC_OFFSET` and that `updi_ocd_read_pc(fd, &out)` reads them back as a 32-bit LE value.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-20"/></traces>
      </test>

      <test name="updi_ocd_set_hw_bp_writes_bp_addr_and_enables_ctrl1">
        <purpose><![CDATA[Verify that `updi_ocd_set_hw_bp(fd, idx=0, addr=0x001234)` writes the 32-bit address (little-endian) to `BP0A` at UPDI `0x0F80 + OCD_BP0A_OFFSET` and then sets the corresponding enable bit in `CTRL1` at UPDI `0x0F80 + OCD_CTRL1_OFFSET`. With `idx=1` the same sequence targets `BP1A` and the BP1 enable bit.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-21"/></traces>
      </test>

      <test name="updi_ocd_clear_hw_bp_clears_only_target_slot">
        <purpose><![CDATA[After setting both BP0 and BP1, verify that `updi_ocd_clear_hw_bp(fd, idx=0)` clears only the BP0 enable bit in `CTRL1` (read-modify-write) and leaves BP1 armed.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-22"/></traces>
      </test>

      <test name="updi_console_poll_returns_pending_bytes_without_halting">
        <purpose><![CDATA[Pre-stuff the master end of the PTY with 4 bytes and verify that `updi_console_poll()` returns 4 and copies the bytes to the output buffer without calling `updi_halt()`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-11"/><trace target="LLR" ref="LLR-UPDI-12"/></traces>
      </test>

      <test name="updi_console_poll_returns_0_when_output_buffer_empty">
        <purpose><![CDATA[With no data pending on the master end, verify that `updi_console_poll()` returns 0 in a non-blocking manner.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-12"/></traces>
      </test>

      <test name="updi_nvm_write_eeprom_rejects_addr_below_window">
        <purpose><![CDATA[Call `updi_nvm_write_eeprom(fd, UPDI_EEPROM_BASE - 1, data, 1)` and verify the function returns -1 without transmitting any bytes on the UPDI line (drain the master end and assert zero bytes captured). Confirms the EEPROM window guard rejects out-of-window addresses before any NVM activity.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-23"/></traces>
      </test>

      <test name="updi_nvm_write_eeprom_rejects_addr_above_window">
        <purpose><![CDATA[Call `updi_nvm_write_eeprom(fd, UPDI_EEPROM_BASE + UPDI_EEPROM_SIZE - 1, data, 2)` and verify the function returns -1: the second byte (at `UPDI_EEPROM_BASE + UPDI_EEPROM_SIZE`) lies past the EEPROM window.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-23"/></traces>
      </test>

      <test name="updi_nvm_write_fuses_rejects_len_exceeds_window">
        <purpose><![CDATA[Call `updi_nvm_write_fuses(fd, UPDI_FUSES_BASE, data, UPDI_FUSES_SIZE + 1)` and verify the function returns -1: the payload extends one byte past the FUSES window.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-25"/></traces>
      </test>

      <test name="updi_nvm_write_userrow_rejects_zero_length">
        <purpose><![CDATA[Call `updi_nvm_write_userrow(fd, UPDI_USERROW_BASE, NULL, 0)` and verify the function returns -1 without UPDI traffic.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-24"/></traces>
      </test>

      <test name="updi_nvm_write_lockbits_returns_locked_when_lockstatus_set">
        <purpose><![CDATA[Pre-stuff the LDCS ASI_SYS_STATUS response with `0x02` (LOCKSTATUS asserted) and call `updi_nvm_write_lockbits(fd, UPDI_LOCK_BASE, unlock_pattern, 4, allow=true)`. Verify the function returns `UPDI_ERR_LOCKED` (-3) — even with the unlock pattern and `allow_updi_disable=true`, a locked device cannot be written.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-26"/></traces>
      </test>

      <test name="updi_nvm_write_lockbits_refuses_updidis_value_without_flag">
        <purpose><![CDATA[Pre-stuff the LDCS ASI_SYS_STATUS response with `0x00` (LOCKSTATUS clear, post-erase state) and call `updi_nvm_write_lockbits(fd, UPDI_LOCK_BASE, {0,0,0,0}, 4, allow=false)`. Verify the function returns `UPDI_ERR_LOCKED`: only the `UPDI_LOCK_UNLOCKED` (`0x5CC5C55C`) 4-byte pattern is accepted without `--allow-lock-updi`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-26"/></traces>
      </test>

      <test name="updi_get_device_default_is_avrda">
        <purpose><![CDATA[Call `updi_get_device()` before any `updi_select_device()` invocation and verify the returned descriptor names AVR-DA, exposes the historical `UPDI_USERROW_BASE`/`UPDI_USERROW_SIZE` and `UPDI_EEPROM_BASE`/`UPDI_EEPROM_SIZE` constants, and has `hw_tested == true`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-27"/></traces>
      </test>

      <test name="updi_select_device_force_avrdd_sets_active_descriptor">
        <purpose><![CDATA[Call `updi_select_device(-1, "avr-dd")` (case-insensitive force path, no UPDI traffic). Verify the function returns 0, `updi_get_device()` now returns the AVR-DD descriptor (`userrow_size == 128`, `hw_tested == false`), then restore the AVR-DA default for subsequent tests.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-27"/></traces>
      </test>

      <test name="updi_select_device_unknown_family_returns_minus1">
        <purpose><![CDATA[Call `updi_select_device(-1, "AVR-XYZ")` and verify the function returns -1 (unknown family name) without modifying the active descriptor.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-27"/></traces>
      </test>

      <test name="updi_family_from_partname_maps_all_known_prefixes">
        <purpose><![CDATA[Call `updi_family_from_partname()` once per supported family with a representative lowercase part-name (`"avr128da28"`, `"avr128db48"`, `"avr64dd32"`, `"avr32du28"`, `"avr32sd20"`) and verify the returned string equals the matching `g_device_table[]` family literal (`"AVR-DA"`, `"AVR-DB"`, `"AVR-DD"`, `"AVR-DU"`, `"AVR-SD"`).]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-28"/></traces>
      </test>

      <test name="updi_family_from_partname_rejects_invalid_inputs">
        <purpose><![CDATA[Verify `updi_family_from_partname()` returns `NULL` for `NULL`, the empty string, strings not beginning with `"avr"` (`"not-an-avr"`), the unsupported NVMCTRL v3 families (`"avr32ea48"`, `"avr16eb14"`), and inputs missing the digit run between `"avr"` and the family-letters (`"avrda28"`). NULL signals "no usable identification" to `app_main()` (LLR-MAIN-13).]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-28"/></traces>
      </test>

      <test name="updi_crc32_returns_zero_for_empty_buffer">
        <purpose><![CDATA[Call `updi_crc32(NULL, 0)` and assert the result equals `0x00000000` — the canonical IEEE 802.3 CRC-32 of the empty message (verified against zlib and crc32 CLI tools).]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-30"/></traces>
      </test>

      <test name="updi_crc32_matches_known_vector_for_123456789">
        <purpose><![CDATA[Call `updi_crc32()` with the canonical IEEE 802.3 / zlib test vector — the 9-byte ASCII string `"123456789"` — and assert the result equals the published expected value `0xCBF43926`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-30"/></traces>
      </test>

      <test name="updi_crc32_matches_known_vector_for_single_zero_byte">
        <purpose><![CDATA[Call `updi_crc32()` with a single 0x00 byte and assert the result equals `0xD202EF8D`, the canonical IEEE 802.3 CRC-32 of a one-byte zero message. Confirms that the polynomial reflection, initial value (`0xFFFFFFFF`), and final XOR (`0xFFFFFFFF`) are all implemented correctly even on the shortest non-empty input.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-30"/></traces>
      </test>
    </file>

    <!-- ============================================================ -->
    <!-- src/gdb_rsp.c unit tests                                     -->
    <!-- ============================================================ -->
    <file path="tests/test_rsp.c" role="unit" count="33">
      <header><![CDATA[Unit tests for `src/gdb_rsp.c`: socket configuration, RSP packet framing, register and memory handlers, hardware-breakpoint comparator management, OCD-based execution control, async Ctrl-C interrupt, detach cleanup, session lifecycle packets, and monitor relay. Uses a `socketpair()` as a drop-in socket substitute and replaces every `updi_*`, `fsm_*`, and `monitor_dispatch` call site with `ld --wrap` mocks so all tests run without real silicon.]]></header>

      <test name="rsp_listen_sets_so_reuseaddr_before_bind">
        <purpose><![CDATA[Verify that `rsp_listen()` calls `setsockopt()` with `SO_REUSEADDR` enabled before calling `bind()`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-01"/></traces>
      </test>

      <test name="rsp_accept_sets_tcp_nodelay_on_client_socket">
        <purpose><![CDATA[After `rsp_accept()` returns a client socket, verify that `TCP_NODELAY` has been set on that socket via `getsockopt()`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-01"/></traces>
      </test>

      <test name="rsp_recv_packet_discards_leading_ack_nak_bytes">
        <purpose><![CDATA[Feed a byte stream that begins with `+` and `-` characters before the `$` delimiter and verify that `rsp_recv_packet()` discards them and correctly decodes the payload that follows.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-02"/></traces>
      </test>

      <test name="rsp_recv_packet_sends_plus_on_valid_checksum">
        <purpose><![CDATA[Feed a well-formed RSP packet with a correct XOR checksum and verify that `rsp_recv_packet()` writes `+` to the socket and returns the payload length.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-02"/></traces>
      </test>

      <test name="rsp_recv_packet_sends_minus_and_returns_minus1_on_bad_checksum">
        <purpose><![CDATA[Feed an RSP packet with a deliberately corrupted checksum and verify that `rsp_recv_packet()` writes `-` to the socket and returns -1.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-02"/></traces>
      </test>

      <test name="on_read_regs_g_returns_78_char_hex_string">
        <purpose><![CDATA[With the mock OCD layer returning canned values for `updi_ocd_read_gpr/sreg/sp/pc`, dispatch a `g` packet and verify that the RSP reply payload is exactly 78 hex characters in GDB's AVR register-block layout.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-03"/></traces>
      </test>

      <test name="on_read_regs_g_places_pc_little_endian_at_positions_70_77">
        <purpose><![CDATA[Inject a known PC byte address via the mocked `updi_ocd_read_pc()` and verify that hex characters at positions 70–77 of the `g` reply encode the PC as 4-byte little-endian.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-03"/></traces>
      </test>

      <test name="on_read_regs_non_active_thread_uses_fsm_register_frame">
        <purpose><![CDATA[Select a non-active virtual thread via `Hg`, dispatch a `g` packet, and verify that the handler routes the read through `fsm_get_registers()` (not the OCD register file).]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-03"/></traces>
      </test>

      <test name="on_write_regs_G_writes_all_registers_via_ocd">
        <purpose><![CDATA[Dispatch a `G` packet containing 78 hex characters and verify that the handler calls `updi_ocd_write_gpr()` 32 times (one per GPR) plus `updi_ocd_write_sreg()`, `updi_ocd_write_sp()`, and `updi_ocd_write_pc()` once each with the decoded values, and replies `OK`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-04"/></traces>
      </test>

      <test name="on_write_regs_P_writes_single_register_via_ocd">
        <purpose><![CDATA[Dispatch a `P5=AB` packet and verify that the handler calls `updi_ocd_write_gpr(n=5, val=0xAB)` exactly once and that no other OCD writer is invoked.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-04"/></traces>
      </test>

      <test name="on_read_mem_m_calls_updi_mem_read_and_returns_hex">
        <purpose><![CDATA[Dispatch an `m addr,4` packet for a SRAM address and verify that the handler calls `updi_mem_read()` with the UPDI physical address (bit 23 flipped) and length, and returns an 8-character hex string.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-05"/></traces>
      </test>

      <test name="on_write_mem_M_calls_updi_mem_write_for_sram_address">
        <purpose><![CDATA[Dispatch an `M addr,4:data` packet targeting a GDB data-space address and verify that the handler calls `updi_mem_write()` (not `updi_nvm_write_flash`) and replies `OK`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-06"/></traces>
      </test>

      <test name="on_write_mem_X_calls_nvm_write_flash_for_flash_address">
        <purpose><![CDATA[Dispatch an `X addr,2:data` packet targeting a GDB program-space address and verify that the handler calls `updi_nvm_write_flash()` and replies `OK`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-06"/></traces>
      </test>

      <test name="on_insert_bp_calls_updi_ocd_set_hw_bp_with_byte_addr">
        <purpose><![CDATA[Dispatch a `Z0 addr,2` packet and verify that the handler calls `updi_ocd_set_hw_bp(idx, byte_addr)` with the GDB byte address (masked to 23 bits) into the first free `RspContext.hw_bp_addr[]` slot, and replies `OK`. The handler shall not call `updi_nvm_write_flash()`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-07"/></traces>
      </test>

      <test name="on_insert_bp_second_slot_succeeds">
        <purpose><![CDATA[Dispatch two `Z0` inserts at distinct addresses and verify that `updi_ocd_set_hw_bp()` is called once for slot 0 and once for slot 1, that both slots in `RspContext.hw_bp_addr[]` are populated, and that both replies are `OK`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-07"/></traces>
      </test>

      <test name="on_insert_bp_duplicate_returns_ok_without_reprogramming">
        <purpose><![CDATA[Dispatch the same `Z0 addr,2` breakpoint insert twice and verify that `updi_ocd_set_hw_bp()` is called only once and that the second call returns `OK` immediately.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-07"/></traces>
      </test>

      <test name="on_remove_bp_calls_updi_ocd_clear_hw_bp">
        <purpose><![CDATA[After inserting a breakpoint with `Z0`, dispatch `z0` for the same address and verify that `updi_ocd_clear_hw_bp()` is called with the slot index that held it, the shadow slot is reset to `0xFFFFFFFF`, and the reply is `OK`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-08"/></traces>
      </test>

      <test name="on_remove_bp_unknown_address_returns_ok_for_resync">
        <purpose><![CDATA[Dispatch `z0` for an address that is not present in either `RspContext.hw_bp_addr[]` slot (e.g. after a server restart that lost the shadow) and verify that the handler replies `OK` rather than an error, so GDB resync after reconnect is non-fatal.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-08"/></traces>
      </test>

      <test name="on_insert_bp_returns_E08_when_both_slots_occupied">
        <purpose><![CDATA[Fill both `RspContext.hw_bp_addr[]` slots with distinct addresses, then dispatch a third `Z0` insert at a different address and verify the reply is `E08` and that no further `updi_ocd_set_hw_bp()` call is made.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-09"/></traces>
      </test>

      <test name="on_step_s_calls_updi_step_and_sends_T05_stop_reason">
        <purpose><![CDATA[Dispatch an `s` packet with the mock `updi_ocd_read_halt_status()` returning `OCD_STATUS1 = 0x00` and verify that the handler calls `updi_step()` once, rebuilds the FSM thread list, and sends a stop-reason reply of the form `T05thread:<id>;`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-10"/></traces>
      </test>

      <test name="on_continue_calls_updi_run_then_fsm_invalidate">
        <purpose><![CDATA[Dispatch a `c` packet with the mock `updi_ocd_poll_halted()` returning 0 on the first poll, and verify that the handler calls `updi_run()` and then `fsm_invalidate()` before entering the poll loop.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-11"/></traces>
      </test>

      <test name="on_continue_rebuilds_thread_list_after_halt_and_sends_stop">
        <purpose><![CDATA[After the simulated halt triggered by `on_continue` (mock `updi_ocd_poll_halted()` returns 0 on the Nth call), verify that `fsm_build_thread_list()` is called and the handler sends a stop-reason packet to the GDB client.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-11"/></traces>
      </test>

      <test name="on_continue_ctrl_c_returns_T02">
        <purpose><![CDATA[Dispatch a `c` packet, then while `on_continue` is in its select/poll loop write `\x03` to the GDB client side of the socketpair. Verify that the handler calls `updi_halt()` and replies `T02thread:<id>;` (SIGINT).]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-11"/></traces>
      </test>

      <test name="on_continue_returns_E01_after_UPDI_FAIL_MAX_consecutive_poll_failures">
        <purpose><![CDATA[Configure the mock `updi_ocd_poll_halted()` to return -1 unconditionally. Dispatch a `c` packet and verify that the handler gives up after `UPDI_FAIL_MAX` (8) consecutive failures and replies `E01` rather than spinning indefinitely.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-11"/></traces>
      </test>

      <test name="on_halt_reason_returns_T02_when_extbrk_set">
        <purpose><![CDATA[Set the mock `updi_ocd_read_halt_status()` to return `OCD_STATUS1 = OCD_STATUS1_EXTBRK (0x10)`, dispatch a `?` packet, and verify the reply is `T02thread:<id>;` (SIGINT). With `OCD_STATUS1 = 0x00` the reply must be `T05thread:<id>;` (SIGTRAP).]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-17"/></traces>
      </test>

      <test name="rsp_dispatch_qsupported_returns_feature_string_no_target_access">
        <purpose><![CDATA[Dispatch a `qSupported` packet and verify that a non-empty feature string is returned and that no UPDI calls are made.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-12"/></traces>
      </test>

      <test name="rsp_dispatch_qattached_returns_1_no_target_access">
        <purpose><![CDATA[Dispatch a `qAttached` packet and verify the reply is `1` and that no UPDI calls are made.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-12"/></traces>
      </test>

      <test name="on_detach_D_resumes_target_closes_socket_resets_gdb_fd">
        <purpose><![CDATA[Dispatch a `D` packet and verify that the handler calls `updi_run()`, closes the GDB client socket, and sets `*ctx->gdb_fd_p` to -1 without exiting the server process.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-13"/></traces>
      </test>

      <test name="on_detach_clears_hw_bps_before_run">
        <purpose><![CDATA[Insert two `Z0` breakpoints, dispatch a `D` packet, and verify the call sequence: `updi_ocd_clear_hw_bp()` is called for both occupied slots BEFORE `updi_run()`, and afterwards both `RspContext.hw_bp_addr[]` slots are `0xFFFFFFFF`. This guarantees the next GDB session inherits clean silicon comparators.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-18"/></traces>
      </test>

      <test name="on_kill_k_sets_g_quit_to_1">
        <purpose><![CDATA[Dispatch a `k` packet and verify that `g_quit` is set to 1, causing the event loop to exit on its next iteration.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-14"/></traces>
      </test>

      <test name="on_monitor_qRcmd_passes_hex_body_to_monitor_dispatch">
        <purpose><![CDATA[Dispatch a `qRcmd` packet with a hex-encoded command body and verify that `monitor_dispatch()` receives the same hex-encoded string without any pre-decoding.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-15"/></traces>
      </test>

      <test name="on_monitor_returns_ok_when_monitor_dispatch_succeeds">
        <purpose><![CDATA[Inject a mock `monitor_dispatch()` that returns 0 and verify that the `qRcmd` handler sends `OK` to the GDB client.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-15"/></traces>
      </test>

      <test name="on_monitor_sends_o_packet_error_on_updi_failure">
        <purpose><![CDATA[Inject a mock `monitor_dispatch()` that returns -1 and verify that the `qRcmd` handler sends an O-packet error message to the GDB console.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-15"/></traces>
      </test>

      <test name="H_packet_stores_thread_id_for_register_operations">
        <purpose><![CDATA[Dispatch `Hg3` (set thread 3 for register operations) and then a `g` packet, and verify that `fsm_get_registers()` is called with the thread entry for GDB thread ID 3.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-16"/></traces>
      </test>

      <test name="H_packet_minus1_and_0_both_map_to_active_fsm_thread">
        <purpose><![CDATA[Dispatch `Hg-1` and `Hg0` in turn, each followed by a `g` packet, and verify that both select the active FSM thread's register frame.]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-16"/></traces>
      </test>
    </file>

    <!-- ============================================================ -->
    <!-- src/elf_parser.c unit tests                                  -->
    <!-- ============================================================ -->
    <file path="tests/test_elf.c" role="unit" count="16">
      <header><![CDATA[Unit tests for `src/elf_parser.c`: ELF validation, symbol table loading, avrOS sentinel symbol discovery, Harvard address conversion, fail-safe degradation, and context cleanup. Fixture ELF binaries are generated by a helper script in `tests/fixtures/`.]]></header>

      <test name="elf_open_accepts_valid_avr_elf32_binary">
        <purpose><![CDATA[Call `elf_open()` with a well-formed AVR ELF32 fixture file and verify the return value is non-negative and `ctx.sym_count > 0`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-ELF-01"/></traces>
      </test>

      <test name="elf_open_returns_minus1_on_invalid_elf_magic">
        <purpose><![CDATA[Call `elf_open()` with a file whose first 4 bytes are not the ELF magic sequence and verify the return value is -1.]]></purpose>
        <traces><trace target="LLR" ref="LLR-ELF-01"/></traces>
      </test>

      <test name="elf_open_returns_minus1_on_wrong_machine_type">
        <purpose><![CDATA[Call `elf_open()` with a valid ELF32 file that has `e_machine != EM_AVR` and verify the return value is -1.]]></purpose>
        <traces><trace target="LLR" ref="LLR-ELF-01"/></traces>
      </test>

      <test name="elf_open_loads_symtab_and_strtab_into_heap_buffers">
        <purpose><![CDATA[After a successful `elf_open()`, verify that `ctx.symtab` and `ctx.strtab` are non-NULL heap pointers, and that `ctx.sym_count` and `ctx.strtab_size` reflect the actual section sizes.]]></purpose>
        <traces><trace target="LLR" ref="LLR-ELF-02"/></traces>
      </test>

      <test name="elf_open_frees_partial_allocs_and_returns_minus1_on_malloc_failure">
        <purpose><![CDATA[Inject a mock `malloc()` that fails on the second allocation, call `elf_open()`, and verify the return value is -1 and no heap memory remains allocated.]]></purpose>
        <traces><trace target="LLR" ref="LLR-ELF-02"/></traces>
      </test>

      <test name="elf_find_avros_tables_performs_single_linear_scan">
        <purpose><![CDATA[Instrument `elf_find_avros_tables()` with a mock symbol table and verify that each symbol entry is examined at most once regardless of how many avrOS sentinels are present.]]></purpose>
        <traces><trace target="LLR" ref="LLR-ELF-03"/></traces>
      </test>

      <test name="elf_find_avros_tables_populates_all_7_avros_sentinel_fields">
        <purpose><![CDATA[Call `elf_find_avros_tables()` with a fixture ELF that contains all 7 avrOS sentinel symbols (`__start_FSM_TABLE`, `__stop_FSM_TABLE`, `__start_QUE_TABLE`, `__stop_QUE_TABLE`, `__start_EVNT_TABLE`, `__stop_EVNT_TABLE`, `currStateMachine`) and verify every populated field of `AvrOsSymbolIndex` is non-zero.]]></purpose>
        <traces><trace target="LLR" ref="LLR-ELF-03"/></traces>
      </test>

      <test name="elf_flash_addr_applies_vma_minus_base_over_2_formula">
        <purpose><![CDATA[Call `elf_flash_addr()` with known `vma` and `ctx.flash_base` values and verify the return equals `(vma - flash_base) / 2`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-ELF-04"/></traces>
      </test>

      <test name="elf_flash_addr_all_avros_symbol_addresses_use_word_formula">
        <purpose><![CDATA[After `elf_find_avros_tables()`, verify that every FLASH-resident address stored in `AvrOsSymbolIndex` was computed via `elf_flash_addr()`, not as a raw VMA.]]></purpose>
        <traces><trace target="LLR" ref="LLR-ELF-04"/></traces>
      </test>

      <test name="elf_find_avros_tables_returns_0_on_partial_symbol_match">
        <purpose><![CDATA[Call `elf_find_avros_tables()` with a fixture ELF containing only 4 of the 8 avrOS sentinels and verify the return value is 0 (not -1).]]></purpose>
        <traces><trace target="LLR" ref="LLR-ELF-05"/></traces>
      </test>

      <test name="elf_find_avros_tables_zero_initialises_absent_symbol_fields">
        <purpose><![CDATA[After `elf_find_avros_tables()` on a partial-symbol ELF, verify that each field in `AvrOsSymbolIndex` that corresponds to a missing symbol is zero.]]></purpose>
        <traces><trace target="LLR" ref="LLR-ELF-05"/></traces>
      </test>

      <test name="elf_close_frees_symtab_strtab_and_closes_fd">
        <purpose><![CDATA[After a successful `elf_open()`, call `elf_close()` and verify that the file descriptor is closed and no memory is leaked (checked via Valgrind or AddressSanitizer).]]></purpose>
        <traces><trace target="LLR" ref="LLR-ELF-06"/></traces>
      </test>

      <test name="elf_close_safe_on_partially_initialised_context">
        <purpose><![CDATA[Construct an `ElfContext` with `symtab = NULL`, `strtab` allocated, and `fd = -1`, call `elf_close()`, and verify no crash or double-free occurs.]]></purpose>
        <traces><trace target="LLR" ref="LLR-ELF-06"/></traces>
      </test>

      <test name="elf_open_sets_flash_base_and_sram_base_from_pt_load_segments">
        <purpose><![CDATA[After a successful `elf_open()` on the full fixture, verify that `ctx.flash_base` equals the VMA of the first `PT_LOAD` segment (0x00000000 for the fixture) and `ctx.sram_base` equals the VMA of the second `PT_LOAD` segment (0x00804000 for the AVR128DA28 fixture).]]></purpose>
        <traces><trace target="LLR" ref="LLR-ELF-08"/></traces>
      </test>

      <test name="elf_open_extracts_device_name_from_deviceinfo_note">
        <purpose><![CDATA[After `elf_open()` on every AVR fixture (all built for AVR128DA28), verify `ctx.device_name` equals `"avr128da28"` — the lowercase part-name string extracted from the `.note.gnu.avr.deviceinfo` ELF note descriptor.]]></purpose>
        <traces><trace target="LLR" ref="LLR-ELF-09"/></traces>
      </test>

      <test name="elf_open_leaves_device_name_empty_when_note_absent">
        <purpose><![CDATA[Copy the full fixture to a temp path and zero every `"avr<digit>"` ASCII triplet in the file so the deviceinfo heuristic finds nothing, then `elf_open()` the rewritten ELF and verify `elf_open()` still returns 0 and that `ctx.device_name` is the empty string. This proves the parser's note-extraction step is non-fatal on inputs that lack the note.]]></purpose>
        <traces><trace target="LLR" ref="LLR-ELF-09"/></traces>
      </test>
    </file>

    <!-- ============================================================ -->
    <!-- src/fsm_mapper.c unit tests                                  -->
    <!-- ============================================================ -->
    <file path="tests/test_fsm.c" role="unit" count="11">
      <header><![CDATA[Unit tests for `src/fsm_mapper.c`: FSM table reading, stable GDB thread ID assignment, active-thread identification, g-packet register frame construction, thread count capping, and stack-free guarantees. All UPDI calls are stubbed via a mock layer.]]></header>

      <test name="fsm_build_thread_list_reads_fsm_table_from_flash_via_updi">
        <purpose><![CDATA[Verify that `fsm_build_thread_list()` calls `updi_mem_read()` targeting `idx->fsm_table_addr` and reads `idx->fsm_table_count` consecutive 9-byte `fsmStateMachineDescr_t` records.]]></purpose>
        <traces><trace target="LLR" ref="LLR-FSM-01"/></traces>
      </test>

      <test name="fsm_build_thread_list_returns_minus1_on_updi_failure">
        <purpose><![CDATA[Inject a mock `updi_mem_read()` that returns -1 and verify that `fsm_build_thread_list()` propagates the error by returning -1.]]></purpose>
        <traces><trace target="LLR" ref="LLR-FSM-01"/></traces>
      </test>

      <test name="fsm_build_thread_list_assigns_1_based_thread_ids_in_order">
        <purpose><![CDATA[Build a thread list from 3 FSM entries and verify that the resulting `gdb_id` values are 1, 2, and 3, in the order the entries appear in the FLASH table.]]></purpose>
        <traces><trace target="LLR" ref="LLR-FSM-02"/></traces>
      </test>

      <test name="fsm_build_thread_list_thread_ids_stable_across_calls">
        <purpose><![CDATA[Call `fsm_build_thread_list()` twice with the same FSM table content and verify that each FSM entry maps to the same `gdb_id` on both calls.]]></purpose>
        <traces><trace target="LLR" ref="LLR-FSM-02"/></traces>
      </test>

      <test name="fsm_build_thread_list_sets_active_thread_from_current_fsm_ptr">
        <purpose><![CDATA[Inject a mock `currStateMachine` SRAM value matching the second FSM entry's `stateMachine` pointer and verify that only that entry has `is_active == true` and `ctx->active_id == 2`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-FSM-03"/></traces>
      </test>

      <test name="fsm_build_thread_list_sets_active_id_0_when_no_entry_matches">
        <purpose><![CDATA[Inject a `current_fsm` SRAM value that does not match any registered FSM and verify that `ctx->active_id == 0`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-FSM-03"/></traces>
      </test>

      <test name="fsm_get_registers_places_state_fn_as_pc_at_hex_positions_70_77">
        <purpose><![CDATA[Call `fsm_get_registers()` for a thread whose `state_fn = 0xABCD` and verify that the 78-character g-packet buffer contains the little-endian encoding of 0xABCD at hex character positions 70–77.]]></purpose>
        <traces><trace target="LLR" ref="LLR-FSM-04"/></traces>
      </test>

      <test name="fsm_get_registers_non_active_r0_r31_sreg_spl_sph_all_zero">
        <purpose><![CDATA[Call `fsm_get_registers()` for a non-active thread and verify that hex positions 0–69 of the 78-character buffer are all `'0'`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-FSM-04"/></traces>
      </test>

      <test name="fsm_get_registers_active_thread_reads_live_sreg_spl_sph">
        <purpose><![CDATA[Call `fsm_get_registers()` for the active thread with a mock that returns known SREG, SPL, and SPH values, and verify they appear at hex positions 64–65, 66–67, and 68–69 respectively.]]></purpose>
        <traces><trace target="LLR" ref="LLR-FSM-04"/></traces>
      </test>

      <test name="fsm_build_thread_list_caps_at_32_entries_and_logs_warning">
        <purpose><![CDATA[Inject an FSM table with 33 entries and verify that `fsm_build_thread_list()` returns `FSM_MAX_THREADS` (32) and emits a diagnostic warning without returning an error.]]></purpose>
        <traces><trace target="LLR" ref="LLR-FSM-05"/></traces>
      </test>

      <test name="fsm_get_registers_non_active_no_updi_read_of_stack">
        <purpose><![CDATA[Call `fsm_get_registers()` for a non-active thread and verify that `updi_mem_read()` is never called with any stack-region address.]]></purpose>
        <traces><trace target="LLR" ref="LLR-FSM-06"/></traces>
      </test>
    </file>

    <!-- ============================================================ -->
    <!-- src/monitor.c unit tests                                     -->
    <!-- ============================================================ -->
    <file path="tests/test_monitor.c" role="unit" count="10">
      <header><![CDATA[Unit tests for `src/monitor.c`: hex command decoding, prefix matching, per-sub-command UPDI reads, O-packet output formatting, 512-byte buffer constraint, and the no-halt guarantee. All UPDI calls are intercepted by a mock layer that records calls and returns injected data.]]></header>

      <test name="monitor_dispatch_hex_decodes_cmd_before_prefix_matching">
        <purpose><![CDATA[Pass `monitor_dispatch()` a hex-encoded `"avros events"` command and verify the function routes to `cmd_events()`, confirming that decoding occurs before prefix matching.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MON-01"/></traces>
      </test>

      <test name="monitor_dispatch_treats_invalid_hex_sequence_as_unrecognised">
        <purpose><![CDATA[Pass `monitor_dispatch()` an odd-length hex string and verify it returns -2 without crashing or producing partial output.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MON-01"/></traces>
      </test>

      <test name="monitor_dispatch_rejects_cmd_without_avros_space_prefix">
        <purpose><![CDATA[Pass a hex-encoded command that decodes to `"other events"` and verify `monitor_dispatch()` returns -2.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MON-02"/></traces>
      </test>

      <test name="monitor_dispatch_sends_usage_hint_o_packet_on_bad_prefix">
        <purpose><![CDATA[Pass a command with an unrecognised prefix and verify that `monitor_dispatch()` sends at least one RSP O-packet containing a usage-hint string before returning -2.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MON-02"/></traces>
      </test>

      <test name="cmd_events_reads_event_count_descriptors_from_evnt_table">
        <purpose><![CDATA[Dispatch `"avros events"` and verify that `updi_mem_read()` is called with address `idx->event_table_addr` and a length equal to `idx->event_count` × 4 bytes (`evntDescriptor_t` size).]]></purpose>
        <traces><trace target="LLR" ref="LLR-MON-03"/></traces>
      </test>

      <test name="cmd_events_reports_name_and_status_value_per_descriptor">
        <purpose><![CDATA[Inject two `evntDescriptor_t` records pointing at FLASH names `"RX"`, `"TX"` and 1-byte SRAM status values `0x01`, `0x00` and verify the decoded O-packet text contains both names and both status bytes formatted as `0xXX`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MON-03"/></traces>
      </test>

      <test name="cmd_events_output_is_hex_encoded_o_packet">
        <purpose><![CDATA[Dispatch `"avros events"` and verify the single captured `rsp_send_packet()` payload begins with `'O'` and every subsequent character is an uppercase hex digit (`[0-9A-F]`), with an overall odd length.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MON-06"/></traces>
      </test>

      <test name="cmd_queues_reads_queue_count_descriptors_from_que_table">
        <purpose><![CDATA[Dispatch `"avros queues"` and verify that `updi_mem_read()` is called with `idx->queue_table_addr` and a length equal to `idx->queue_count` × 10 bytes (`queDescriptor_t` size).]]></purpose>
        <traces><trace target="LLR" ref="LLR-MON-04"/></traces>
      </test>

      <test name="cmd_queues_formats_capacity_and_sizeofelement_for_each_entry">
        <purpose><![CDATA[Inject two `queDescriptor_t` records with `(capacity, sizeOfElement)` of `(8, 1)` and `(16, 4)` respectively and verify the decoded O-packet text contains `"capacity=8 sizeOfElement=1"` and `"capacity=16 sizeOfElement=4"`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MON-04"/></traces>
      </test>

      <test name="monitor_dispatch_and_helpers_never_call_updi_halt">
        <purpose><![CDATA[Execute both sub-commands (`events`, `queues`) through `monitor_dispatch()` and verify that `updi_halt()` is never called by inspecting the mock call log.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MON-07"/></traces>
      </test>
    </file>

    <!-- ============================================================ -->
    <!-- Integration tests (system-level HLR properties)             -->
    <!-- ============================================================ -->
    <file path="tests/test_integration.c" role="integration" count="4">
      <header><![CDATA[Integration tests validating system-level properties not covered by per-LLR unit tests: server startup latency (HLR-005), RSP protocol purity (HLR-020), and build portability (HLR-033, HLR-034).]]></header>

      <test name="integration_server_ready_within_2_seconds_of_client_connect">
        <purpose><![CDATA[Launch `avrOSdb` against a loopback UPDI stub and a fixture ELF, connect a GDB client, and measure the elapsed time from TCP accept to the first valid RSP response. Assert the elapsed time is less than 2.0 seconds.]]></purpose>
        <traces><trace target="HLR" ref="HLR-005"/></traces>
      </test>

      <test name="integration_server_emits_only_standard_rsp_no_ide_extensions">
        <purpose><![CDATA[Capture all TCP traffic from a full debug session (connect, register read, memory read, continue, breakpoint, detach) and verify that every server-originated packet conforms to the GDB RSP specification with no DAP, Cortex-Debug, or other IDE-specific packet types present.]]></purpose>
        <traces><trace target="HLR" ref="HLR-020"/></traces>
      </test>

      <test name="build_compiles_clean_on_linux_with_c99_and_posix">
        <purpose><![CDATA[Execute `make` with `CC=gcc CFLAGS="-std=c99 -Wall -Wextra -Wpedantic -D_POSIX_C_SOURCE=200809L"` and assert that the build exits with status 0 and produces no compiler warnings or errors on a Linux x86-64 host.]]></purpose>
        <traces>
          <trace target="HLR" ref="HLR-033"/>
          <trace target="LLR" ref="LLR-ELF-07"/>
        </traces>
      </test>

      <test name="runtime_links_only_libc_no_heavyweight_deps">
        <purpose><![CDATA[Run `ldd avrOSdb` on the linked binary and verify that the only shared-library dependency is `libc.so`. Assert that no Java runtime, Python interpreter, Electron libraries, or other non-POSIX dependencies appear.]]></purpose>
        <traces><trace target="HLR" ref="HLR-034"/></traces>
      </test>
    </file>

    <!-- ============================================================ -->
    <!-- Installation and documentation tests                         -->
    <!-- ============================================================ -->
    <file path="tests/test_install.c" role="integration" count="8">
      <header><![CDATA[Verification tests for the Makefile install/uninstall targets, the `check-tools` pre-flight target, and the Unix man page. Tests fork a subprocess or invoke `make` directly and inspect the resulting file-system state or exit codes.]]></header>

      <test name="check_tools_exits_nonzero_when_required_tool_is_absent">
        <purpose><![CDATA[Temporarily shadow a required tool (e.g. `avr-gcc`) with a dummy wrapper that is not executable, then run `make check-tools` and verify it exits with a non-zero status and prints a diagnostic to stderr naming the missing tool.]]></purpose>
        <traces><trace target="LLR" ref="LLR-INST-01"/></traces>
      </test>

      <test name="make_install_places_binary_at_prefix_bin">
        <purpose><![CDATA[Run `make install PREFIX=/tmp/aod_test_$$` (using a temporary directory as the prefix) and verify that the file `$(PREFIX)/bin/avrOSdb` exists and is executable after the target completes.]]></purpose>
        <traces><trace target="LLR" ref="LLR-INST-02"/></traces>
      </test>

      <test name="make_install_places_man_page_at_prefix_man1">
        <purpose><![CDATA[After `make install PREFIX=...`, verify that the file `$(PREFIX)/share/man/man1/avrOSdb.1` exists and that `man -l` parses it without error.]]></purpose>
        <traces><trace target="LLR" ref="LLR-INST-02"/><trace target="LLR" ref="LLR-INST-05"/></traces>
      </test>

      <test name="make_uninstall_removes_all_installed_files">
        <purpose><![CDATA[After `make install PREFIX=...`, run `make uninstall PREFIX=...` and verify that both `$(PREFIX)/bin/avrOSdb` and `$(PREFIX)/share/man/man1/avrOSdb.1` no longer exist.]]></purpose>
        <traces><trace target="LLR" ref="LLR-INST-03"/></traces>
      </test>

      <test name="user_manual_exists_and_contains_required_sections">
        <purpose><![CDATA[Read `doc/UserManual.md` and verify the file exists and contains all required section headings: Prerequisites, Build, Connection Wiring, Usage (or Options), and at least one Example.]]></purpose>
        <traces><trace target="LLR" ref="LLR-INST-04"/></traces>
      </test>

      <test name="make_bundle_produces_deb_package">
        <purpose><![CDATA[Run `make bundle VERSION=0.1.0` and verify that `dist/avrOSdb_0.1.0_amd64.deb` exists, that `dpkg-deb --info` exits 0, and that `dpkg-deb --contents` lists both `./usr/bin/avrOSdb` and `./usr/share/man/man1/avrOSdb.1`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-INST-06"/></traces>
      </test>

      <test name="make_bundle_produces_rpm_package">
        <purpose><![CDATA[Run `make bundle VERSION=0.1.0` and verify that `dist/avrOSdb-0.1.0-1.x86_64.rpm` exists and that `rpm -qp --list` exits 0 and lists both `/usr/bin/avrOSdb` and `/usr/share/man/man1/avrOSdb.1`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-INST-07"/></traces>
      </test>

      <test name="make_bundle_produces_homebrew_formula">
        <purpose><![CDATA[Run `make bundle VERSION=0.1.0` and verify that `dist/avrOSdb.rb` exists, is valid Ruby syntax (parseable by `ruby -c`), and contains the required fields: `desc`, `url`, `sha256`, `version`, and an `install` block.]]></purpose>
        <traces><trace target="LLR" ref="LLR-INST-08"/></traces>
      </test>
    </file>

    <!-- ============================================================ -->
    <!-- Device-signature diagnostic mode tests (Phase 7)             -->
    <!-- ============================================================ -->
    <file path="tests/test_device.c" role="unit" count="6">
      <header><![CDATA[Unit and harness-level tests covering the `--device` one-shot diagnostic mode: argument parsing, mutual exclusion with `--load`, `updi_read_device_info()` success and failure paths over a PTY, the human-readable report format, and verification that the diagnostic path bypasses the GDB listener.]]></header>

      <test name="parse_args_accepts_device_flag_without_elf_operand">
        <purpose><![CDATA[Invoke `parse_args()` with `argv = {"avrOSdb", "--device", "/dev/ttyUSB0"}` and assert `cfg.device_info == true`, `cfg.serial_device == "/dev/ttyUSB0"`, `cfg.elf_path == NULL`, and `parse_args()` returns without exiting.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-08"/></traces>
      </test>

      <test name="parse_args_rejects_device_combined_with_load">
        <purpose><![CDATA[Fork a subprocess that calls `parse_args()` with `--device --load /dev/ttyUSB0 fw.elf`; assert the child exits with status 1 and that captured stderr contains `"--device is mutually exclusive with --load"`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-08"/></traces>
      </test>

      <test name="updi_read_device_info_returns_sigrow_and_asi_bytes">
        <purpose><![CDATA[Open a PTY pair; in a helper thread script the slave end to echo half-duplex bytes and respond to a SIGROW DEVICEID read at `0x1100`-`0x1102` with canned signature `0x1E 0x97 0x0A`, a SYSCFG.REVID read at `0x0F01` returning `0xA6`, a SIGROW.SERNUM read at `0x1110`-`0x111F` returning a canned 16-byte serial number, and to LDCS reads of SYS_STATUS / KEY_STATUS / STATUSB. Call `updi_read_device_info()` and assert all struct fields match the canned values and that the function returns 0 with `info.fail_op == NULL`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-13"/></traces>
      </test>

      <test name="updi_read_device_info_reports_failed_step_on_nak">
        <purpose><![CDATA[Drive the PTY harness to return an unexpected byte in place of the SIGROW response. Assert `updi_read_device_info()` returns -1, `info.fail_op` equals the string `"sigrow"`, and `info.fail_errno` is negative.]]></purpose>
        <traces><trace target="LLR" ref="LLR-UPDI-13"/></traces>
      </test>

      <test name="run_device_mode_prints_report_to_stdout">
        <purpose><![CDATA[Capture `stdout` from `run_device_mode()` driven by the PTY harness with signature `0x1E 0x97 0x0A`. Assert the captured output contains the literal lines `Serial device:`, `Baud rate:`, `Signature:`, `Family:`, `Revision:`, `Serial:`, and `UPDI status:` (case-sensitive prefix match).]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-09"/></traces>
      </test>

      <test name="device_mode_does_not_call_rsp_listen">
        <purpose><![CDATA[Link the test binary with a stub `rsp_listen()` that flags a global. Invoke `run_device_mode()` to completion and assert the flag remains clear, proving the diagnostic path bypasses GDB listener startup.]]></purpose>
        <traces><trace target="LLR" ref="LLR-MAIN-09"/></traces>
      </test>
    </file>

    <!-- ============================================================ -->
    <!-- On-target hardware integration tests (manual; not in CI)     -->
    <!-- ============================================================ -->
    <file path="tests/hw/hw_test.c" role="hardware" count="22">
      <header><![CDATA[On-target hardware integration test harness for the AVR-Dx UPDI driver, device-info report, SRAM/FLASH memory access, and the GDB RSP server. Manual-only: never invoked by `make test`. Driven by the Makefile targets `hw-test`, `hw-test-nvm`, `hw-test-rsp`, and `hw-test-all`. Configurable via `HW_PORT`, `HW_DEVICE_ID`, `HW_SRAM_ADDR`, `HW_FLASH_PAGE_ADDR`, `HW_RSP_PORT`, `HW_TEST_ELF`, `HW_ADDR_24BIT`, `HW_VERBOSE` (env or CLI). Destructive groups gated by `HW_TEST_NVM_CONFIRM=YES` (FLASH) and `HW_TEST_RSP=YES` (RSP).]]></header>

      <test name="A1_port_open_and_cold_start_handshake">
        <purpose><![CDATA[Open the configured serial device and execute the UPDI cold-start handshake (BREAK + SYNCH). Assert that `updi_open()` returns a valid file descriptor and that the handshake completes without error. Implicitly verifies that `HW_PORT` / `--port` resolution and CLI-overrides-environment precedence work end-to-end.]]></purpose>
        <traces><trace target="LLR" ref="LLR-HWTEST-01"/><trace target="LLR" ref="LLR-HWTEST-02"/></traces>
      </test>

      <test name="A2_read_device_info">
        <purpose><![CDATA[Call `updi_read_device_info()` against the live target and capture the returned struct. Assert the call returns 0 and `info.fail_op == NULL`; the captured fields are reused by A3..A7.]]></purpose>
        <traces><trace target="LLR" ref="LLR-HWTEST-02"/></traces>
      </test>

      <test name="A3_deviceid_matches_expected">
        <purpose><![CDATA[Compare the SIGROW `DEVICEID0..2` bytes against the configured `HW_DEVICE_ID` (default `0x1E970A`). Assert exact byte-for-byte match.]]></purpose>
        <traces><trace target="LLR" ref="LLR-HWTEST-02"/></traces>
      </test>

      <test name="A4_signature_non_zero_non_ff">
        <purpose><![CDATA[Assert that the captured SIGROW signature is neither all `0x00` nor all `0xFF` (catches dead-bus / pull-up-only conditions that A3 might miss if `HW_DEVICE_ID` were misconfigured).]]></purpose>
        <traces><trace target="LLR" ref="LLR-HWTEST-02"/></traces>
      </test>

      <test name="A5_revid_non_zero">
        <purpose><![CDATA[Assert that the captured `SYSCFG.REVID` byte is non-zero.]]></purpose>
        <traces><trace target="LLR" ref="LLR-HWTEST-02"/></traces>
      </test>

      <test name="A6_sernum_non_zero_non_ff">
        <purpose><![CDATA[Assert that the 10-byte SIGROW `SERNUM` field is neither all `0x00` nor all `0xFF`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-HWTEST-02"/></traces>
      </test>

      <test name="A7_nvmprog_bit_clear">
        <purpose><![CDATA[Assert that the `ASI_SYS_STATUS.NVMPROG` bit is clear, proving the target is not stuck in a programming session at harness start-up.]]></purpose>
        <traces><trace target="LLR" ref="LLR-HWTEST-02"/></traces>
      </test>

      <test name="B0_sram_single_byte_round_trip">
        <purpose><![CDATA[Write one byte to `HW_SRAM_ADDR` via `updi_mem_write()`, read it back via `updi_mem_read()`, and assert byte-equality. Reaching this test through any of the four `hw-test*` Makefile targets implicitly verifies that the recipes forward `HW_PORT`, `HW_SRAM_ADDR`, and related variables to the child process.]]></purpose>
        <traces><trace target="LLR" ref="LLR-HWTEST-03"/><trace target="LLR" ref="LLR-HWTEST-06"/></traces>
      </test>

      <test name="B1_sram_64_byte_round_trip">
        <purpose><![CDATA[Write a 64-byte deterministic pattern to `HW_SRAM_ADDR`, read it back, and assert byte-for-byte match.]]></purpose>
        <traces><trace target="LLR" ref="LLR-HWTEST-03"/></traces>
      </test>

      <test name="B1a_sigrow_read_repeatability">
        <purpose><![CDATA[Read SIGROW twice via `updi_mem_read()` and assert both reads return identical bytes. Isolates the read path from the write path; a failure indicates a bus or framing problem rather than RAM corruption.]]></purpose>
        <traces><trace target="LLR" ref="LLR-HWTEST-03"/></traces>
      </test>

      <test name="B1b_sram_constant_0xAA_round_trip">
        <purpose><![CDATA[Write 64 bytes of constant `0xAA` to `HW_SRAM_ADDR` and read back. Asserts that data fidelity does not depend on byte value (rules out value-dependent framing or escape-byte bugs).]]></purpose>
        <traces><trace target="LLR" ref="LLR-HWTEST-03"/></traces>
      </test>

      <test name="B1c_sram_write_once_read_twice">
        <purpose><![CDATA[Write a known pattern to `HW_SRAM_ADDR` once, then read the region back twice; assert both reads are identical. On failure, the harness shall print a hint instructing the operator to choose a different `--sram-addr` / `HW_SRAM_ADDR`, because the live target CPU is writing to the probed region.]]></purpose>
        <traces><trace target="LLR" ref="LLR-HWTEST-03"/></traces>
      </test>

      <test name="B2_sram_600_byte_round_trip">
        <purpose><![CDATA[Write a 600-byte buffer to `HW_SRAM_ADDR`, read it back, and assert byte-equality. Exercises multi-chunk transfer and any internal block-size boundary handling in the UPDI driver.]]></purpose>
        <traces><trace target="LLR" ref="LLR-HWTEST-03"/></traces>
      </test>

      <test name="B3_flash_24bit_addressing_smoke">
        <purpose><![CDATA[When `HW_ADDR_24BIT` is true, perform a small read at `HW_FLASH_PAGE_ADDR` using the UPDI 24-bit addressing path; assert the call returns the expected number of bytes. Skipped when `HW_ADDR_24BIT` is false.]]></purpose>
        <traces><trace target="LLR" ref="LLR-HWTEST-03"/></traces>
      </test>

      <test name="C1_flash_erase_and_write">
        <purpose><![CDATA[When `HW_TEST_NVM_CONFIRM=YES`, erase the FLASH page at `HW_FLASH_PAGE_ADDR` and write a deterministic pattern via the NVM controller; assert all NVM controller status checks succeed. Skipped otherwise.]]></purpose>
        <traces><trace target="LLR" ref="LLR-HWTEST-04"/></traces>
      </test>

      <test name="C2_flash_read_back_verify">
        <purpose><![CDATA[Read the FLASH page written by C1 and assert a byte-for-byte match against the written pattern. Skipped unless `HW_TEST_NVM_CONFIRM=YES`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-HWTEST-04"/></traces>
      </test>

      <test name="D1_rsp_server_accepts_tcp_connection">
        <purpose><![CDATA[When `HW_TEST_RSP=YES`, `fork`+`execl` the production `build/avrOSdb` against the configured serial port; open a TCP client to `127.0.0.1:HW_RSP_PORT` and assert the connect succeeds within a bounded timeout. Skipped otherwise.]]></purpose>
        <traces><trace target="LLR" ref="LLR-HWTEST-05"/></traces>
      </test>

      <test name="D2_rsp_qsupported_round_trip">
        <purpose><![CDATA[Send an RSP `$qSupported#37` packet to the spawned server and assert a non-empty, properly-framed RSP reply is received. Skipped unless `HW_TEST_RSP=YES`. On completion the harness shall terminate the spawned server cleanly via SIGTERM + `waitpid`.]]></purpose>
        <traces><trace target="LLR" ref="LLR-HWTEST-05"/></traces>
      </test>

      <test name="D3_rsp_ctrl_c_interrupt_returns_T02">
        <purpose><![CDATA[With the spawned server running and a connected TCP client, dispatch a `c` (continue) RSP packet, wait long enough for the loop to enter its poll/select state, then send a single `0x03` byte (GDB Ctrl-C async-interrupt) on the same TCP connection. Assert that the server replies with a stop-reason packet whose signal field is `T02` (SIGINT) and that the live target is observably halted afterwards (via a subsequent `g` packet succeeding). Skipped unless `HW_TEST_RSP=YES`. Exercises the production async-interrupt path end-to-end through real silicon, validating both LLR-RSP-11 (Ctrl-C path) and LLR-UPDI-08 (`updi_halt()`).]]></purpose>
        <traces><trace target="LLR" ref="LLR-RSP-11"/><trace target="LLR" ref="LLR-UPDI-08"/><trace target="LLR" ref="LLR-HWTEST-05"/></traces>
      </test>

      <test name="F1_autobaud_picks_reliable_rung">
        <purpose><![CDATA[Close the harness UPDI fd, then call `updi_probe_baud(cfg->port, 8, visitor, NULL)`. The visitor records each rung's `{baud, errors, samples}` triple for the log. PASS if the returned baud is ≥ 19200 (the slowest rung in the ladder) — proving the probe walked the ladder, observed at least one zero-error rung on live silicon, and returned the highest-rate working candidate. Exercises the full read-only probe path end-to-end.]]></purpose>
        <traces><trace target="LLR" ref="LLR-HWTEST-07"/><trace target="LLR" ref="LLR-UPDI-31"/></traces>
      </test>

      <test name="F2_fuse_pretty_print_round_trip">
        <purpose><![CDATA[With the harness UPDI fd re-opened at `cfg->baud`, read the FUSES window and the 4-byte LOCK window via `updi_nvm_read()`, then call `updi_format_fuses()` into a 2 KiB stack buffer. PASS if the formatted output contains the literal substrings `WDTCFG`, `OSCCFG`, `SYSCFG0`, and `Lock:` — confirming the per-family fuse decode table fired and the lock-byte trailer was emitted.]]></purpose>
        <traces><trace target="LLR" ref="LLR-HWTEST-07"/><trace target="LLR" ref="LLR-UPDI-29"/><trace target="LLR" ref="LLR-UPDI-32"/></traces>
      </test>

      <test name="F3_prog_mode_end_to_end_with_verify">
        <purpose><![CDATA[Destructive — gated by `HW_TEST_NVM_CONFIRM=YES`. With the harness UPDI fd closed, `fork` and `execl` the production `build/avrOSdb --prog --erase --baud <N> <port> <elf>` against the bench ELF (`build/fixtures/all_nvm.elf` by default, which intentionally avoids FUSES/LOCK windows so the bench stays in a known state). Capture the child's stdout+stderr through an 8 KiB buffer. PASS if `WEXITSTATUS == 0` AND the captured output contains the literal `verify: OK`. Validates `--prog` end-to-end including chip-erase, FLASH/EEPROM/USERROW programming, and read-back verify against live silicon.]]></purpose>
        <traces><trace target="LLR" ref="LLR-HWTEST-07"/><trace target="LLR" ref="LLR-MAIN-15"/><trace target="LLR" ref="LLR-MAIN-16"/></traces>
      </test>
    </file>

  </tests>
</project>
