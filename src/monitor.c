/* src/monitor.c — stub (Phase 4 will implement) */
#include "monitor.h"

int monitor_dispatch(int rsp_fd, int updi_fd, const AvrOsSymbolIndex *idx,
                     const char *cmd)
{
    (void)rsp_fd; (void)updi_fd; (void)idx; (void)cmd;
    return -1;
}
